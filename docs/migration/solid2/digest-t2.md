
### lib-stores-task
  HAZ[high] src/stores/taskStore.ts:108 — updateBarPosition's existence guard `if (!tasks[id]) return;` reads the COMMITTED store, but every caller path can have a staged write to that same id earlier in the same synchronous turn. Concretely: ShowcaseDemo.tsx:657 calls updateTasks(tasks) and then updateBarPosition('task-a', ...) at :807; Gantt.tsx:395 calls updateBarPosition(taskId, {y}) inside a layout pass that may follow a task write;  || FIX: Move the guard inside the draft callback, where staged state IS visible (verified on rc.6): setTasks((draft) => { const task = draft[id]; if (!task?._bar) return; for (const k in position) { const v = position[k]; if (v !== undefined) task._bar[k] = v; } }); No flush() is needed anywhere and the 'no
  HAZ[high] src/stores/taskStore.ts:134 — removeTask writes with the 1.x path-setter delete idiom `setTasks(id, undefined)`. In solid-js 1.9.12 (store/dist/store.js:123-124) `value === undefined` triggers `delete state[property]`; in rc.6 the equivalent draft assignment `draft[id] = undefined` KEEPS the key (verified: Object.keys still returned ['a']). Any reader in the same turn or later that counts keys then reads a stale/wrong shape: t || FIX: setTasks((draft) => { delete draft[id]; }); — verified this is the only form that removes the key in rc.6. Keep tests L72-81 (taskCount across add/remove/clear) as the guard for this, since it is the only assertion in the suite that distinguishes delete from undefined-assignment.
  HAZ[high] src/stores/taskStore.ts:80 — getBarPosition returns geometry read from the COMMITTED store, and the constraint/cascade layer both writes and reads bar geometry inside one turn. taskLayerConstraints.ts:86-104 (resolveResizeConstraints) reads getBarPosition(taskId) to obtain x/width and feeds them to resolveConstraints — but it runs after useBarDrag.ts:196/208 has already staged the resize via updateBarPosition, so it computes  || FIX: Two options, in order of preference: (a) pass the proposed geometry explicitly — resolveResizeConstraints already knows the x/width it just wrote, so take them as parameters instead of re-reading the store (this also removes a proxy read from the 60fps path); (b) if the read must stay, hoist a singl
  HAZ[high] tests/taskStore.test.ts:98 — The gantt-6hx regression guard itself is broken by deferred writes: `store.updateBarPosition('t1', { x: 999 }); expect(x()).toBe(999); expect(runs).toBe(2);`. I ran this exact sequence against rc.6 with the draft-mutation rewrite of updateBarPosition and got x()===0 with runs===1 — the guard fails, and it fails in the direction that LOOKS like the original gantt-6hx bug (store updated, subscriber  || FIX: Insert flush() between the write and the assertions (verified to yield exactly 999 / runs 2), and strengthen the guard as described in the L100 site: add a second memo on store.tasks['t1']?.name whose run count must stay at 1, pinning that updateBarPosition no longer replaces the task object. Option
  HAZ[high] tests/taskStore.test.ts:322 — Three tests write SIGNALS inside a createRoot body (L322/L324 collapseTask/expandAllTasks, L346 setDraggingTaskId, L362 setDraggingTaskId). rc.6's setSignal write guard throws REACTIVE_WRITE_IN_OWNED_SCOPE there. This is an asymmetry that is easy to get wrong: the STORE setter guard (devGuardStoreSetterWrite, dist/dev.js:4895) explicitly exempts roots via `!context._root`, but setSignal (dist/dev. || FIX: Restructure the three tests so the root body contains only primitive creation and the writes happen outside it: `const store = createTaskStore(); let size; createRoot(d => { size = createMemo(() => store.collapsedTasks().size); dispose = d; }); store.collapseTask('a'); flush(); expect(size()).toBe(1
  STRUCT-SITE[test-flush] tests/taskStore.test.ts:100 —             expect(runs).toBe(2);
            dispose(); => Add the anti-over-notification half of the gantt-6hx guard here: const name = createMemo(() => { nameRuns++; return store.tasks['t1']?.name; }); ... after updateBarPosition + flush, expect(nameRuns).toBe(1).
  STRUCT-SITE[write-in-component-body] tests/taskStore.test.ts:322 — createRoot((dispose) => {
            const store = createTaskStore();
            const s => Move the signal writes out of the createRoot body: create the store outside, keep only createMemo inside the root, and drive writes from the enclosing test scope — or wrap each write in queueMicrotask/an unowned callback. Then flush() before each assertion.
  STRUCT-SITE[write-in-component-body] tests/taskStore.test.ts:346 — createRoot((dispose) => {
            const store = createTaskStore();
            const i => Same restructuring as L322: keep createMemo inside the root, perform setDraggingTaskId outside the root body (or via { ownedWrite: true }), then flush() before the L347 assertion.
  STRUCT-SITE[createComputed] tests/taskStore.test.ts:358 — createComputed(() => {
                observed = store.draggingTaskId();
            }); => createEffect(() => store.draggingTaskId(), (v) => { observed = v; }); then store.setDraggingTaskId('task-9') from an unowned scope, flush(), and assert. Or delete the test — it duplicates the memo test above.
  STRUCT-SITE[config] vitest.config.ts:16 —     test: {
        environment: 'node',
        globals: false,
    }, => Remove `environment: 'node'` (let @solidjs/vite-plugin default it to 'jsdom'), remove the manual resolve.conditions block, and swap `import solidPlugin from 'vite-plugin-solid'` for `import { solid } from '@solidjs/vite-plugin'`.

### lib-stores-config-date
  HAZ[high] src/stores/ganttDateStore.ts:247 — changeViewMode stages setViewModeSignal(found) on line 247 and immediately calls generateDates() on line 248. generateDates reads step() (line 204) and unit() (line 205) — memos derived from viewMode(). Under deferred writes the viewMode signal is only STAGED, so parsedStep/unit/step have not recomputed and still return the PREVIOUS view mode's duration and scale. The entire dates array is therefo || FIX: Parse the incoming mode locally and pass the derived values in: `const p = dateUtils.parse_duration(found.step); setViewModeSignal(found); generateDates(ganttStart(), ganttEnd(), p?.duration ?? 1, p?.scale ?? 'day');` with generateDates taking optional (start, end, step, unit) params. Do NOT reach f
  HAZ[high] src/stores/ganttDateStore.ts:193 — setupDates writes setGanttStart(start) / setGanttEnd(end) on lines 193-194, then calls generateDates() on line 195 which reads ganttStart() and ganttEnd() back (lines 202-203). Both reads return the previously committed boundaries, so the first setupDates call generates dates from the two `new Date()` defaults seeded at lines 70/73 — i.e. an empty or single-element dates array — and every later ca || FIX: Thread the values explicitly: `setGanttStart(start); setGanttEnd(end); generateDates(start, end);` (and `generateDates(alignedStart, end)` in the empty branch at 152-154), with generateDates accepting optional start/end parameters that default to the signal reads for standalone public use.
  HAZ[high] src/stores/ganttDateStore.ts:195 — Second-order hazard across the module boundary: utils/ganttSetup.ts:66 calls dateStore.setupDates(rawTasks) and then on lines 69-73 immediately reads dateStore.ganttStart(), ganttEnd(), unit(), step(), columnWidth() to push into ganttConfig, and again on lines 74-82 to build the `config` object handed to processTasks(). Every one of those five reads returns the pre-setupDates committed value, so t || FIX: Have setupDates RETURN the computed window (`{ start, end, unit, step, columnWidth }`) and have ganttSetup consume the returned object rather than re-reading the accessors. That removes the cross-module read-your-own-write entirely without any flush().
  HAZ[high] src/stores/ganttDateStore.ts:237 — extendTimeline stages setGanttStart(newStart) (line 231) or setGanttEnd(newEnd) (line 234) and then calls generateDates() (line 237), which reads both boundaries back. The extended boundary is not yet committed, so the regenerated dates array does not include the newly extended range — the infinite-padding scroll would append nothing and the user would hit a hard wall at the old edge. || FIX: `generateDates(direction === 'left' ? newStart : ganttStart(), direction === 'right' ? newEnd : ganttEnd(), stepVal, unitVal)`. stepVal/unitVal are already captured on lines 222-223 before any write, so they are safe to reuse.
  HAZ[high] src/stores/ganttConfigStore.ts:161 — makeSetter computes the updater's `prev` argument by reading the committed store proxy `state[key]` (line 161) and then stages the write on line 163. Two functional updates to the same key in one synchronous tick both observe the same committed prev, so the first update is discarded — `setStep(n => n + 1); setStep(n => n + 1)` yields +1, not +2. Additionally, because this is a live store proxy rea || FIX: Move the prev read into the draft, where staged state IS visible: `setState(s => { s[key] = typeof value === 'function' ? (value as any)(s[key]) : value; })`. This is both correct under deferred writes and untracked.
  HAZ[high] src/stores/ganttConfigStore.ts:175 — Not a deferred-write hazard but a total reactivity loss with the same symptom, so it belongs on the same list: toggleTaskExpansion/expandTask/collapseTask mutate the Set in place (lines 176-180, 190, 200). Verified against rc.6 isWrappable() (dist/dev.js:7341-7367): a Set stringifies as '[object Set]', fails the '[object Object]' tag test, and is therefore served RAW from the store proxy — in-plac || FIX: Change expandedTasks (line 146) to a proxied representation — `Record<string, true>` is best because it also restores per-task path tracking — or, if the Set shape must stay in the public API, copy-and-reassign on every mutation: `setState(s => { const next = new Set(s.expandedTasks); next.add(taskI
  STRUCT-SITE[store-builtin-collection] src/stores/ganttConfigStore.ts:146 — expandedTasks: new Set(options.expandedTasks || []), => Either (a) move expandedTasks out of the store into `createSignal<ReadonlySet<string>>(new Set(...))` with whole-Set replacement, or (b) store `expandedTasks: Record<string, true>` (a plain object, which IS proxied and gives per-task path tracking).
  STRUCT-SITE[custom-setter-shim] src/stores/ganttConfigStore.ts:151 — function makeSetter<K extends keyof GanttConfigState>(key: K): Setter<GanttConfigState[K]> => function makeSetter<K extends keyof GanttConfigState>(key: K) { return (value: GanttConfigState[K] | ((prev: GanttConfigState[K]) => GanttConfigState[K])): void => { setState(s => { s[key] = typeof value === 'function' ? (value as any)(s[key]) : value; }); }; 
  STRUCT-SITE[store-path-setter] src/stores/ganttConfigStore.ts:173 — setState(
            'expandedTasks',
            produce((set: Set<string>) => { => setState(s => { const next = new Set(s.expandedTasks); next.has(taskId) ? next.delete(taskId) : next.add(taskId); s.expandedTasks = next; })  — or with the Record shape: setState(s => { if (s.expandedTasks[taskId]) delete s.expandedTasks[taskId]; else s.expand
  STRUCT-SITE[store-produce] src/stores/ganttConfigStore.ts:175 — produce((set: Set<string>) => { if (set.has(taskId)) { set.delete(taskId); } else { set.ad => Body becomes the setter body (see line 173 rewrite). The Set must be COPIED and REASSIGNED, not mutated.
  STRUCT-SITE[store-produce] src/stores/ganttConfigStore.ts:189 — produce((set: Set<string>) => { set.add(taskId); }), => Inline into the draft callback; copy+reassign if the Set shape is kept.
  STRUCT-SITE[store-delete] src/stores/ganttConfigStore.ts:199 — produce((set: Set<string>) => { set.delete(taskId); }), => delete draft[key] — `delete s.expandedTasks[taskId]` (assigning undefined would KEEP the key in rc.6).
  STRUCT-SITE[write-in-component-body] src/stores/ganttDateStore.ts:152 — setGanttStart(dateUtils.start_of(start, unit()));
            setGanttEnd(end);
           => Reachable only from an event handler / timer / split-effect APPLY callback. The caller chain (Gantt.tsx:290-304 createEffect → untrack → ganttSetup.initializeTasks:58) must be restructured into createEffect(compute, apply) with setupDates called from apply.
  STRUCT-SITE[write-in-compute] src/stores/ganttDateStore.ts:201 — const generateDates = (): void => { const start = ganttStart(); const end = ganttEnd(); co => const generateDates = (startArg?: Date, endArg?: Date, stepArg?: number, unitArg?: TimeScale): void => { const start = startArg ?? ganttStart(); ... }
  STRUCT-SITE[write-in-compute] src/stores/ganttDateStore.ts:247 — setViewModeSignal(found);
                generateDates(); => const p = dateUtils.parse_duration(found.step); setViewModeSignal(found); generateDates(ganttStart(), ganttEnd(), p?.duration ?? 1, p?.scale ?? 'day');
  STRUCT-SITE[write-in-compute] src/stores/ganttDateStore.ts:251 — setViewModeSignal(mode);
            generateDates(); => Same as line 247, deriving step/unit from `mode.step` locally.

### lib-stores-resource-selection
  HAZ[high] src/utils/ganttSetup.ts:101 — WRITE resourceStore.updateResources(extracted) -> setResources(...) on line 101, then READ resourceStore.resourceIndexMap() on line 102, a memo derived from resources(). Under deferred writes the memo still holds the value derived from the PREVIOUSLY COMMITTED resources array. On the first run resources() is [] so resourceIndexMap() is an empty Map, and processTasks(rawTasks, config, emptyMap) on  || FIX: Build the map from the local `extracted` array instead of reading it back: `const extractedMap = new Map(extracted.map((r, i) => [r.id, i]));` (identical to the loop in resourceStore.ts:62-72, which cannot skip collapsed groups on a fresh extraction anyway), pass that to processTasks, and keep updat
  HAZ[high] src/utils/ganttSetup.ts:66 — WRITE dateStore.setupDates(rawTasks) on line 66 (which calls setGanttStart/setGanttEnd and generateDates internally, ganttDateStore.ts:140-156), then READ dateStore.ganttStart()/ganttEnd()/unit()/step()/columnWidth() on lines 69-73 to push into ganttConfig, and again on lines 76-80 to build the `config` object handed to processTasks. All five reads return pre-setupDates values, so the config store || FIX: Change setupDates to RETURN the bounds it computed ({ ganttStart, ganttEnd, unit, step, columnWidth }) and use the returned object for lines 69-73 and 76-80, writing to the signals purely for downstream reactive consumers. This also removes the redundant double read.
  HAZ[high] tests/ganttSetup.test.ts:30 — Every assertion in the 'resource extraction signal' describe block (lines 30, 45, 52, 66, 76, 78, 85) reads stores.resourceStore.resources() in the same synchronous tick as the initializeTasks call that wrote it. All of these read the committed value and fail (or, at lines 41/56-style spots, pass vacuously against the stale empty Set). || FIX: Insert flush() (imported from 'solid-js') after every initializeTasks/updateResources call, before the matching expect. Do not wrap the test bodies in createRoot - that would make the writes throw.
  STRUCT-SITE[write-in-component-body] src/stores/resourceStore.ts:115 — const updateResources = (newResources: ResourceInput[]): void => { setResources(normalizeR => The setter itself is fine; the CALL CHAIN must move. Gantt.tsx:294 onMount(() => runSetup(...)) and Gantt.tsx:297/303/324 createEffect(... untrack(() => runSetup(...))) reach this line from owned scopes and will throw REACTIVE_WRITE_IN_OWNED_SCOPE (untrack doe
  STRUCT-SITE[other] src/utils/ganttSetup.ts:66 — dateStore.setupDates(rawTasks); => setupDates must return the computed bounds (or accept an out-param) instead of the caller re-reading them; see syncHazards.
  STRUCT-SITE[other] src/utils/ganttSetup.ts:101 — resourceStore.updateResources(extracted); resourceIndexMap = resourceStore.resourceIndexMa => Build the map locally from `extracted` (the same loop resourceStore.ts:62-72 runs) and pass THAT to processTasks; still call updateResources for the reactive consumers. Do not read the memo back.
  STRUCT-SITE[write-in-component-body] tests/selectionStore.test.ts:66 — createRoot((dispose) => { const s = createSelectionStore(); const size = createMemo(...);  => Split the block: create the store + memo inside createRoot and hoist them out via captured variables, then do every write (and flush) OUTSIDE the root body.
  STRUCT-SITE[write-in-component-body] tests/selectionStore.test.ts:74 — s.add('a'); => Move outside the createRoot body: `s.add('a'); flush(); size(); expect(observedSize).toBe(1);`
  STRUCT-SITE[write-in-component-body] tests/selectionStore.test.ts:77 — s.toggle('b'); => Move outside the root body; add flush() before `size(); expect(observedSize).toBe(2);`
  STRUCT-SITE[write-in-component-body] tests/selectionStore.test.ts:80 — s.clear(); => Move outside the root body; add flush() before `size(); expect(observedSize).toBe(0);`
  STRUCT-SITE[write-in-component-body] tests/selectionStore.test.ts:89 — createRoot(() => { const s = createSelectionStore(); s.add('a'); const memo = createMemo(. => Create the store OUTSIDE the root (unowned createSignal is legal and verified), do `s.add('a'); flush();`, then createRoot only to own the memo, capturing dispose.
  STRUCT-SITE[write-in-component-body] tests/selectionStore.test.ts:91 — s.add('a'); => Hoist above the createRoot call and follow with flush().
  STRUCT-SITE[write-in-component-body] tests/selectionStore.test.ts:99 — s.add('a'); memo(); expect(runs).toBe(1); => Move outside the root body: `s.add('a'); flush(); memo(); expect(runs).toBe(1);`
  STRUCT-SITE[config] vitest.config.ts:12 — plugins: [solidPlugin()], resolve: { conditions: ['development','browser'] }, test: { envi => Replace vite-plugin-solid with @solidjs/vite-plugin@3.0.0-next.35 and set test.environment to 'jsdom' (the plugin's own default in test mode) so the client build with a live subscription graph resolves.

### lib-contexts-index
  HAZ[high] src/stores/ganttConfigStore.ts:164 — makeSetter writes via `setState(key, next)` on line 163 and then, in the same synchronous statement sequence, reads `return state[key]` to hand the caller the 'new' value. Under rc.6 deferred writes the store read still returns the previously committed value, so every one of the 20 exported GanttConfigStore setters (setColumnWidth, setBarHeight, setRenderMode, …) returns the OLD value while claimi || FIX: Do not re-read the store. Capture the computed value and return it: `let next; setState(draft => { next = …; draft[key] = next; }); return next;`
  HAZ[high] src/stores/ganttConfigStore.ts:161 — The updater branch of makeSetter computes `prev` by reading committed `state[key]` before the write. Two updater-form calls in the same tick (e.g. setColumnWidth(w => w+10) twice, or an updater called from a drag handler that already staged a write) both observe the same committed prev, so the first update is lost. || FIX: Move the updater invocation inside the store draft callback, where the draft reflects staged state: `setState(draft => { draft[key] = typeof value === 'function' ? value(draft[key]) : value; });`
  STRUCT-SITE[context-default] src/contexts/GanttEvents.tsx:23 — const GanttEventsContext = createContext<GanttEventHandlers>(); => const GanttEventsContext = createContext<GanttEventHandlers | null>(null);
  STRUCT-SITE[context-default] src/contexts/GanttStores.tsx:30 — const GanttStoresContext = createContext<GanttStores>(); => const GanttStoresContext = createContext<GanttStores | null>(null);
  STRUCT-SITE[store-builtin-collection] src/types.ts:352 —     expandedTasks?: Set<string>; => Either change the public option to `string[]` (normalised to a Set only in signal-held state), or keep Set<string> but never mutate it in place inside the store — always assign a fresh Set to the draft property.
  STRUCT-SITE[other] src/types.ts:268 —     getBarPosition: (id: string) => BarPosition | undefined; => Document/redefine ConstraintContext as reading a SNAPSHOT: build it from `snapshot(store)` or from the setStore draft, not from live committed store reads, whenever the caller has already written positions in the same tick.
  STRUCT-SITE[custom-setter-shim] src/stores/ganttConfigStore.ts:154 — return ((value: unknown) => { const next = typeof value === 'function' ? value(state[key]) => return ((value: unknown) => { let next: any; setStore(draft => { next = typeof value === 'function' ? (value as any)(draft[key]) : value; (draft as any)[key] = next; }); return next; }) as Setter<GanttConfigState[K]>;
  STRUCT-SITE[store-produce] src/stores/ganttConfigStore.ts:175 — setState('expandedTasks', produce((set: Set<string>) => { if (set.has(taskId)) set.delete( => setState(draft => { const next = new Set(draft.expandedTasks); if (next.has(taskId)) next.delete(taskId); else next.add(taskId); draft.expandedTasks = next; });

### lib-scroll-container
  HAZ[high] src/components/GanttContainer.tsx:172 — WRITE: GanttContainer.tsx:172 `setContainerWidth(scrollAreaRef.clientWidth)`. READ: useGanttScroll.ts:52 `setViewportWidth(api.getContainerWidth())`, where getContainerWidth is the closure defined at GanttContainer.tsx:191 as `() => containerWidth()`. Both run in the SAME synchronous onMount/onSettled call stack: line 172 writes, line 187 synchronously invokes props.onContainerReady, which reaches || FIX: Preferred: delete the mirror. Make useGanttScroll derive `viewportWidth = createMemo(() => containerApi()?.containerWidthSignal?.() ?? 0)` so the measured width arrives on the normal flush and no read-after-write exists. If the handshake must stay imperative, pass the measured numbers explicitly ins
  HAZ[high] src/hooks/useGanttScroll.ts:81 — SIDE-EFFECTFUL COMPUTE. Lines 81-89 read scrollLeft()/scrollTop() in a tracked compute, compare against mutable closure vars, then MUTATE those vars (82-83), write a signal (84), and start/cancel a timer (85-86) — all inside the compute. In 2.0 the compute is required to be pure and write-free: line 84 throws REACTIVE_WRITE_IN_OWNED_SCOPE outright, and even without the throw, a compute that rc.6 m || FIX: Split so compute is `() => [scrollLeft(), scrollTop()] as const` and everything else lives in apply, which runs unowned and may write and return a cleanup. Better still, delete lines 45-48 and 75-90 entirely — grep shows no reader of isScrolling anywhere in src/ or tests/, so this whole hazard can b
  STRUCT-SITE[other] src/hooks/useGanttScroll.ts:39 — export function useGanttScroll(): GanttScroll { => export function useGanttScroll(containerApi: Accessor<ContainerAPILike | null>): GanttScroll — take the api as a reactive input instead of receiving it through a lifecycle callback; drop `handleContainerReady` from the GanttScroll interface (lines 35-36).
  STRUCT-SITE[other] src/hooks/useGanttScroll.ts:40 — const [scrollLeft, setScrollLeft] = createSignal(0); ... const [viewportHeight, setViewpor => const scrollLeft = createMemo(() => containerApi()?.scrollLeftSignal?.() ?? 0); const scrollTop = createMemo(() => containerApi()?.scrollTopSignal?.() ?? 0); const viewportWidth = createMemo(() => containerApi()?.containerWidthSignal?.() ?? 0); const viewportH
  STRUCT-SITE[primitives-in-callback] src/hooks/useGanttScroll.ts:50 — const handleContainerReady = (api: ContainerAPILike): void => { => Delete the whole function (lines 50-98). Its six createEffect calls become top-level memos/effects in the hook body driven by the `containerApi` accessor.
  STRUCT-SITE[createEffect-split] src/hooks/useGanttScroll.ts:58 — createEffect(() => { const sl = api.scrollLeftSignal!(); const st = api.scrollTopSignal!() => If mirroring is kept at all: createEffect(() => [api.scrollLeftSignal!(), api.scrollTopSignal!()] as const, ([sl, st]) => { setScrollLeft(sl); setScrollTop(st); }); — but the memo redesign deletes this effect entirely.
  STRUCT-SITE[createEffect-split] src/hooks/useGanttScroll.ts:78 — createEffect(() => { const sl = scrollLeft(); const st = scrollTop(); if (sl !== lastScrol => createEffect(() => [scrollLeft(), scrollTop()] as const, ([sl, st], prev) => { if (prev && sl === prev[0] && st === prev[1]) return; setIsScrolling(true); if (scrollTimeout) clearTimeout(scrollTimeout); scrollTimeout = setTimeout(() => setIsScrolling(false), S
  STRUCT-SITE[other] src/hooks/useGanttScroll.ts:100 — return { scrollLeft, scrollTop, viewportWidth, viewportHeight, handleContainerReady }; => return { scrollLeft, scrollTop, viewportWidth, viewportHeight };
  STRUCT-SITE[onMount] src/components/GanttContainer.tsx:170 — onMount(() => { => onSettled(() => { let ro: ResizeObserver | undefined; if (scrollAreaRef) { const el = scrollAreaRef; setContainerWidth(el.clientWidth); setViewportHeight(el.clientHeight); ro = new ResizeObserver((entries) => { for (const entry of entries) { setContainerWidth(
  STRUCT-SITE[primitives-in-callback] src/components/GanttContainer.tsx:187 — props.onContainerReady?.({ scrollTo, getScrollLeft: () => scrollLeft(), ... scrollTimingSi => Keep the call, but the contract changes: document that onContainerReady runs inside a children-forbidden scope and MUST NOT create reactive primitives. Consumers store the api in a signal (Gantt.tsx:490 setContainerApi(api) — legal) and declare all derivations

### lib-gantt
  HAZ[high] src/utils/ganttSetup.ts:66 — WRITE: dateStore.setupDates(rawTasks) stages setGanttStart / setGanttEnd / setDates. READ (same synchronous stack, 3-18 lines later): dateStore.ganttStart(), ganttEnd(), unit(), step(), columnWidth() at L69-73 and again at L75-84. Under deferred writes all five reads return the PREVIOUS committed timeline, so (a) ganttConfig is synced to the old bounds and (b) the `config` object handed to process || FIX: Change ganttDateStore.setupDates to return the computed { ganttStart, ganttEnd, unit, step, columnWidth } (and to pass start/end into generateDates as arguments instead of re-reading signals), then build both the ganttConfig sync at L69-73 and the `config` object at L75-84 from that returned value. 
  HAZ[high] src/utils/ganttSetup.ts:101 — WRITE: resourceStore.updateResources(extracted) at L101 stages setResources(normalizeResources(extracted)). READ on the very next line: resourceStore.resourceIndexMap() at L102, a createMemo chained resources() -> displayResources() -> resourceIndexMap() (resourceStore.ts L46, L57, L62). Under deferred writes the memo still reflects the previous resource set. On the first run that set is `normaliz || FIX: Compute the index map locally instead of round-tripping through the store: `const normalized = normalizeResources(extracted); const display = computeDisplayResources(normalized, resourceStore.collapsedGroups()); resourceIndexMap = new Map(display.map((r, i) => [r.id, i]));` and still call updateReso
  HAZ[high] src/components/Gantt.tsx:496 — WRITE (in GanttContainer.tsx L172, inside its onMount): setContainerWidth(scrollAreaRef.clientWidth). READ (same synchronous stack, 14 lines later, via the onContainerReady callback it invokes at L186): this handler calls api.getContainerWidth(), which is `() => containerWidth()`. Under deferred writes it returns the initial 0, so the 'today' scroll target becomes `todayX - 0/4` = todayX and today || FIX: Do not read container measurements inside the onContainerReady callback. Gate the work on the containerApi signal instead: after `setContainerApi(api)` (L490), let a hoisted createEffect with compute `() => { const a = containerApi(); return a && { w: a.getContainerWidth(), h: a.getContainerHeight()
  HAZ[high] src/components/Gantt.tsx:321 — WRITE: dateStore.changeViewMode(viewMode) stages setViewModeSignal(found) (ganttDateStore.ts L241/L245). READ, same synchronous stack: (a) immediately inside changeViewMode, generateDates() reads step() and unit() at L202-203 — memos derived from viewMode — so the new `dates` array is generated with the OLD step/unit; (b) three lines later at L324, runSetup -> initializeTasks reads dateStore.unit( || FIX: Two-part: (1) in ganttDateStore.changeViewMode, pass the resolved mode into generateDates rather than re-reading viewMode-derived memos (generateDates(modeStep, modeUnit)); (2) in the Gantt effect's apply, insert an explicit `flush()` between dateStore.changeViewMode(viewMode) and runSetup(tasks) so
  STRUCT-SITE[write-in-compute] src/components/Gantt.tsx:266 — const runSetup = (rawTasks: GanttTask[]): void => {
    const result = initializeTasks(raw => Keep runSetup as-is, but ensure it is ONLY invoked from an effect APPLY callback, an event handler, or onSettled — never from an effect compute or the component body. All four current call sites (L288, L298, L302, L324) are illegal or need restructuring.
  STRUCT-SITE[onMount] src/components/Gantt.tsx:288 — onMount(() => runSetup(effectiveTasks())); => Delete this line entirely and let the (now split) effect at L290 perform the first run in its apply callback — createEffect's apply runs on first settle, so the initial setup still happens.
  STRUCT-SITE[createEffect-split] src/components/Gantt.tsx:290 — createEffect(() => {
    const tasks = effectiveTasks();
    const _collapsed = resourceSt => createEffect(() => { const tasks = effectiveTasks(); resourceStore.collapsedGroups(); taskStore.collapsedTasks(); return tasks; }, (tasks) => { runSetup(tasks && tasks.length > 0 ? tasks : []); });
  STRUCT-SITE[write-in-compute] src/components/Gantt.tsx:298 — untrack(() => runSetup(tasks)); => Move into the apply callback of the split effect: `(tasks) => runSetup(tasks)`. Delete the untrack.
  STRUCT-SITE[write-in-compute] src/components/Gantt.tsx:302 — untrack(() => runSetup([])); => Move into the same apply callback: `runSetup(tasks?.length ? tasks : [])`.
  STRUCT-SITE[write-in-compute] src/components/Gantt.tsx:309 — if (opts) ganttConfig.updateOptions(opts); => Move to the apply callback (see L307). ganttConfig.updateOptions is a store write (`setState(produce(...))` in ganttConfigStore.ts L214).
  STRUCT-SITE[createEffect-split] src/components/Gantt.tsx:316 — createEffect(() => {
    const viewMode = props.options?.viewMode;
    const prev = prevVi => createEffect(() => ({ viewMode: props.options?.viewMode, tasks: effectiveTasks() }), ({ viewMode, tasks }, prev) => { if (viewMode && viewMode !== prev?.viewMode) { dateStore.changeViewMode(viewMode); flush(); if (tasks?.length) runSetup(tasks); } }, { defer: 
  STRUCT-SITE[write-in-compute] src/components/Gantt.tsx:320 — setPrevViewMode(viewMode); => Delete. Use the `prev` parameter of the split effect's apply callback.
  STRUCT-SITE[write-in-compute] src/components/Gantt.tsx:321 — dateStore.changeViewMode(viewMode); => Move to the apply callback. changeViewMode writes viewMode and then dates (ganttDateStore.ts L237-249).
  STRUCT-SITE[write-in-compute] src/components/Gantt.tsx:324 — untrack(() => runSetup(tasks)); => Move to the apply callback; delete the untrack.
  STRUCT-SITE[createEffect-split] src/components/Gantt.tsx:382 — createEffect(() => {
    const layouts = rowLayouts();
    if (!layouts || layouts.size == => createEffect(() => { const layouts = rowLayouts(); const out: Array<[string, number]> = []; if (layouts) for (const [rid, l] of layouts) { if (rid === '__total__' || !l.taskPositions) continue; for (const [tid, p] of l.taskPositions) out.push([tid, p.y]); } re
  STRUCT-SITE[write-in-compute] src/components/Gantt.tsx:395 — taskStore.updateBarPosition(taskId, { y: taskPos.y }); => Replace with a single `setTasks(draft => {...})` in the effect's apply (see L382). taskStore.updateBarPosition itself must be rewritten — it uses `produce`, removed in 2.0.
  STRUCT-SITE[primitives-in-callback] src/components/Gantt.tsx:490 — const handleContainerReady = (api: ContainerAPILike): void => {
    setContainerApi(api);
 => Keep setContainerApi(api) here (legal), but stop creating effects inside the callback: change useGanttScroll to accept the containerApi accessor and declare its effects at hook-construction time, gated on `const a = containerApi(); if (!a) return;`.
  STRUCT-SITE[primitives-in-callback] src/components/Gantt.tsx:491 — scroll.handleContainerReady(api); => Replace with `const scroll = useGanttScroll(containerApi);` at L248 and delete this call; move the six effects in useGanttScroll.handleContainerReady into the hook body, each starting with `const api = containerApi(); if (!api) return;`.
  STRUCT-SITE[write-in-compute] src/utils/ganttSetup.ts:48 — export function initializeTasks(rawTasks, stores, useResourceStore = true, hasExplicitReso => No signature change required, but document/enforce that it may only be called from an effect apply, an event handler, or onSettled. Every store setter inside it throws if reached from an effect compute or a memo.
  STRUCT-SITE[write-in-compute] src/utils/ganttSetup.ts:66 — dateStore.setupDates(rawTasks); => Keep, but either (a) change setupDates to RETURN { ganttStart, ganttEnd, unit, step, columnWidth } and use that below, or (b) call `flush()` immediately after this line before reading dateStore at L69-84.
  STRUCT-SITE[write-in-compute] src/utils/ganttSetup.ts:69 — ganttConfig.setGanttStart(dateStore.ganttStart());
ganttConfig.setGanttEnd(dateStore.gantt => Feed these from the values returned by setupDates (or after an explicit flush()). Also replace ganttConfig's makeSetter-based setters with a single `ganttConfig.updateOptions({ ganttStart, ganttEnd, unit, step, columnWidth })` — one store write instead of five
  STRUCT-SITE[write-in-compute] src/utils/ganttSetup.ts:75 — const config = { ganttStart: dateStore.ganttStart(), ganttEnd: dateStore.ganttEnd(), unit: => Build `config` from the plain values returned by setupDates, not by reading the date store back.
  STRUCT-SITE[write-in-compute] src/utils/ganttSetup.ts:101 — const extracted = extractResourcesFromTasks(rawTasks);
resourceStore.updateResources(extra => Compute the map locally from `extracted` (normalizeResources -> computeDisplayResources -> index map) and pass that to processTasks, in addition to calling updateResources for the store's own consumers. Alternatively `flush()` between L101 and L102.
  STRUCT-SITE[primitives-in-callback] src/hooks/useGanttScroll.ts:50 — const handleContainerReady = (api: ContainerAPILike): void => { => Change the hook to `useGanttScroll(containerApi: Accessor<ContainerAPILike | null>)` and declare all six effects in the hook body, each opening with `const api = containerApi(); if (!api) return;`. Drop handleContainerReady from the returned API.
  STRUCT-SITE[createEffect-split] src/hooks/useGanttScroll.ts:58 — createEffect(() => {
    const sl = api.scrollLeftSignal!();
    const st = api.scrollTopS => createEffect(() => { const a = containerApi(); return a?.scrollLeftSignal && a?.scrollTopSignal ? [a.scrollLeftSignal(), a.scrollTopSignal()] as const : null; }, (v) => { if (!v) return; setScrollLeft(v[0]); setScrollTop(v[1]); });
  STRUCT-SITE[write-in-compute] src/hooks/useGanttScroll.ts:84 — setIsScrolling(true); => Move to the apply callback of the split effect at L78.
  STRUCT-SITE[custom-setter-shim] src/stores/ganttConfigStore.ts:154 — return ((value: unknown) => { const next = typeof value === 'function' ? value(state[key]) => Drop the Setter<T> emulation. Expose `setOption(key, valueOrFn)` implemented as `setState(draft => { draft[key] = typeof v === 'function' ? v(draft[key]) : v; })` and return void.

### lib-hooks
  HAZ[high] src/hooks/useDrag.ts:177 — WRITTEN: the task store, by the consumer's onDragMove invoked at L161 (SummaryBar.tsx:145 -> taskStore.batchMovePositions, which does setTasks(...)). READ: the same task store, by the consumer's onDragEnd invoked at L177 in the same synchronous stack — TaskLayer.tsx:98-102 handleResizeEnd calls buildCtx(taskId) and resolveResizeConstraints(taskId, ctx), both of which re-read task geometry out of t || FIX: Import flush from 'solid-js' and call it between the final onDragMove and onDragEnd: `if (pendingMove) { onDragMove?.(pendingMove, dragData, dragStateNow); pendingMove = null; flush(); }`. Better long-term: change the UseDragOptions contract so onDragEnd receives the resolved final geometry as part 
  HAZ[high] src/hooks/useDrag.ts:224 — WRITTEN: dragState (L218) and isDragging (L219). READ: dragState at L130 inside handleMouseMove and L155 inside handleMouseUp — handlers that are registered at L224-225 in this very same synchronous run. In a real browser the mousedown handler returns, the JS stack empties, the microtask checkpoint runs the flush, and the next mousemove arrives as a fresh task, so the guards see the committed new  || FIX: Introduce the non-reactive mirror `let dragStateNow: DragState = 'idle';` written next to each setDragState, and switch every internal guard (L79, L84, L130, L155, L161, L177, L196) to read it. The signal then exists purely for rendering. This is zero-cost and matches how the file already treats dra
  STRUCT-SITE[other] src/hooks/useDrag.ts:69 — const [dragState, setDragState] = createSignal<DragState>('idle'); => Keep the signal for rendering, but add a plain mirror: `let dragStateNow: DragState = 'idle';` written next to every setDragState and read by every internal guard.
  STRUCT-SITE[other] src/hooks/useDrag.ts:161 — onDragMove?.(pendingMove, dragData, dragState()); => `onDragMove?.(pendingMove, dragData, dragStateNow);` and — critically — `flush()` immediately after this call, before L166/L177, so onDragEnd observes the positions onDragMove just wrote.
  STRUCT-SITE[other] src/hooks/useDrag.ts:177 — onDragEnd?.(finalMove, dragData, dragState()); => `import { flush } from 'solid-js';` then `flush(); onDragEnd?.(finalMove, dragData, dragStateNow);` — or restructure so onDragEnd receives the final geometry explicitly instead of re-reading the store.
  STRUCT-SITE[other] src/hooks/useBoxSelect.ts:91 — const [overlay, setOverlay] = createSignal<BoxOverlay>(HIDDEN); => Add a plain mirror for the gesture-machine read at L135: `let overlayNow: BoxOverlay = HIDDEN;` written alongside every setOverlay.

### lib-virtualization
  HAZ[high] src/hooks/useTaskVirtualization.ts:78 — WRITE: any taskStore mutation that changes the key SET without changing the key COUNT in one tick — e.g. removeTask('a') (setTasks('a', undefined)) followed by updateTask('b', t) , or updateTasks(arr) replacing N tasks with a different N-length set. READ: `taskKeys.length === cachedTaskCount` at line 78 returns the stale `cachedGrouping`. Under 1.x each unbatched setter flushed render effects sync || FIX: Delete the count heuristic. Track the key set for real: `const tasksByResource = createMemo(() => { const t = props.taskStore?.tasks; if (!t) return new Map(); const keys = Object.keys(t); return untrack(() => buildGroups(t, keys)); });` — Object.keys on the proxy inside the tracked compute subscrib
  HAZ[high] src/components/TaskLayerMinimal.tsx:53 — Identical defect to useTaskVirtualization.ts:78, duplicated: `taskKeys.length === cachedTaskCount` (line 53) plus `cachedTaskCount = taskKeys.length` (line 57), consumed by the visibleTasks memo at line 80. Same-tick add+remove now coalesces into one flush and the cache never invalidates, so TaskLayerMinimal (the perf-demo path, Gantt.tsx:597) renders a stale task list. || FIX: Apply the same rewrite as useTaskVirtualization.ts:78, or better, factor the grouping into a single shared helper so the two copies cannot drift again.
  HAZ[high] src/utils/taskLayerConstraints.ts:87 — WRITE-THEN-READ across a call boundary. Bar.tsx:300-302 does `props.taskStore?.updateBarPosition(taskId, { width: newWidth }); onDateChange?.(...); onResizeEnd?.(taskId);` inside one keydown handler. onResizeEnd is TaskLayer.tsx:98 -> resolveResizeConstraints, which at line 87 READS `ctx.taskStore.getBarPosition(taskId)` and feeds taskBar.x/taskBar.width into resolveConstraints, then WRITES cascad || FIX: Stop re-reading state the caller just wrote. Widen the callback to carry geometry — `onResizeEnd(taskId, { x, width })` from Bar.tsx and useBarDrag.ts, threaded through TaskLayer.tsx:98 into resolveResizeConstraints(taskId, ctx, geom) — and use `geom` in place of the getBarPosition call at line 87. 
  HAZ[high] src/stores/ganttConfigStore.ts:171 — `expandedTasks: Set<string>` lives inside a createStore and is mutated IN PLACE via `setState('expandedTasks', produce(set => set.add/delete(taskId)))` (toggleTaskExpansion line 171, expandTask line 188, collapseTask line 198). rc.6 does not proxy built-in collections, so an in-place Set mutation notifies nothing at all. Consumer: useTaskVirtualization.ts:66/161 isTaskExpanded -> splitTaskIds's re || FIX: (1) Reassign instead of mutate: `setState(s => { const next = new Set(s.expandedTasks); next.has(id) ? next.delete(id) : next.add(id); s.expandedTasks = next; })` — replacing the property is what notifies for a non-proxied collection. (2) In useTaskVirtualization, hoist a real dependency out of the 
  STRUCT-SITE[other] src/components/TaskLayer.tsx:98 — const handleResizeEnd = (taskId: string): void => {
    const ctx = buildCtx(taskId);
     => Change the contract to carry geometry: `onResizeEnd?: (taskId: string, geom?: {x:number;width:number}) => void` and forward it to resolveResizeConstraints(taskId, ctx, geom) so it never re-reads the store for the just-written bar. (Fallback: `flush()` before t
  STRUCT-SITE[other] src/components/TaskLayer.tsx:88 — const handleConstrainPosition = (taskId, newX, newY) => { const ctx = buildCtx(taskId); .. => Move the read+cascade-write pair inside one store draft: `setTasks(draft => { /* read draft[succ]._bar, compute, mutate draft */ })`, so successor reads see staged state. Keep the pure return value (constrainedX) as the function result.
  STRUCT-SITE[write-in-compute] src/components/TaskLayerMinimal.tsx:53 — if (taskKeys.length === cachedTaskCount && cachedGrouping) { return cachedGrouping; }
...
 => Replace the count-keyed closure cache with a real derived memo: `const tasksByResource = createMemo(() => { const keys = Object.keys(props.taskStore?.tasks ?? {}); /* tracked */ return group(keys); })`, or key the cache on a monotonic store revision instead of
  STRUCT-SITE[write-in-compute] src/hooks/useTaskVirtualization.ts:78 — if (taskKeys.length === cachedTaskCount && cachedGrouping) {
    return cachedGrouping;
}
 => Make grouping a tracked memo and drop the closure cache: `const tasksByResource = createMemo(() => { const t = props.taskStore?.tasks; if (!t) return new Map(); const keys = Object.keys(t); /* tracked ownKeys */ return untrack(() => group(t, keys)); });` — key
  STRUCT-SITE[store-builtin-collection] src/hooks/useTaskVirtualization.ts:161 — expandedCache.set(id, isTaskExpanded(id)); => Two-part fix. (1) In ganttConfigStore make expandedTasks reassign instead of mutate: `setState(s => { const next = new Set(s.expandedTasks); next.add/delete(id); s.expandedTasks = next; })`. (2) Take an explicit dependency here — read `props.ganttConfig?.expan

### lib-bars
  HAZ[high] src/hooks/useBarDrag.ts:271 — Drop position is read from the store one tick stale. useDrag.handleMouseUp (useDrag.ts:160-163) runs the final onDragMove — which WRITES via taskStore.updateBarPosition (useBarDrag.ts:164/196-199/208) or batchMovePositions (line 149) — and then, still in the same synchronous stack, calls onDragEnd (useDrag.ts:177). onDragEnd's first act (useBarDrag.ts:271) is `deps.taskStore?.getBarPosition(t.id)` || FIX: Preferred: make onDragMove record what it wrote. Add `let lastGeom: { x: number; width: number } | null = null;` in useBarDrag, set it in each write branch (e.g. `lastGeom = { x: newX, width: originalWidth }` for dragging_bar, `{ x: newX, width: newWidth }` for dragging_left, `{ x: data.originalX as
  HAZ[high] src/hooks/useBarDrag.ts:281 — Progress drag reports the pre-drag percentage. In the same mouseup stack, the final onDragMove executes the dragging_progress branch and stages `updateTask(t.id, {...currentTask, progress: newProgress})` (line 252). onDragEnd then reads `t.progress` where `t = deps.taskInfo()` (line 261) — that accessor is Bar.tsx:207, `() => ({ id: t().id, progress: t().progress })`, which reads the `t` createMem || FIX: Hoist the computed progress out of the dragging_progress branch: store it (`data['finalProgress'] = newProgress` at line 254, or a hook-local `lastProgress`) and report `deps.handlers.onProgressChange?.(t.id, (data['finalProgress'] as number) ?? t.progress)` at line 281. Do NOT rely on flush() alone
  HAZ[high] src/components/Bar.tsx:302 — Keyboard resize computes its constraint cascade from the pre-resize width. Line 300 stages `updateBarPosition(taskId, { width: newWidth })`; line 302 then calls `onResizeEnd?.(taskId)` in the same synchronous handler. That resolves to TaskLayer.handleResizeEnd (TaskLayer.tsx:98-102) -> resolveResizeConstraints (utils/taskLayerConstraints.ts:83-105), whose first statement is `ctx.taskStore.getBarPo || FIX: Either (a) `import { flush } from 'solid-js'` and call `flush()` between line 300 and line 302, or (b) better, change resolveResizeConstraints to take the new geometry explicitly — `resolveResizeConstraints(taskId, ctx, { x: x(), width: newWidth })` — and drop its internal getBarPosition read. Optio
  HAZ[high] src/components/SummaryBar.tsx:148 — Summary-bar drop resolves constraints against pre-drag descendant positions. useDrag.handleMouseUp runs the final onDragMove, which calls `props.taskStore.batchMovePositions(batchOriginals, deltaX)` at line 142 (staging a new x for the summary task and every descendant), and then invokes onDragEnd at line 146-150 in the same stack, which calls `props.onDragEnd?.(taskId())` = TaskLayer.handleResize || FIX: Same options as the useBarDrag hazard: `flush()` in useDrag.handleMouseUp after the final onDragMove (one fix covers Bar, SummaryBar and progress), or widen the onDragEnd contract to carry the applied deltaX so the constraint resolver can add it to the originals it already has in `data['batchOrigina
  STRUCT-SITE[context-default] src/components/Bar.tsx:108 — const events = useGanttEvents(); => In contexts/GanttEvents.tsx change `createContext<GanttEventHandlers>()` to `createContext<GanttEventHandlers | null>(null)` and keep useGanttEvents' `if (!context)` no-op fallback (type it `T | null`). Bar.tsx:108 itself is unchanged.
  STRUCT-SITE[other] src/components/Bar.tsx:302 — onResizeEnd?.(taskId); => Either `flush()` between line 300 and 302, or change resolveResizeConstraints to accept the new x/width explicitly instead of re-reading getBarPosition.
  STRUCT-SITE[other] src/components/SummaryBar.tsx:148 — props.onDragEnd?.(taskId()); => flush() before invoking, or thread the final deltaX/x through drag data into the onDragEnd payload.
  STRUCT-SITE[custom-setter-shim] src/hooks/useBarDrag.ts:250 — const currentTask = deps.taskStore.getTask(t.id);\nif (currentTask) { deps.taskStore.updat => Replace the read-modify-write with a draft mutation: `deps.taskStore.setTaskProgress(t.id, newProgress)` backed by `setTasks(draft => { const tk = draft[id]; if (tk) tk.progress = p; })`.
  STRUCT-SITE[other] src/hooks/useBarDrag.ts:271 — const pos = deps.taskStore?.getBarPosition(t.id);\ndeps.handlers.onDateChange?.(t.id, { x: => Stash the last geometry written by onDragMove in a module-local (`let lastGeom: {x:number;width:number} | null`) and use it here, falling back to getBarPosition only when no move occurred. Alternatively `flush()` at the top of onDragEnd.
  STRUCT-SITE[other] src/hooks/useBarDrag.ts:281 — deps.handlers.onProgressChange?.(t.id, t.progress); => Capture the progress computed in the dragging_progress branch (line 239) into drag `data` or a hook-local, and report that value here.
  STRUCT-SITE[other] src/hooks/useDrag.ts:161 — if (pendingMove) { onDragMove?.(pendingMove, dragData, dragState()); pendingMove = null; } => Insert `flush()` (imported from 'solid-js') immediately after this block, before the onDragEnd call at line 177 — the minimal, mechanically-safe fix for the whole family of drag-end hazards.
  STRUCT-SITE[context-default] src/contexts/GanttEvents.tsx:23 — const GanttEventsContext = createContext<GanttEventHandlers>(); => const GanttEventsContext = createContext<GanttEventHandlers | null>(null);

### lib-arrows
  HAZ[high] src/components/ArrowLayer.tsx:103 — positionMap's untrack(...) block suppresses every dependency on task data (verified: the store proxy's ownKeys trap DOES track — dev.js:9271 — so `for (const taskId in tasks)` would subscribe to the key set if it were not wrapped). The memo's only live dependencies are `props.taskStore` identity and `draggingTaskId()`. WRITE: any code path that stages tasks / _bar positions into the taskStore. REA || FIX: Make the dependency real. Minimum: hoist the key-set read out of untrack — `const tasks = props.taskStore?.tasks; const ids = tasks ? Object.keys(tasks) : [];` (tracked via ownKeys) and iterate `ids` inside the untrack for the _bar reads. Better: delete the untrack entirely and let 2.0's flush coale
  HAZ[high] src/components/ArrowLayerBatched.tsx:123 — `void props.positionVersion;` is the sole tracked dependency that reflects bar movement, because both position reads (lines 139-140) are wrapped in untrack. WRITE: producers call taskStore.updateBarPosition / batchMovePositions and then separately bump the counter (GanttPerfIsolate.tsx:1529 `triggerArrowUpdate`, invoked at 1587 inside a `batch()` that must be deleted in 2.0). READ: this memo re-ru || FIX: Delete the counter protocol: remove the untrack at lines 139-140 so getBarPosition subscribes normally, and drop line 123 plus the positionVersion prop (line 40). If the manual protocol must stay for perf, (1) change line 123 to a used read included in the memo's return value so it cannot be elided,
  HAZ[high] src/stores/taskStore.ts:108 — Followed one level from both ArrowLayer and ArrowLayerBatched, which read positions through this store. `updateBarPosition` guards its write with a read of COMMITTED state: `if (!tasks[id]) return;` at line 108, before `setTasks(produce(...))` at line 110. Under 2.0 deferred writes, a setup or drag sequence that adds a task and sets its bar position in the same synchronous run has the task write s || FIX: Move the existence check inside the draft, where staged state is visible: `setTasks(s => { const t = s[id]; if (!t?._bar) return; t._bar = { ...t._bar, ...position }; })`. This also removes the `produce` import (produce is deleted in 2.0 — its body IS the setter body now) and the path-setter forms a
  STRUCT-SITE[createMemo-args] src/components/ArrowLayer.tsx:91 — const positionMap = createMemo<Map<string, BarPosition>>(() => { => Single-arg form is fine. The body must be restructured (see line 103) so it actually depends on task positions.
  STRUCT-SITE[other] src/components/ArrowLayer.tsx:103 — untrack(() => {
    for (const taskId in tasks) {
        const task = tasks[taskId];
     => Drop the untrack and let the store subscribe, or make the dependency explicit: `const ids = Object.keys(tasks); void props.taskStore?.taskCount?.();` outside the untrack, then read _bar values inside it. Minimal fix: read the key set outside untrack (the ownKe
  STRUCT-SITE[other] src/components/ArrowLayerBatched.tsx:70 — let arrowPathCache = new Map<number, ArrowPaths>();
let cachedResult: StyleGroup[] = [];
l => Move all three into the component body (or into the value returned by spatialIndex) so they are per-instance. Two mounted ArrowLayerBatched components currently clobber each other's caches, and Vite HMR preserves the stale module state across reloads.
  STRUCT-SITE[createMemo-args] src/components/ArrowLayerBatched.tsx:105 — const spatialIndex = createMemo(() => { => Single-arg form is valid in 2.0. The body needs restructuring for the module-cache side effects (lines 125-128) and the untracked position reads (lines 139-140).
  STRUCT-SITE[other] src/components/ArrowLayerBatched.tsx:125 — arrowPathCache.clear();
lastVisibleSet = new Set();
cachedResult = []; => Return the invalidated caches as part of spatialIndex's value: `return { index, positions, pathCache: new Map(), epoch }` and have batchedPaths derive its own cache from that value, instead of two memos communicating through module-level `let`s.
  STRUCT-SITE[createMemo-args] src/components/ArrowLayerBatched.tsx:168 — const batchedPaths = createMemo((): StyleGroup[] => { => Single-arg form is valid. Body writes module state at lines 212, 232 and 260 — same fix as line 125.

### lib-chrome
  STRUCT-SITE[top-level-read] src/components/ColumnPanel.tsx:133 — const tasks = props.taskStore?.tasks; ... for (const id of Object.keys(tasks)) { const t = => Keep the helper, but only ever call it from inside a tracking scope — see the fix at line 202. Body needs no edit.
  STRUCT-SITE[top-level-read] src/components/ColumnPanel.tsx:202 — {(item) => { const task = firstTaskForResource(item.id); return ( <div class="column-panel => {(item) => { const task = createMemo(() => firstTaskForResource(item.id)); return (<div class="column-panel-row" data-resource={item.id} style={rowStyle(item)}> ... </div>); }}
  STRUCT-SITE[top-level-read] src/components/ColumnPanel.tsx:211 — const value = col.render ? col.render(task, item.id) : ((task && (task as unknown as Recor => Delete the `const value = ...` statement and inline it into JSX: <div class="column-panel-cell" data-col-key={col.key} style={cellStyle(col)}>{col.render ? col.render(task(), item.id) : ((task() as unknown as Record<string, unknown> | undefined)?.[col.key] ?? 

### demo-components
  HAZ[high] src/demo/ConstraintDemo.tsx:778 — onChange writes selectedScenario then, in the same synchronous handler, calls updateTasks() which reads allTasks() — a memo whose only dependency is selectedScenario() (line 628). Under deferred writes the signal write is staged, the memo has not recomputed, and allTasks() returns the PREVIOUS scenario's task list. taskStore.updateTasks then reconciles the store to the OLD tasks while <For each={a || FIX: Delete the imperative pairing. Replace the onMount at 662 and the updateTasks helper at 667 with a split effect: createEffect(() => allTasks(), (tasks) => taskStore.updateTasks(tasks)) — compute reads the memo (tracked, write-free), apply does the store write (untracked, writes allowed). Then onChan
  HAZ[high] src/demo/ConstraintDemo.tsx:251 — In the successor branch of resolveMovement, line 239 writes the predecessor's position via taskStore.updateBarPosition(otherTaskId, ...), and then line 251 (the maxDistance clamp) reads predTask._bar.x and predTask._bar.width off the same store proxy to compute newX. Under deferred writes that read returns the pre-write x, so the successor is clamped against where the predecessor USED to be. Line  || FIX: Make resolveMovement pure. Build a local Map<taskId, {x,y}> seeded lazily from taskStore.getBarPosition, have every read go through a getPos(id) helper that checks the map first, have every 'write' set the map entry, and return the accumulated updates. handleMouseMove then applies the whole plan in 
  STRUCT-SITE[config] src/demo/ArrowDemo.tsx:1 — // @ts-nocheck => remove // @ts-nocheck once migrated
  STRUCT-SITE[write-in-component-body] src/demo/ConstraintDemo.tsx:178 — taskStore.updateBarPosition(otherTaskId, { x: result.x, y: result.y }); => collect into a pending-updates array (or mutate a local working map of positions) and apply once after the traversal completes
  STRUCT-SITE[write-in-component-body] src/demo/ConstraintDemo.tsx:203 — taskStore.updateBarPosition(otherTaskId, { x: result.x, y: result.y }); => accumulate; apply after traversal
  STRUCT-SITE[write-in-component-body] src/demo/ConstraintDemo.tsx:239 — taskStore.updateBarPosition(otherTaskId, { x: result.x, y: result.y }); => accumulate; apply after traversal
  STRUCT-SITE[write-in-component-body] src/demo/ConstraintDemo.tsx:264 — taskStore.updateBarPosition(otherTaskId, { x: result.x, y: result.y }); => accumulate; apply after traversal
  STRUCT-SITE[onMount] src/demo/ConstraintDemo.tsx:662 — onMount(() => { taskStore.updateTasks(allTasks()); }); => Preferred: delete the onMount and the updateTasks helper and use a split effect - createEffect(() => allTasks(), (tasks) => taskStore.updateTasks(tasks)). Minimal: onSettled(() => { taskStore.updateTasks(allTasks()); }).
  STRUCT-SITE[write-in-component-body] src/demo/ConstraintDemo.tsx:667 — const updateTasks = () => { taskStore.updateTasks(allTasks()); }; => delete in favour of the split createEffect at 662; or make it take the scenario key explicitly: const updateTasks = (key) => taskStore.updateTasks(buildTasks(key))
  STRUCT-SITE[other] src/demo/ConstraintDemo.tsx:777 — onChange={(e) => { setSelectedScenario(e.target.value); updateTasks(); }} => onChange={(e) => setSelectedScenario(e.target.value)} and let the split createEffect at 662 re-seed the store; or, if the imperative shape must stay, flush() between the write and the read
  STRUCT-SITE[custom-setter-shim] src/stores/ganttConfigStore.ts:151 — return ((value) => { const next = typeof value === 'function' ? value(state[key]) : value; => return ((value) => setState(s => { s[key] = typeof value === 'function' ? value(s[key]) : value; })) as (v) => void  — and drop the Setter<T> typing / the return value from the public interface

### demo-showcase
  HAZ[high] src/demo/ShowcaseDemo.tsx:1085 — HAZARD #1 (name): `setTaskConfig('name', e.target.value)` at 1084 is immediately followed by `updateTaskA()` at 1085, and updateTaskA (665-678) reads `taskConfig.name` at line 670 to build the object it writes into taskStore. Under deferred writes the store read at 670 still returns the PREVIOUS committed name, so every keystroke writes the stale name into taskStore and <Bar task={{name: taskConfi || FIX: Stop round-tripping through the store: give updateTaskA a patch parameter - `const updateTaskA = (patch = {}) => { const prev = snapshot(taskStore.getTask('task-a')); taskStore.updateTask('task-a', { ...prev, name: taskConfig.name, ..., ...patch }); }` and call `updateTaskA({ name: e.target.value })
  HAZ[high] src/demo/ShowcaseDemo.tsx:1098 — HAZARD #2 (color): `setTaskConfig('color', ...)` at 1097 then `updateTaskA()` at 1098, which reads `taskConfig.color` at 672. Stale by one edit - the color swatch (JSX, reactive) updates but the stored task keeps the previous color. || FIX: Same as hazard #1: pass the new value explicitly (`updateTaskA({ color: e.target.value })`) or derive taskStore's task-a from taskConfig with a split createEffect.
  HAZ[high] src/demo/ShowcaseDemo.tsx:1119 — HAZARD #3 (colorProgress): `setTaskConfig('colorProgress', ...)` at 1115-1118 then `updateTaskA()` at 1119, which reads `taskConfig.colorProgress` at 673. Stale by one edit. || FIX: Same as hazard #1: `updateTaskA({ colorProgress: e.target.value })`.
  HAZ[high] src/demo/ShowcaseDemo.tsx:1142 — HAZARD #4 (progress): `setTaskConfig('progress', parseInt(...))` at 1138-1141 then `updateTaskA()` at 1142, which reads `taskConfig.progress` at 671. On a range input this fires continuously, so the stored progress trails the slider by exactly one input event - and when dragging stops, the last value is never committed to taskStore. || FIX: Same as hazard #1: `updateTaskA({ progress: parseInt(e.target.value) })`, or derive via split createEffect so the trailing value is always applied.
  HAZ[high] src/demo/ShowcaseDemo.tsx:1192 — HAZARD #5 (locked): `setTaskConfig('locked', e.target.checked)` at 1188-1191 then `updateTaskA()` at 1192, which reads `taskConfig.locked` at 674 into `constraints: { locked }`. The checkbox flips but taskStore keeps the OPPOSITE lock state, so drag/constraint behaviour (resolveConstraints reads task.constraints.locked via context.getTask) is inverted relative to the UI until the next toggle. || FIX: Same as hazard #1: `updateTaskA({ constraints: { locked: e.target.checked } })`. This one is a behaviour inversion, not just a display lag - fix it before shipping.
  HAZ[high] src/demo/ShowcaseDemo.tsx:1217 — HAZARD #6 (invalid): `setTaskConfig('invalid', e.target.checked)` at 1213-1216 then `updateTaskA()` at 1217, which reads `taskConfig.invalid` at 675. Stale by one toggle. || FIX: Same as hazard #1: `updateTaskA({ invalid: e.target.checked })`.
  HAZ[high] src/demo/ShowcaseDemo.tsx:1776 — HAZARD #7 (Task B locked): `setTaskBConfig('locked', e.target.checked)` at 1775 then `updateTaskB()` at 1776, which reads `taskBConfig.locked` at 690. Same inverted-lock-state failure as hazard #5, for task-b. || FIX: Give updateTaskB a patch parameter and call `updateTaskB({ constraints: { locked: e.target.checked } })`, or derive task-b from taskBConfig with a split createEffect.
  HAZ[high] src/demo/ShowcaseDemo.tsx:829 — HAZARD #8 (applyPreset clobbers the preset positions): lines 807-826 call taskStore.updateBarPosition for task-a..task-d (each stages a draft write that rewrites state[id]._bar). Line 829 then reads `taskStore.getTask('task-a')`, which under deferred writes returns the task with the OLD _bar, and line 831 writes it back with `setTasks('task-a', {...taskA, ...})` - a whole-node replacement that ove || FIX: Do the field update and the position update in ONE draft write, e.g. `setTasks(d => { const t = d['task-a']; Object.assign(t, {name: preset.taskConfig.name, progress: ..., color: ..., colorProgress: ..., invalid: ...}); t.constraints.locked = preset.taskConfig.locked; Object.assign(t._bar, {...posit
  HAZ[high] src/demo/ShowcaseDemo.tsx:843 — HAZARD #9 (same clobber for tasks B/C/D): `taskStore.getTask('task-b')` at 843 (and 'task-c' at 854, 'task-d' at 865) read committed state after the staged updateBarPosition writes at 812/817/822, then lines 845/856/867 write the stale `{...taskX}` back as a whole-node replacement, discarding the new _bar for all three tasks. || FIX: Same fix as hazard #8, applied to task-b/task-c/task-d: fold the position reset and the field reset into a single draft write per task (or one draft write for all four).
  HAZ[high] src/demo/ShowcaseDemo.tsx:910 — HAZARD #10 (resize-end constraint cascade): handleResizeEnd reads `taskStore.getBarPosition(taskId)` at 910 and feeds taskBar.x/taskBar.width into resolveConstraints at 919-924. Its caller Bar.tsx:300 writes the new width (`props.taskStore?.updateBarPosition(taskId, { width: newWidth })`) and then calls onResizeEnd on line 302 in the SAME synchronous handler (keyboard shift+Arrow resize). Under de || FIX: Change the onResizeEnd contract to pass the new geometry: `onResizeEnd(taskId, { x, width })` (Bar.tsx already has newWidth in scope) and have handleResizeEnd use the passed values instead of re-reading the store. If the contract must stay, call flush() at the top of handleResizeEnd - it runs from a

### demo-perf-isolate
  HAZ[high] src/demo/GanttPerfIsolate.tsx:1648 — handleConstrainResize writes the dragged task at L1645 (updateBarPosition -> setTasks draft, setting startHours AND durationHours) and then, in the same synchronous call, reads the store back through calculateCascadeUpdates(taskId, finalX, context) at L1648. That helper (src/utils/constraintEngine.ts:554) calls context.getBarPosition = mockTaskStore.getBarPosition (L1537), which reads tasks[id].st || FIX: Move the cascade computation above the write and feed the new geometry explicitly: const cascadeCtx = { ...context, getBarPosition: (id) => id === taskId ? { ...mockTaskStore.getBarPosition(id), x: finalX, width: finalWidth } : mockTaskStore.getBarPosition(id) }; const cascadeUpdates = calculateCasc
  STRUCT-SITE[onMount] src/demo/GanttPerfIsolate.tsx:1758 —     onMount(() => { => onSettled(() => { const updateSize = () => {...}; updateSize(); window.addEventListener('resize', updateSize); let rafId = null; if (autoTest && scrollerRef) { const tick = () => { ...; rafId = requestAnimationFrame(tick); }; rafId = requestAnimationFrame(tick
  STRUCT-SITE[context-provider] src/demo/GanttPerfIsolate.tsx:2086 —     return useContext ? (
        <GanttEventsProvider>{content}</GanttEventsProvider> => Keep <GanttEventsProvider> (it is a component), but fix its body in src/contexts/GanttEvents.tsx; simplest demo-level fix is to mount it unconditionally so no variant can hit a missing context.

### demo-perf-profiler
  HAZ[high] src/demo/GanttPerfDemo.tsx:336 — startFpsCounter() is @solid-primitives/raf's start(), whose body is `if (running()) return; setRunning(true); requestAnimationFrame(loop)`. It is invoked from inside onMount -> onSettled, which in rc.6 is a TRACKED effect. Two failures compound: (1) the running() read subscribes the onSettled bundle to createRAF's internal signal and the setRunning(true) in the same body dirties it, so after the m || FIX: onSettled(() => { untrack(() => { loadTasks(); startFpsCounter(); }); }); - untrack removes the subscription while writes stay legal in an onSettled body. Belt and braces: guard with a plain `let fpsStarted = false` closure latch so re-entry does not depend on committed signal state.
  STRUCT-SITE[onMount] src/demo/GanttPerfDemo.tsx:334 — onMount(() => { loadTasks(); startFpsCounter(); }); => onSettled(() => { untrack(() => { loadTasks(); startFpsCounter(); }); });
  STRUCT-SITE[other] benchmarks/profiler/index.js:71 — const { createFrameMetrics } = require('./metrics/frameMetrics.js'); => delete the three require() blocks (71-82) and use the module's own static ESM imports/re-exports
  STRUCT-SITE[other] benchmarks/profiler/index.js:77 — const { startMemoTracking, stopMemoTracking, analyzeMemos, clearMemoTracking } = require(' => replace with a top-level `import { ... } from './instrumentation/memoTracker.js'`
  STRUCT-SITE[other] benchmarks/profiler/index.js:82 — const { createBenchmarkResult, downloadJSON, generateReport } = require('./export/jsonExpo => replace with a top-level `import { ... } from './export/jsonExporter.js'`
  STRUCT-SITE[other] benchmarks/profiler/index.js:241 — createFrameMetrics: () => require('./metrics/frameMetrics.js').createFrameMetrics(), => createFrameMetrics: () => createFrameMetrics(), using the static import

### demo-db
  HAZ[high] src/demo/DbDemo.tsx:196 — onDateChange runs synchronously inside the mouseup call stack (useDrag.ts:159-177 drains the pending move, writing every dragged/batch-moved bar's _bar via taskStore.updateBarPosition/batchMovePositions, then calls onDragEnd -> useBarDrag.ts:263-275 -> Gantt.handleDateChange:467-476 -> this handler). It then READS taskStore.tasks[id]._bar.x/.width for every task to compute dxPx/dwPx against lastX/ || FIX: Call flush() (exported from solid-js) as the first statement of onDateChange — the handler is unowned, so flush is allowed — before the `for (const id of Object.keys(taskStore.tasks))` loop. Structurally better: change the Gantt/useBarDrag contract so onDragEnd captures the final {x, width} it just 
  HAZ[high] src/demo/DbDemo.tsx:240 — After `await Promise.all(patches.map(api.patchTask))` the microtask flush has run, so `taskStore.tasks[p.id]._bar` here returns the COMMITTED final position, while the delta that produced the persisted start/end was computed at lines 196-212 from the pre-flush (stale) position. lastX/lastWidth are therefore re-based to a pixel position that does not correspond to dbState's dates, so the next drag' || FIX: Record the exact values used for the delta: push `{ id, start, end, x: bar.x, width: bar.width }` in the loop at line 219 and, after the PATCH, do `lastX.set(p.id, p.x); lastWidth.set(p.id, p.width);` instead of re-reading the store. (Combine with the flush() fix so both reads agree.)

### demo-features
  HAZ[high] src/demo/BoxSelectDemo.tsx:141 — WRITE: setSelectedIds(...) executed from Gantt.tsx:184-188's effect COMPUTE (wrapped in untrack, which does not clear the owner). READ: selectedIds() in JSX at 131-132. In rc.6 dev the write throws REACTIVE_WRITE_IN_OWNED_SCOPE before any read happens; in prod the write is staged and the badge updates only after the microtask flush, so any same-tick readback of selectedIds() is stale. || FIX: Split the Gantt effect: createEffect(() => Array.from(selectionStore.selectedIds()), (ids) => props.onSelectionChange?.(new Set(ids))). Compute must return a plain array (not the Set behind a store proxy) so apply never triggers STRICT_READ_UNTRACKED; drop untrack. Demo file needs no change.
  HAZ[high] src/demo/MultiSelectDemo.tsx:123 — Identical to BoxSelectDemo:141 — parent setter invoked from Gantt's selection effect compute; write throws in dev, and the `Selected (n)` readback at 113-114 lags one flush behind the click once fixed. || FIX: Same split-effect fix in Gantt.tsx:184-188; no change in the demo.
  STRUCT-SITE[createEffect-split] src/demo/BoxSelectDemo.tsx:141 — onSelectionChange={(set) => setSelectedIds(Array.from(set).sort())} => Demo text can stay, but it only works once Gantt.tsx:184-188 becomes a split effect: createEffect(() => [...selectionStore.selectedIds()], (ids) => props.onSelectionChange?.(new Set(ids))) — callback then runs in the untracked apply phase where writes are lega
  STRUCT-SITE[createEffect-split] src/demo/MultiSelectDemo.tsx:123 — onSelectionChange={(set) => setSelectedIds(Array.from(set).sort())} => Same as BoxSelectDemo: requires Gantt.tsx:184-188 split into compute (returns a plain array snapshot) + apply (invokes props.onSelectionChange). No textual change needed in this file.

### entries
  STRUCT-SITE[config] package.json:118 — "solid-js": "^1.9.12"  (deps); "vite-plugin-solid": "^2.11.12", "babel-preset-solid": "^1. => deps: solid-js 2.0.0-rc.6 + @solidjs/web 2.0.0-rc.6; devDeps: @solidjs/vite-plugin 3.0.0-next.35, drop vite-plugin-solid and babel-preset-solid; @solid-primitives/raf -> 4.0.0-next.2, @solid-primitives/utils -> 7.0.0-next.4 and the 'next' majors of memo/resize

### tooling-docs
  HAZ[high] vitest.config.ts:15 — test.environment:'node' puts @solidjs/vite-plugin into its SERVER posture: the plugin skips injecting the 'browser' condition and force-inlines solid-js/@solidjs/web (test.server.deps.inline=[/solid-js/,/@solidjs[+/]web/]) so the SERVER build resolves - a build with no subscription graph. Every reactive assertion in tests/taskStore.test.ts (createRoot + createMemo over store.tasks[id]._bar.x, line || FIX: Drop `environment: 'node'` and `resolve.conditions` entirely, add a jsdom devDependency, and let the plugin apply its client posture (jsdom + solid/development/browser conditions). Then add `flush()` after every write before an assertion. If a node environment is genuinely required, you must keep an
  HAZ[high] docs/PERFORMANCE.md:407 — Documented house pattern 'Gantt Y-sync effect: wrap position updates in untrack() to avoid feedback loop'. In 2.0 untrack() disables tracking but does NOT clear the owner, so a store write executed inside a createEffect compute callback throws REACTIVE_WRITE_IN_OWNED_SCOPE in dev even when wrapped. Any contributor following this doc writes code that throws on first render; worse, the pattern's int || FIX: Rewrite the guidance (and src/components/Gantt.tsx) as a split effect: createEffect(compute, apply) where compute reads plain values (numbers, not store proxies) and returns them, and apply performs the setStore(draft => ...) write. The apply callback runs from the scheduler with no owner, so it may
  HAZ[high] docs/DATABASE.md:138 — The documented drag-persistence algorithm (steps 1-5, lines 136-145; implemented in src/demo/DbDemo.tsx) writes bar positions through the chart's store during drag and then, on the onDateChange callback, WALKS THE SAME STORE reading `_bar.x`/`_bar.width` to diff them against a snapshot. If onDateChange is emitted in the same synchronous tick as the store write (drag-end handler, or a constraint ca || FIX: Await the flush before diffing: make the onDateChange handler `async (id, dates) => { await flush(); ...walk store... }` (or queueMicrotask), or - better - have the chart pass the new geometry to the callback explicitly so the demo never re-reads the store it just wrote. Update both this doc and CLA
  STRUCT-SITE[write-in-compute] docs/PERFORMANCE.md:407 — 3. **Gantt Y-sync effect**: Wrap position updates in `untrack()` to avoid feedback loop => Replace with the 2.0 pattern: 'Gantt Y-sync effect: split it - createEffect(() => <plain values read here>, (vals) => { setStore(draft => { ...write here... }) }). The apply callback runs from the scheduler with no owner, so it may write; untrack() no longer m