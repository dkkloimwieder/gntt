
====================================================================================================
PLAN incremental-safety: Prepare-on-1.9, then one atomic swap, then small legal-scope fixes (incremental safety)  | review score=8 | est effort=Roughly 42 issues: 8 S in E0/E7 (~1 day), E1 ten issues (~5 days, two L), E2-1 one XL (~2 days including the checklist and shim), E3 four issues (~2 days), E4 six issues (~3 days), E5 two issues (~2 days), E6 five issues (~4 days including the smoke matrix and re-baseline), E7 five issues (~2 days). Total ≈ 4 engineer-weeks on the critical path, with E4/E5/E7 parallelizable once E3-1 renders.
BRANCH: Create a long-lived feature branch `solid-2` from main (main stays releasable on 1.9.12 throughout). Every issue lands as exactly one commit on `solid-2` and is pushed immediately (`git push origin solid-2`), matching the gantt-6jw convention. E0 and E1 commits are runtime-neutral refactors on 1.9.12 and could be cherry-picked to main if the migration stalls (they are bug-fix-quality on their own). E2-1 is the only commit that cannot be split. After E6-5 (dist-demo refresh) merge `solid-2` into main with a merge commit (not squash, so per-issue reverts stay possible) and only then consider a `
VERSIONS: During the RC line pin EXACT versions (no caret): solid-js 2.0.0-rc.6, @solidjs/web 2.0.0-rc.6, @solidjs/vite-plugin 3.0.0-next.38, @solid-primitives/raf 4.0.0-next.2 (moved to devDependencies), jsdom ^25 (caret is fine, not part of the RC line), optionally @solidjs/testing-library 1.0.0-beta.3 and @solidjs/diagnostics 2.0.0-rc.6 (exact). Rationale: rc-to-rc changes have altered write-guard semantics and plugin posture already; an accidental minor bump inside a caret range would invalidate the a
PRINCIPLES: Green after every commit: pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm build:demo must pass on every commit of the migration branch; runtime s | Do the read-your-own-write redesigns BEFORE the swap, on solid-js 1.9.12, as pure refactors: producers return their computed values, consumers never re-read sta | Exactly one commit is allowed to be non-incremental: the dependency/config/import swap (E2-1). Everything that can be moved out of it (dead code, geometry threa | Smallest correct change first, idiomatic end state second: createContext(null) before any context redesign; flush() at the mouseup gesture-commit boundary (lega | flush() legality is scope-dependent and is written into every issue that uses it: LEGAL in event handlers, timers, promise continuations, test bodies; SILENT NO | Signals vs stores differ under createRoot: store setters are legal inside createRoot bodies, plain signal setters throw. Tests for signal-only stores keep write | Every callback handed to onSettled/createEffect apply or crossing a component boundary gets a block body (Setter<T> returns the new value and rc.6 throws on non | Built-ins inside a store are replace-only; prefer a signal holding an immutable collection (taskStore.collapsedTasks, resourceStore.collapsedGroups, selectionSt | One issue -> one commit -> one push, each independently revertable; dist-demo/ refresh is its own final chore commit; no scope creep without an explicit decisio

## E0 [P1] Pre-flight on 1.9.12: decisions, tooling globs, dead code, callback bodies, shared demo helper
   goal: Make every later commit smaller and verifiable: record the decisions the migration depends on, make config files visible to the quality gates, delete code the audit proved dead, and normalize the patterns (callback block bodies, five copy-pasted lifecycle bodies, seven self-reading latches) that wou
   gate: pnpm typecheck && pnpm lint && pnpm prettier-check && pnpm test && pnpm build && pnpm build:demo; pnpm dev smoke: examples/gantt.html, examples/perf.html, examples/showcase.html render and drag works (unchanged behavior).
   - E0-1 [decision/P0/S] Decision record: pins, branch, UMD, peerDeps, dead code, laziness, ts-nocheck scope, vitest projects  deps=[]  files=5
       Record, in the issue body, the answers to the open decisions listed in this plan (exact pins during RC; feature branch solid-2; drop the umd format vs ship a self-contained bundle; peerDependencies deferred to E7-5; keep taskStore collapse API, delete GanttSubtaskDemo + isScrolling + dead ContainerAPI surface; memos stay eager b
   - E0-2 [chore/P1/S] Make config files visible to the gates: tsconfig include, lint/prettier globs, CI node 22 + typecheck + test  deps=[]  files=4
       tsconfig.json:22 include omits vitest.config.ts and config/vite/*.js; package.json:30/32/33 lint/prettier/prettier-check globs omit config/vite/**, vitest.config.ts and eslint.config.mjs; publish.yml pins node 18 and pnpm 9 while @solidjs/vite-plugin requires node ^20.19||>=22.12 and packageManager is pnpm@10.23.0. Add vitest.co
   - E0-3 [chore/P1/S] Delete dead code: isScrolling machinery, dead ContainerAPI surface, duplicate onMount initializer, dead imports  deps=[]  files=6
       Grep-verified dead code (digest lib-scroll-container, lib-gantt, demo critic): useGanttScroll.ts:45-48 and :75-90 (isScrolling signal + timer; no reader in src/ or tests/); GanttContainer.tsx getSvgElement (:52, :193), scrollTimingSignal (:57, :198), resetWorstTiming (:58, :199-201), ScrollTiming interface (:39-44), the never-wr
   - E0-4 [chore/P3/S] Delete GanttSubtaskDemo (no entry file) and normalize examples/*.html .jsx -> .tsx entry paths  deps=['E0-1']  files=5
       GanttSubtaskDemo.tsx is mounted by no entry (digest demo-main). Delete it. Eleven examples pages load `/src/entries/<name>.jsx` for files that are .tsx (bar, arrow, perf, perf-isolate, experiments, subtask, showcase, resource-groups, constraint, gantt, profiler); Vite resolves it today and @solidjs/vite-plugin will too, but norm
   - E0-5 [chore/P1/S] Block-body every setter-returning or async callback handed across a component boundary  deps=[]  files=5
       rc.6 validates the return value of onSettled bodies and createEffect apply callbacks and throws on any non-undefined, non-function return, while Setter<T> still returns the new value (digest demo critic, cross-cutting). Convert the known concise arrows to block bodies now, on 1.9, where it is a no-op: ExportDemo.tsx:197 onReady 
   - E0-6 [chore/P2/M] One shared demo viewport/FPS lifecycle helper; plain-boolean latches for the seven self-reading run guards  deps=['E0-5']  files=6
       Five perf demos copy-paste the same onMount + ResizeObserver + RAF/interval + onCleanup body (GanttMinimalTest.tsx:417/439, GanttExperiments.tsx:964/978, GanttPerfIsolate.tsx:1758/1769, GanttPerfDemo.tsx:269/276/298, GanttProfiler.tsx:165/177) and three of them leak an uncancelled RAF (GanttMinimalTest:468, GanttPerfIsolate:1806

## E1 [P0] Read-your-own-write redesigns as pure refactors on 1.9.12
   goal: Rewrite every write-then-read-back seam the tracer found (chains A, B, C, D, F, G, I, J1, J2, J3) so producers return values and consumers never re-read what they just wrote, while the synchronous 1.9 runtime still guarantees identical behavior and today's tests and demos verify it.
   gate: pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm build:demo; pnpm dev smoke on examples/gantt.html (first paint has bars, view-mode switch regenerates columns), showcase.html (drag/resize/progress/presets), constraint.html (cascade), box-select.html, db.html with pnpm dev:server (drag 
   - E1-1 [bug/P0/M] taskStore: delete-in-draft removeTask, guard-inside-draft + leaf-mutation updateBarPosition/batchMovePositions, patchTask, explicit-id collapseAllTasks, strengthened gantt-6hx guard  deps=[]  files=3
       taskStore.ts:134 `setTasks(id, undefined)` relies on the 1.x delete idiom that keeps the key in 2.0 -> `setTasks(produce(s => { delete s[id]; }))` now (literal draft body after the swap). taskStore.ts:108 `if (!tasks[id]) return;` reads committed state outside the draft -> delete it; the produce body at :112-113 already checks t
   - E1-2 [bug/P0/M] ganttConfigStore: expandedTasks -> signal-held immutable Set, makeSetter reads prev inside the update, retype setters to void  deps=[]  files=4
       ganttConfigStore.ts:146 holds a Set inside createStore and :172-203 mutate it in place via produce; rc.6 does not proxy built-in collections so expand/collapse would notify nothing (Gantt.tsx:371 rowLayouts and useTaskVirtualization.ts:161 go dead). Per decision E0-1, move it OUT of the store: `const [expandedTasks, setExpandedT
   - E1-3 [bug/P0/L] Setup pipeline: generateDates(start,end,step,unit), setupDates/changeViewMode return the window, ganttSetup computes config and resourceIndexMap locally  deps=['E1-2']  files=5
       Chain A (blank chart on first paint) and chain J1 (view-mode switch regenerates at the old granularity). ganttDateStore.ts:201-216 generateDates reads its own just-written signals; give it parameters `(start = ganttStart(), end = ganttEnd(), stepVal = step(), unitVal = unit())` so the public no-arg form still works. setupDates (
   - E1-4 [bug/P0/L] Gesture-commit boundary: thread applied geometry/progress from onDragMove into onDragEnd, onResizeEnd(taskId, geom), resolveResizeConstraints(taskId, ctx, geom)  deps=['E1-1']  files=7
       Chains B, C, D (and H downstream): useDrag.handleMouseUp runs the final onDragMove (writes) then onDragEnd (re-reads) in one stack. Make onDragMove record what it wrote and onDragEnd use that: in useBarDrag.ts set `data['lastGeom'] = { x, width }` in each write branch (:164 `{x:newX, width: originalWidth}`, :196-199 `{x:newX, wi
   - E1-5 [bug/P1/S] useDrag/useBoxSelect: non-reactive mirrors for gesture guards; re-entrancy guard on dragData  deps=[]  files=2
       Chain J3 and lib-hooks: useDrag reads `dragState()` at seven internal sites expecting the value it just staged (:79, :84, :130, :155, :161, :177, :196). Add `let dragStateNow: DragState = 'idle'` next to `rafId/pendingMove/dragData` (:73-75), write it beside every setDragState (:218, :183), read it in every guard and pass it to 
   - E1-6 [bug/P0/M] useGanttScroll takes the containerApi accessor; container passes measured dimensions as data  deps=['E0-3']  files=3
       Showstopper (b) and chain I: useGanttScroll.handleContainerReady creates six effects inside the onContainerReady callback, which fires from GanttContainer's onMount (children-forbidden after the swap). Invert the API now: `useGanttScroll(containerApi: Accessor<ContainerAPILike | null>)` declares `scrollLeft/scrollTop/viewportWid
   - E1-7 [bug/P1/M] Virtualization: tracked grouping memo (shared helper) instead of count-keyed caches; pool counters via memo prev; explicit expandedTasks dependency  deps=['E1-1', 'E1-2']  files=3
       useTaskVirtualization.ts:70-96 and its copy TaskLayerMinimal.tsx:45-73 invalidate only when the key COUNT changes; under coalesced flushes a same-tick add+remove leaves the grouping permanently stale (chain G). Extract `groupTasksByResource(tasks: Record<string, ProcessedTask|undefined>, keys: string[])` into a pure helper and m
   - E1-8 [bug/P1/M] ShowcaseDemo: explicit-patch mirrors (updateTaskA/B(patch)), single-draft applyPreset, handleResizeEnd(taskId, geom)  deps=['E1-1', 'E1-4']  files=1
       Seven input handlers write a config store then call updateTaskA/updateTaskB which re-read it (ShowcaseDemo.tsx:1084-1085, 1097-1098, 1115-1119, 1138-1142, 1188-1192, 1213-1217, 1775-1776); applyPreset (:807-875) stages four updateBarPosition writes then reads getTask back and clobbers them with whole-node replacements. On 1.9 ma
   - E1-9 [bug/P2/L] ConstraintDemo: explicit-argument updateTasks(key) and pure-planner resolveMovement over a local working map  deps=['E1-1']  files=1
       ConstraintDemo.tsx:777-779 writes selectedScenario then calls updateTasks() which reads the allTasks memo derived from it (stale under deferred writes -> invisible bars). Make `updateTasks(key)` build from the key explicitly and have onChange call `updateTasks(e.target.value)` after setSelectedScenario. resolveMovement (:90-275)
   - E1-10 [bug/P2/S] GanttPerfIsolate: cascade computed from explicit geometry before the write; mount GanttEventsProvider unconditionally  deps=['E1-1']  files=1
       GanttPerfIsolate.tsx:1645-1648 handleConstrainResize writes the dragged task then calls calculateCascadeUpdates, whose context reads mockTaskStore.getBarPosition back (stale width under deferred writes). Build the cascade context first with an overlay: `getBarPosition: (id) => id === taskId ? { ...mockTaskStore.getBarPosition(id

## E2 [P0] The swap: solid-js 2.0.0-rc.6 + @solidjs/web + @solidjs/vite-plugin, imports, removed symbols, contexts, vitest projects (one atomic commit)
   goal: Move the workspace to the rc.6 runtime and toolchain in one commit that leaves typecheck, lint, both Vitest projects, build and build:demo green, using a demo-only compat shim so the 20 @ts-nocheck demos are touched only on import lines.
   gate: pnpm install (lockfile committed) && pnpm typecheck && pnpm lint && pnpm prettier-check && pnpm test (both projects: client jsdom + server node, all 18 suites) && pnpm build && pnpm build:demo. Runtime smoke is NOT expected to pass yet (Gantt's effects still write inside computes until E3-1); note t
   - E2-1 [task/P0/XL] Atomic swap commit: deps, vite/vitest/tsconfig, import moves, removed symbols in library, createContext(null), test flush/createRoot restructure, demo compat shim  deps=['E0-1', 'E0-2', 'E0-3', 'E0-5', 'E0-6', 'E1-1', 'E1-2', 'E1-3', 'E1-4', 'E1-5', 'E1-6', 'E1-7', 'E1-8', 'E1-10']  files=14
       Checklist (execute in this order, commit once). (1) package.json: dependencies solid-js 2.0.0-rc.6 + @solidjs/web 2.0.0-rc.6 (exact); devDependencies @solidjs/vite-plugin 3.0.0-next.38, @solid-primitives/raf 4.0.0-next.2 (moved from dependencies), jsdom ^25; remove vite-plugin-solid, babel-preset-solid, @solid-primitives/{memo,r

## E3 [P0] Runtime legality under the 2.0 write guard and onSettled (library path renders)
   goal: Make the shipped <Gantt> render and interact correctly in rc.6 dev mode: no write inside a compute, no primitive inside onSettled, gesture-commit callbacks see committed state, DbDemo persists exact geometry.
   gate: pnpm dev with dev-mode console open: examples/gantt.html, showcase.html, box-select.html, multi-select.html, export.html, critical-path.html, filter-search.html, db.html (+ pnpm dev:server) load with zero REACTIVE_WRITE_IN_OWNED_SCOPE / PRIMITIVE_IN_FORBIDDEN_SCOPE / invalid-cleanup errors, bars are
   - E3-1 [bug/P0/M] Gantt.tsx: split every effect into compute/apply; plain prevViewMode; onReady deferred via queueMicrotask  deps=['E2-1', 'E1-3']  files=2
       Six effects write from their compute (all throw in rc.6 dev; untrack does not clear the owner). Selection (:184-188): `createEffect(() => [...selectionStore.selectedIds()], (ids) => { props.onSelectionChange?.(new Set(ids)); })` — compute returns a plain array so apply never reads a proxy. Setup (:290-304): `createEffect(() => {
   - E3-2 [task/P1/S] GanttContainer onSettled hygiene: single returned cleanup, timer disposal, seeded dimensions, documented onContainerReady contract  deps=['E2-1', 'E1-6']  files=1
       E2-1 converted onMount -> onSettled mechanically; finish it: the ResizeObserver binding is hoisted out of the `if (scrollAreaRef)` guard and returned as the ONLY cleanup (`return () => ro?.disconnect();`), `setViewportHeight(scrollAreaRef.clientHeight)` is seeded next to setContainerWidth (:172) so viewportHeight is not observer
   - E3-3 [task/P1/S] Gesture-commit flush(): commit the store before public onDateChange/onResizeEnd/onProgressChange fire  deps=['E2-1', 'E1-4']  files=2
       E1-4 made the library's own consumers read threaded geometry, but host apps receive onDateChange/onResizeEnd/onProgressChange from the same mouseup stack and may read the exported stores (DbDemo does). useDrag.handleMouseUp is a DOM event handler, where flush() is LEGAL: `import { flush } from 'solid-js'` and call `flush()` imme
   - E3-4 [bug/P1/M] DbDemo drag-PATCH under deferred writes: capture x/width into patches, flush() first, functional TaskForm updaters, onSettled block body  deps=['E3-3', 'E3-1']  files=2
       Chain H (persisted data). DbDemo.tsx:146 onDateChange: make `flush()` the first statement (unowned handler, legal) before the `for (const id of Object.keys(taskStore.tasks))` loop at :195; push `{ id, start, end, x: bar.x, width: bar.width }` at :219 and after the PATCH set `lastX.set(p.id, p.x); lastWidth.set(p.id, p.width)` (:

## E4 [P1] Deferred-write and strict-read semantics in components, hooks, arrows and harnesses
   goal: Remove every remaining STRICT_READ storm, proxy leak into apply phases, hidden-dependency hack and same-tick latch that the audit found, and take the memo-laziness decision with measurements.
   gate: pnpm dev with dev console: examples/custom-columns.html, gantt.html (hover popup + modal), perf-isolate.html?bar=full, experiments.html, index-test.html show zero STRICT_READ_UNTRACKED warnings during scroll and drag; pnpm test passes; pnpm build:demo passes.
   - E4-1 [bug/P1/S] ColumnPanel: row-level memo + inline cell expression (no store reads in For callback bodies); document ColumnDef.render purity  deps=['E3-1']  files=3
       ColumnPanel.tsx:202 `const task = firstTaskForResource(item.id)` and :211-220 `const value = ...` read the task-store proxy inside `<For>` callback bodies, which carry a strict-read label in rc.6 -> STRICT_READ_UNTRACKED per cell per render, and the cells never updated when task data changed (pre-existing silent bug). Rewrite: `
   - E4-2 [task/P2/S] useGanttModals: snapshot() at the memo boundary; SummaryBar reads leaves; RowLayout/ConstraintContext invariants documented  deps=['E3-1']  files=4
       useGanttModals.ts:45-63 memos return live store proxies (getTask) into TaskPopup/TaskModal, which would warn STRICT_READ_UNTRACKED if read inside an apply; wrap the four returns in `snapshot(...)` from solid-js (getBarPosition already allocates a plain object; give those two memos `{ equals: (a,b) => a?.x===b?.x && a?.y===b?.y &
   - E4-3 [decision/P2/M] Memo laziness policy: measure mount cost, apply { lazy: true } only to measured rarely-read memos  deps=['E3-1', 'E2-1']  files=5
       rc.6 memos are eager and non-autodisposing by default; `{ lazy: true }` defers the first compute AND opts into autodisposal (teardown when unobserved, recompute on next read), which churns for virtualized bars (digest critic). Measure first: examples/perf.html at 10K tasks, mount time and scroll frame time (chrome-devtools-cli p
   - E4-4 [bug/P2/M] Arrow layers: real dependencies (no untrack key-set, no positionVersion counter), per-instance caches, regression test  deps=['E3-1']  files=5
       Demo-only today (the shipped Gantt mounts no arrow layer — verified by grep), but both layers hide dependencies: ArrowLayer.tsx:103 untrack suppresses the store's ownKeys tracking so positionMap depends only on draggingTaskId (eager first run reads pre-flush state and never recovers -> permanently blank); ArrowLayerBatched.tsx:1
   - E4-5 [task/P3/M] Perf harness semantics: splitEquals decision, closure-cache experiments, index-test probes, RAF-end flush() where frame timing is the point  deps=['E3-1', 'E0-6']  files=4
       GanttExperiments.tsx:706/722 previously swallowed `{ equals: idsEqual }` as the initial value; after E2-1 the equality is active — decide whether the experiment is 'custom equality' (keep) or 'ID staging without equality' (remove the option) and record it. smartCache (:652-698) and cachedVisibleTasks (:596-599) mutate closure st
   - E4-6 [chore/P3/S] GanttPerfIsolate legacy bar variants: normalize props.task shape (accessor) to remove 6 STRICT_READ top-level reads  deps=['E1-10', 'E3-1']  files=1
       BarMinimal/BarWithText/BarWithHandles/BarReactive/BarWithDrag/BarWithEvents/BarFull treat props.task as a plain object while the <For> at :2038 passes an accessor; they are already broken in 1.x (task._bar undefined) and additionally warn STRICT_READ in 2.0. Converge on the V8-V17 shape (`typeof props.task === 'function' ? props

## E5 [P1] Test hardening for deferred-write semantics
   goal: Pin the behaviors the migration fixed so they cannot regress: first-paint pipeline, delete semantics, expandedTasks reactivity, same-tick virtualization invalidation, drag-end geometry, view-mode regeneration, setter composition; optionally add component tests with diagnostics matchers.
   gate: pnpm test (client + server projects) green; new suites listed in the issue run under the client project; optional diagnostics test reports toHaveNoDiagnostics for a bare <Gantt> mount.
   - E5-1 [task/P1/M] Regression suites: first-paint pipeline, removeTask delete, expandedTasks reactivity, same-tick add+remove grouping, drag-end geometry via synthetic events, changeViewMode window, makeSetter composition  deps=['E3-1', 'E3-3', 'E1-7']  files=6
       Write with flush() after every write and no signal writes inside createRoot bodies (store writes are allowed there). (a) ganttSetup: fresh stores -> initializeTasks -> flush -> every task `_resourceIndex >= 0`, `_isHidden false`, `_bar.x` from its start date, dates().length > 0 (already added in E1-3; extend with the explicit-re
   - E5-2 [feature/P3/M] Optional: @solidjs/testing-library component test of bare <Gantt> with @solidjs/diagnostics toHaveNoDiagnostics  deps=['E5-1', 'E3-2']  files=4
       Per decision E0-1 (optional deps): add @solidjs/testing-library 1.0.0-beta.3 and @solidjs/diagnostics 2.0.0-rc.6 as exact devDependencies, register `@solidjs/diagnostics/vitest` matchers in a setupFiles entry for the client project, and write one test that renders `<Gantt tasks={...} />` (bare, no provider — this pins the create

## E6 [P1] Demos on the real APIs, smoke matrix, perf re-baseline, built artifacts
   goal: Retire the demo compat shim, strip @ts-nocheck where decided, run the full example smoke matrix, re-baseline the performance corpus on rc.6, and refresh dist-demo/ as the final commit.
   gate: grep -rn solidCompat src returns nothing; pnpm typecheck passes with @ts-nocheck removed from the component/feature/db demos; smoke matrix checklist fully ticked; benchmarks/scripts/*.sh run against a fresh dist-demo and the new tables are in the docs; final commit is exactly 'chore(demo): refresh b
   - E6-1 [task/P1/L] Component/feature demos on real 2.0 APIs: ArrowDemo, BarDemo, ConstraintDemo, ShowcaseDemo, features, db (drop shim; storePath or drafts; onSettled; strip @ts-nocheck)  deps=['E3-1', 'E3-4', 'E1-9']  files=9
       Replace shim imports with real APIs: `<Index>` -> `<For keyed={false}>` (BarDemo:397; note its :395 comment about pooling still holds), onMount -> onSettled with block bodies (ArrowDemo:350, BarDemo:173, ConstraintDemo:662 -> prefer a split createEffect `createEffect(() => allTasks(), (t) => { taskStore.updateTasks(t); })` repla
   - E6-2 [task/P2/L] Perf harnesses and benchmarks on real 2.0 APIs: shared helper -> onSettled, index-test/MinimalTest/PerfDemo/PerfIsolate/Profiler/Experiments/DateHeadersOptimized, profiler index.js ESM  deps=['E3-1', 'E4-5', 'E4-6']  files=10
       Convert the one shared helper (E0-6) to `onSettled(() => { ...; return () => { ro.disconnect(); cancelAnimationFrame(raf); clearTimeout(t); }; })`; convert remaining shim usages: `<Index>` -> `<For keyed={false}>` at GanttMinimalTest, GanttExperiments (3), DateHeadersOptimized, index-test (:2298/2307/2316/2325); produce -> draft
   - E6-3 [chore/P1/M] Full demo smoke matrix run (21 examples) with dev-mode console — checklist issue  deps=['E6-1', 'E6-2', 'E4-1', 'E4-2', 'E4-4', 'E5-1']  files=1
       Run pnpm dev (and pnpm dev:server for db.html) and tick every row of the smoke matrix in the verification plan: page loads with zero dev errors/warnings, first paint shows bars, and the page's primary interaction works (drag/resize/progress/keyboard, selection, box-select, export, filters, view-mode switch, expand/collapse, perf
   - E6-4 [chore/P2/M] Perf re-baseline on rc.6: build:demo, serve, run bench scripts, date-stamp old tables, append new results  deps=['E6-3', 'E4-3']  files=5
       All ~10 result tables (PERFORMANCE.md:13-20, 292-300, 385-393, 524-529; DEMOS.md:121-126, 130-140, 202-208; EXPERIMENTS.md:33-44; ANALYSIS.md:9-16, 178-254) were measured on solid-js 1.9.12 and the harness changed (<Index> -> <For keyed={false}>, deferred DOM writes, eager memos). `pnpm build:demo`, `npx serve dist-demo -l 5174`
   - E6-5 [chore/P1/S] chore(demo): refresh built artifacts  deps=['E6-4']  files=4
       Final commit of the migration branch, following the repo convention (commits 20bd2e6, 2e9d8bd, 4a0633e): run `pnpm build:demo`, `git add -A dist-demo`, remove the stale hash-named chunks that the build no longer emits (see bd memory 'when-checked-in-build-artifacts-dist-demo-aren'), and commit as 'chore(demo): refresh built arti

## E7 [P2] Docs, agent guidance, memory, and follow-ups
   goal: Make the written guidance match the migrated code (several current sentences prescribe patterns that now throw), rewrite the bd memory, add the README install/contract section, and file the deliberate follow-ups (re-bump, peerDependencies, DbDemo modernization, UMD bundle).
   gate: grep -rn 'untrack' docs/PERFORMANCE.md shows no write-wrapping advice; grep -rn "solid-js/store\|solid-js/web\|<Index\|Context.Provider\|onMount" README.md CLAUDE.md AGENTS.md docs returns only dated historical tables; bd recall of the rewritten memory prints the new text; follow-up issues exist and
   - E7-1 [task/P1/M] CLAUDE.md + AGENTS.md: Solid 2.0 rules (deferred writes, flush legality table, onSettled constraints, callback block bodies, built-ins-in-store, For strict-read, split-effect pattern), fix file-name drift  deps=['E6-3']  files=2
       Add a 'SolidJS 2.0 rules' section (near CLAUDE.md:7 and mirrored in AGENTS.md so non-Claude agents get it): (1) writes are staged; reads return committed values until the microtask/flush(); (2) flush() legality table — legal in event handlers/timers/promise continuations/test bodies, silent no-op in effect apply, throws in onSet
   - E7-2 [task/P1/S] README: install section (solid-js + @solidjs/web), useGanttStores contract, setter timing note, UMD/exports outcome  deps=['E6-3']  files=2
       README has no installation section (digest tooling critic): add `pnpm add ganttss solid-js@2.0.0-rc.6 @solidjs/web@2.0.0-rc.6` with a note that both runtimes must be the same instance as the host app (resolve.dedupe guidance) until peerDependencies land (E7-5). Keep README.md:106 ('useGanttStores() returns undefined outside a pr
   - E7-3 [task/P2/M] docs/*: ARCHITECTURE, PERFORMANCE (untrack-write advice), MINIMAL_TEST, DATABASE (drag-PATCH), DEMOS, EXPERIMENTS, RFC-undo-redo; benchmarks/traces/ANALYSIS.md references  deps=['E6-4']  files=8
       PERFORMANCE.md:407 (Y-sync effect 'wrap position updates in untrack()') -> the split-effect pattern from E3-1; :421 untrack sample -> note that untrack around a proxy lookup leaves leaf reads tracked and warns STRICT_READ_UNTRACKED in 2.0. ARCHITECTURE.md:365 (updateTasks via reconcile(v,'id')), :379 (Set-in-store table: safe vs
   - E7-4 [chore/P2/S] bd memory: rewrite 'solidjs-createstore-produce-state-id-newobj-never-wrap' for 2.0 (forget + remember)  deps=['E6-3']  files=1
       Current text says 'createStore + produce + state[id] = newObj' and prescribes object replacement; after E1-1 the store uses leaf mutation and produce is gone. `bd forget solidjs-createstore-produce-state-id-newobj-never-wrap` then `bd remember` a new entry: 'SolidJS 2.0 taskStore: mutate leaves in the draft (task._bar.x = v), ne
   - E7-5 [task/P3/M] Follow-ups: re-bump to newest RC/stable and relax pins; peerDependencies; DbDemo actions/optimistic; self-contained bundle if UMD consumers exist  deps=['E6-5']  files=3
       File as separate issues under this epic, all explicitly optional and post-merge: (a) re-bump solid-js/@solidjs/web/@solidjs/vite-plugin/@solid-primitives/raf together to the newest RC (or 2.0.0 stable), re-run the full verification plan, and relax to caret ranges once stable — one commit; (b) move solid-js and @solidjs/web to pe

TOTAL child issues: 39
CRITICAL PATH: E0-1 decisions -> E0-3 dead code (unblocks E1-6) -> E1-1 taskStore setters (unblocks E1-4, E1-7, E1-8) -> E1-2 config store (unblocks E1-3) -> E1-3 setup pipeline returns window -> E1-4 gesture-commit geometry threading -> E1-6 useGanttScroll accessor redesign -> E1-8 ShowcaseDemo mirrors -> E2-1 atomic swap -> E3-1 Gantt effects split (first rc.6 render) -> E3-3 gesture-commit flush() -> E3-4 DbDemo persistence -> E5-1 regression suites -> E6-1 demos on real APIs -> E6-2 perf harnesses on real APIs -> E6-3 smoke matrix -> E6-4 perf re-baseline -> E6-5 dist-demo refresh -> E7-1 CLAUDE.md/AGENTS.md rules
SEQUENCING: The digest's two verdicts drive the order: chain A fails closed (blank chart, no error) and chain H persists stale data, and both are read-your-own-write architectures rather than syntax. Those redesigns are pure refactors on 1.9.12, so they go FIRST (E1), where the synchronous runtime and the existing dev-server smoke verify them for free and where each is its own revertable commit; by the time the runtime changes, the store layer no longer uses produce or path setters and the swap commit becomes a literal body move. E0 runs before E1 only for the items E1 touches (dead code in useGanttScroll/GanttContainer must go before the accessor redesign; the widened globs must exist before the swap so the swap is checked). E2-1 is the single unavoidable big commit and is kept mechanical by the demo shim; it depends on all of E0/E1 except the optional ConstraintDemo planner. E3 follows immediately
VERIFICATION: Per-commit gate (every issue): pnpm typecheck && pnpm lint && pnpm prettier-check && pnpm test && pnpm build && pnpm build:demo. From E2-1 on, pnpm test must show two projects (client: jsdom, 14 suites in tests/*.test.ts; server: node, 4 suites in tests/server/) and both green. Per-epic gate: the epic's verificationGate text (dev-server smoke with the dev console open; any REACTIVE_WRITE_IN_OWNED_SCOPE, PRIMITIVE_IN_FORBIDDEN_SCOPE, CLEANUP_IN_FORBIDDEN_SCOPE, invalid-cleanup-return error or STRICT_READ_UNTRACKED warning fails the gate). End-of-migration (before E6-5 and again on the merge commit): (1) pnpm install from a clean node_modules on node 22; (2) the per-commit gate; (3) pnpm build output inspected: dist/ganttss.es.js externalizes solid-js and @solidjs/web (no renderer code inlined), dist/ganttss.css present, no umd file referenced by package.json; (4) demo smoke matrix with pn
DECISIONS:
   ? Exact pins or caret ranges for the RC line (solid-js, @solidjs/web, @solidjs/vite-plugin, @solid-primitives/raf)? => Exact pins for all four during the RC line; relax solid-js/@solidjs/web to ^2.0.0 only at stable via the E7-5 re-bump issue. (rc-to-rc changes have already altered write-guard semantics (createRoot exemption for stores only) and plugin test posture; a caret bump would silently invalida)
   ? Feature branch or migrate on main? => Feature branch `solid-2`, one commit per issue, merge with a merge commit after E6-5. (main must stay releasable on 1.9.12 (publish.yml triggers on the release branch); per-issue reverts and the stated window where the chart throws (between E2-1 a)
   ? Drop the UMD format or ship a self-contained bundle? => Drop 'umd' in E2-1 (ESM-only; update package.json main/exports.require and README); file the self-contained IIFE bundle as an optional follow-up (E7-5d) only if a consumer asks. (Neither solid-js nor @solidjs/web publishes a global build, so the SolidJS/SolidJSWeb globals contract has no upstream artifact — the UMD output has been decora)
   ? peerDependencies for solid-js and @solidjs/web now or later? => Later (E7-5b), after merge; during migration keep them in dependencies with exact pins and add resolve.dedupe guidance to the README. (It is the correct library shape (two runtimes that must be the host's instance) but it changes consumers' install story; keeping it out of the migration keeps t)
   ? Keep or delete: taskStore collapse API, useGanttScroll isScrolling, dead ContainerAPI surface, GanttSubtaskDemo, duplica => Keep the taskStore collapse API (exported public TaskStore type; collapsedTasks() is consumed by Gantt.tsx:294 and ganttSetup.ts:116; migrating its three tests costs ~20 lines). Delete isScrolling, th (Only the collapse API is public and partially live; the rest has zero readers in src/ or tests/ and each is the hardest site in its file to migrate.)
   ? DbDemo modernization scope? => Minimal in E3-4 (flush() first, captured x/width, functional TaskForm updaters, onSettled block body); actions/optimistic store + geometry-through-onDateChange as follow-up E7-5c. (The minimal fix closes the data-corruption chain H; the modernization is a public Gantt API addition and a demo rewrite, which the user asked to see as separate)
   ? Memo laziness policy ({ lazy: true } opts into autodisposal as well as deferral)? => Eager by default; apply lazy only to the measured rarely-read memos in E4-3 (useBarConfig showExpectedProgress/ignoredPositions/readonlyProgress, getAllDateInfos, modal/popup formatters) after a 10K-t (Four audits recommended lazy without stating the autodisposal churn for virtualized bars; one measured decision across ~25 memos beats 25 local guesses.)
   ? expandedTasks representation: signal-held immutable Set or Record<string, true> inside the store? => Signal-held immutable ReadonlySet (E1-2). (Matches the three existing safe instances (collapsedTasks, collapsedGroups, selectedIds), keeps the public option type Set<string> and rowLayoutCalculator's par)
   ? Which demos lose @ts-nocheck? => Strip it from the component/feature/db demos (Arrow, Bar, Constraint, Showcase, BoxSelect, MultiSelect, CriticalPath, FilterSearch, Export, CustomColumns, Gantt*, Db + db/*); keep it on the five perf  (tsc proves nothing behind @ts-nocheck, and the component demos are the ones that document the public contracts; the harnesses are 1-2K-line exploratory files wh)
   ? Add the optional @solidjs/testing-library and @solidjs/diagnostics devDependencies? => Yes, as E5-2 (one component test file, exact pins), droppable if the beta misbehaves. (toHaveNoDiagnostics turns the migration's whole hazard class (write guard, strict-read storms, invalid cleanup returns) into one assertion on a bare <Gantt> mou)
   ? useGanttStores return type: keep GanttStores | undefined or switch to | null? => Keep undefined via `useContext(Ctx) ?? undefined` (E2-1). (README.md:106 and Gantt.tsx:170 depend on it; zero public API churn.)
RISKS:
   ! The E2-1 swap commit is large and, by design, leaves the dev server throwing until E3-1 lands. => Everything movable is moved out (E0/E1 before, demo shim during); the commit message and the E2 gate state the expected runtime state; E3-1 is the next issue and is small once E1-3/E1-6 exist; the bra
   ! 1.9 pre-swap refactors (E1) could regress current behavior in ways the tests do not cover. => Each E1 issue has a dev-server smoke acceptance on the current runtime and lands as its own revertable commit; E1-1's strengthened gantt-6hx guard and E1-3's first-run assertions are added in the same
   ! <For keyed={false}> over sparse pooled arrays (holes) may not preserve <Index>'s pooling, tearing down Bar listeners mid-drag. => E2-1 acceptance explicitly proves holes render as undefined rows and a bar survives a drag; fallback is `<Repeat count>` for the pools (E6-2 benchmarks it for the header).
   ! @solidjs/vite-plugin's native compiler needs a postinstall that pnpm 10 blocks, or resolves the .js->.ts import rewrite differently. => E2-1 checklist: add @solidjs/compiler to onlyBuiltDependencies if pnpm reports a blocked script; build:demo in the gate exercises the demo import paths.
   ! Vitest projects: server suites (better-sqlite3, drizzle migrate, Hono app.fetch) misbehave under the shared worker pool or the client project picks up tests/ser => Root config stays condition-free; include globs are disjoint (tests/*.test.ts vs tests/server/**); the gate requires both projects to report.
   ! Eager memos regress mount time on large charts (7 useBarConfig memos per Bar) and STRICT_READ storms make the dev console unusable. => E4-3 measures before deciding; E4-1/E4-2/E4-6 remove the known storms; the smoke matrix requires a warning-free console.
   ! RC churn: a newer rc changes an API this plan relies on (reconcile key arg, storePath, lazy option) before the branch merges. => Exact pins; E7-5 owns the re-bump with the full verification plan; no issue depends on unreleased behavior.
   ! Perf baselines are silently compared across runtimes. => E6-4 date-stamps every 1.9 table and appends rc.6 tables; E6-5 refreshes dist-demo so the bench scripts serve the new runtime.
FOLLOWUPS: Re-bump to the newest RC / 2.0.0 stable and relax pins to caret (E7-5a). | peerDependencies for solid-js and @solidjs/web (E7-5b). | DbDemo -> action() + optimistic store; pass geometry through onDateChange so the demo stops scraping window.__ganttTaskStore (E7-5c). | Self-contained IIFE bundle for global-script consumers if requested (E7-5d). | Vitest browser mode for drag tests if jsdom synthetic events prove flaky (E7-5e). | Consider `<Repeat count from>` for the TaskLayer pools if E6-2's header benchmark favors it. | Retype ColumnDef.render and the 20 GanttConfigStore setters in a documented API changelog entry for the next release.

### REVIEW (score 8): Strong plan, and the strongest single idea in the field: doing the read-your-own-write redesigns on 1.9.12 first, as pure refactors verified by the current runtime, so the swap commit becomes a body move. Coverage of the digest's high-severity hazards is near-complete (chains A, B, C, D, F, G, I, J1, J2, J3 all owned; showstoppers a-h owned), the file:line citations I spot-checked are accurate (config/vite/solid.js:14/18/20-22/28, publish.yml node 18 + pnpm 9, tsconfig include, SummaryBar.tsx:63, tests/ counts 14+4=18), and the plan correctly overrides the three audit errors the critic caught (no flush in apply, no flush in onSettled, onSettled is untracked, GanttEventsProvider IS mounted). It also encodes the createRoot signal/store asymmetry that the briefed FACTS got wrong. Three defects block execution as written. (1) The compat shim is sold on two things it cannot do: it cannot intercept ShowcaseDemo's 38 path setters (a call shape, not an import), and its onMount=onSettled throws in six demo bodies that call onCleanup — including E0-6's own shared helper — so the demos are broken from E2-1 to E6-1/E6-2 and the 'green after every commit' principle fails. (2) E4-4 writes tests/
  BLOCKING (3):
    * COMPAT SHIM IS FICTION FOR THE TWO THINGS IT IS SOLD ON. E2-1 says the shim (Index/onMount/batch/produce) 'keeps the 20 @ts-nocheck demos out of that commit entirely' and E1-8 leaves the 38 ShowcaseDemo path setters because 'the compat shim covers them through the swap'. Path setters are a CALL SHAPE on a real 2.0 store setter (`setTaskConfig('name', v)`), not an importable symbol — audit-result c
    * SHIM onMount = onSettled THROWS IN SIX DEMO BODIES AND IN THE PLAN'S OWN HELPER. FACT 3: onSettled is children-forbidden — onCleanup inside throws. The digest lists onCleanup-inside-onMount at GanttPerfIsolate.tsx:1769, GanttMinimalTest.tsx:439, GanttExperiments.tsx:978, GanttPerfDemo.tsx:276/298, GanttProfiler.tsx:177 (19 onCleanup-in-lifecycle sites total). Worse, E0-6 builds src/demo/shared/use
    * E4-4 WRITES A TEST THAT CANNOT RUN, AND E5-2 NEARLY REPEATS IT. E2-1 sets the client project include to ['tests/*.test.ts'] and E0-2 leaves tsconfig include ('tests/**/*.ts', verified at tsconfig.json:22), the lint glob and the prettier glob ('tests/**/*.ts', package.json:30/32/33) unchanged. E4-4 creates tests/arrowLayer.test.tsx and does not list vitest.config.ts in its files, so its 'write the 
  MAJOR (8):
    * THE 'ZERO STRICT_READ WARNINGS' GATE IS UNREACHABLE — 24 AUDITED top-level-read SITES HAVE NO OWNING ISSUE. E4's gate demands 'zero STRICT_READ_UNTRACKED warnings' on gantt.html / perf-isolate / experiments / index-test and E6-3 requires a warning-free console, but the plan only owns ColumnPanel (E4-1), index-test's three V7 probes (E4-5) and GanttPerfIsolate's legacy variants (E4-6, itself marked
    * DOCS AND bd MEMORY LAND DEAD LAST, CONTRADICTING THE DIGEST'S OWN VERDICT. digest-t1 CROSS: 'Fix the FACTS document once, before any batch writes test code'; the flush-legality table, the createRoot signal/store asymmetry and the block-body rule are exactly what an executing agent needs while doing E1-E6. Yet E7-1 (CLAUDE.md/AGENTS.md rules) depends on E6-3 and E7-4 (bd memory, 'if bd memories are
    * resourceStore.collapseAll (src/stores/resourceStore.ts:129-132) IS NOT COVERED. Chain G's fix (2) is 'Give collapseAllTasks (taskStore.ts:232-243) AND resourceStore.collapseAll (:129-132) an explicit id-list parameter'; E1-1 does only the taskStore half. collapseAll reads the getGroups() memo derived from resources() then writes, so called in the same turn as updateResources it collapses stale gro
    * E3-1's SETUP EFFECT MAY SILENTLY STOP RESPONDING TO COLLAPSE, AND NOTHING PINS IT. E3-1 (following the digest) writes compute as `() => { const tasks = effectiveTasks(); resourceStore.collapsedGroups(); taskStore.collapsedTasks(); return tasks; }`. If createEffect gates apply on the compute VALUE changing (default equality), a group-collapse toggle re-runs compute, returns the same `tasks` referen
    * benchmarks/constraint/earlyTerminationBench.ts IS ONE OF ONLY TWO FILES THE OFFICIAL SMA REPORT NAMES AND APPEARS IN NO ISSUE. It is inside tsconfig include and the lint/prettier globs (verified: 'benchmarks/constraint/**/*.ts' in tsconfig.json:22 and package.json:30/32/33), so E2-1's typecheck/lint gate can fail on a file no issue owns. The plan covers the SMA's other named file (memoTracker.js, 
    * E6-2 (REQUIRED) DEPENDS ON E4-6 (MARKED OPTIONAL). E6-2's dependsOn is ['E3-1','E4-5','E4-6'] while E4-6's notes say 'Optional.' If E4-6 is dropped, `bd ready` never surfaces E6-2 and the critical path (E6-2 -> E6-3 -> E6-4 -> E6-5) stalls. Same shape in reverse for E1-9, which E2-1 deliberately excludes yet E6-1 requires.
    * PUBLIC API CHANGES ARE SPREAD ACROSS 'bug' ISSUES WITH NO API-CHANGE DECISION OR CHANGELOG ISSUE. E1-1 adds taskStore.patchTask and collapseAllTasks(ids?), E3-1 adds taskStore.setBarYs, E1-2 retypes 20 GanttConfigStore setters from Setter<T> to void-returning, E1-4 widens onResizeEnd to (taskId, geom?), E4-1 changes the exported ColumnDef.render contract, E2-1 changes useGanttStores' internals. Al
    * E2-1 IS BIGGER THAN IT NEEDS TO BE. The shim-import rewiring ('every demo/entry that imported those from solid-js or solid-js/store imports them from the shim instead' — ~20 files) is runtime-neutral on 1.9: create src/demo/shared/solidCompat.tsx in E0 re-exporting Index/onMount/batch/produce straight from solid-js, rewire the demos there, and let E2-1 change only the shim's four internals. That r
  FACTERRORS (5):
    * 'a demo-only compat shim (Index/onMount/batch/produce) keeps the 20 @ts-nocheck demos out of that commit entirely' (E2-1 rationale) and 'Leave the 38 path setters ... the compat shim covers them through the swap' (E1-8 notes). FACT 7: path setters are removed. `setTaskConfig('name', v)` is a call against a real createStore setter — E2-1 itself moves those demo files' imports to real solid-js — so 
    * Shim `onMount(fn)` implemented as 'onSettled with a block body' contradicts FACT 3 (children-forbidden: no onCleanup inside, throws). Six demo onMount bodies contain onCleanup (GanttPerfIsolate.tsx:1769, GanttMinimalTest.tsx:439, GanttExperiments.tsx:978, GanttPerfDemo.tsx:276/298, GanttProfiler.tsx:177), and E0-6's own useDemoViewport.ts is specified as 'onMount + onCleanup inside'. All of them t
    * E1-7: 'Pool high-water counters become createMemo((prev) => ..., [])-style derivations ... keep it 1-arg-compatible for E2-1'. FACT 6: createMemo takes no initial-value argument in 2.0, and `createMemo(fn, [])` is precisely the removed form; the sentence also contradicts itself (a 2-arg call is not 1-arg-compatible). E2-1's `{ loadingValue }` guidance is the correct target and the two issues disag
    * package.json engines.node '>=20.19' does not match the plugin's declared '^20.19.0 || >=22.12.0' (it admits Node 21.x, which upstream excludes).
    * E6-1 asserts `setX(storePath('key', v))` as the ShowcaseDemo replacement without any verification citation, while every other API claim in the plan is sourced to the digest or the runtime. The digest only records that solid-js exports `storePath` (tooling critic, ARCHITECTURE.md:431); its signature is unpinned.
  ORDERINGERRORS (8):
    * E4-4 depends on E3-1 only and creates tests/arrowLayer.test.tsx, but the client project include set in E2-1 is ['tests/*.test.ts'] — the 'write the regression test first' step cannot run at all. E5-2 has the same exposure. The glob widening must land in E0-2/E2-1, before either.
    * E6-2 (required, on the critical path) dependsOn E4-6, which its own notes mark 'Optional'. If E4-6 is skipped the dependency never closes and E6-3/E6-4/E6-5 never become ready.
    * E1-9 has no ordering relative to E2-1: it is in E1 (whose whole premise is 'pure refactor on 1.9') yet is explicitly excluded from E2-1's dependsOn and its notes permit deferral past the swap, where its file uses removed path setters/produce and the shim does not help. Pick a side.
    * E7-1 (CLAUDE.md/AGENTS.md rules) dependsOn E6-3 and E7-4 (bd memory) dependsOn E6-3, so the flush-legality table, the createRoot signal/store asymmetry and the block-body rule are written AFTER all the code that must obey them. E0-5 and E4-1 both explicitly defer their rule to E7-1. Split the rules out of E7-1 into E0.
    * E0-6 dependsOn E0-5 for no stated reason (block-bodying ExportDemo/BoxSelect/MultiSelect/DbDemo callbacks does not gate the viewport helper or the seven latches); it needlessly serializes the only parallelizable pair in E0.
    * E1-3 dependsOn E1-2 although its actual edits (generateDates parameters, setupDates returning a window, ganttSetup consuming it, one updateOptions call) do not require the expandedTasks/makeSetter work. The stated critical path is longer than the real one.
    * E3-2 and E1-6 both prescribe the same GanttContainer.tsx:172 viewportHeight seeding; whichever lands second is a no-op or a conflict.
    * E5-2 dependsOn E3-2 with no explanation (a bare <Gantt> mount test does not depend on the container's cleanup hygiene); E5-1 is the only real prerequisite.
  MISSINGSITES (20):
    * src/stores/resourceStore.ts:129-132 — collapseAll reads getGroups() then writes setCollapsedGroups (chain G fix 2); no issue covers it
    * src/stores/taskStore.ts:195-196 — isTaskCollapsed (and resourceStore isGroupCollapsed) answer about COMMITTED state; chain G fix (5) asks for this to be documented as a non-oracle; not in any issue
    * src/components/Gantt.tsx:173, 263, 462 — three component-body top-level reactive reads (lib-gantt topLevelReads=7); no issue
    * src/contexts/GanttStores.tsx:41-43 — three top-level props reads in GanttProvider; no issue
    * src/components/Bar.tsx:195-205 — eight top-level reads in the useBarDrag deps literal (lib-bars topLevelReads=8); digest says they deliberately freeze the handler set, but they still warn under the E4/E6 gates
    * src/components/TaskLayer.tsx:197-198 — props resolved eagerly into useBoxSelect (lib-virtualization: 2 component-body STRICT_READ reads)
    * src/components/TaskLayer.tsx:88 — handleConstrainPosition's read+cascade-write pair; digest-t2 asks for it to move inside one store draft (tracer downgrades it to per-frame-safe, but the plan never records that decision)
    * src/entries/index-test.tsx:1283, 1284, 1505, 1836 — top-level reads that 'fire per instance per re-create' at 340 bars/frame; E4-5 deletes only the V7d/e/f probes at 848/903/974
    * src/demo/GanttExperiments.tsx:554 and src/demo/GanttMinimalTest.tsx:270 — top-level `Object.keys(tasks)` store reads
    * src/demo/BarDemo.tsx:462 — store read inside a For child body (digest: 'quietly renders stale or empty coordinates', no warning in 2.0)
    * src/demo/BarDemo.tsx:199 — getTask + updateTask(taskId, {...task, progress}) whole-node replacement from a committed snapshot; same class as useBarDrag.ts:250 which E1-1 fixes with patchTask
    * src/demo/db/TaskForm.tsx:200 — the one top-level props read demo-db asks to untrack; E3-4 covers only :232/:254/:257/:263/:266
    * src/demo/ShowcaseDemo.tsx:595-606 — hoveredTask/modalTask memos return live store proxies into TaskDataPopup/TaskDataModal; E4-2 snapshots only useGanttModals' four memos
    * src/components/Gantt.tsx:210 — buildExportInput serializes committed taskStore.tasks/relationships (chain J2); the digest asks for an optional pre-built payload or an explicit 'reads committed state' contract
    * src/components/Gantt.tsx:382-400 — chain F's recommended option (delete the _bar.y write-back and feed useBoxSelect.findBoxHits from rowLayouts) is never weighed; E3-1 keeps the write-back via setBarYs with no decision record
    * benchmarks/constraint/earlyTerminationBench.ts — one of only two files named by the official SMA report; inside tsconfig include and lint globs; no issue
    * src/index.ts:26 — useGanttEvents' public contract (library critic MISSED item: the one index.ts export skipped while the same decision was made for useGanttStores)
    * src/demo/DateHeadersOptimized.tsx:38-95 — upperSlots allocate fresh objects per recompute so pooled accessors churn; E6-2 benchmarks Repeat-vs-For but never fixes the identity problem the digest names
    * src/components/Grid.tsx:196, src/components/DateHeaders.tsx:212/229, src/components/ColumnPanel.tsx:200 — four <For> lists over freshly-allocated memo output that lib-chrome recommends switching to keyed={false} at migration time (optional, but should be an explicit 'not doing this' decision)
    * docs/DATABASE.md:17 (names a vite.config.ts that does not exist) and the remaining CLAUDE.md drift items (b)/(c)/(e) from the tooling audit — E7-3 enumerates the perf-traces/ and DateHeadersOptimized fixes but not these
  SCOPECREEP (6):
    * E4-4 (priority 2, type bug, not marked optional) rewrites the arrow-layer architecture — deletes the positionVersion protocol including the GanttPerfIsolate.tsx:1529/1587 triggerArrowUpdate contract, moves three module caches into component scope, adds a test file — for a component the plan itself states 'the shipped Gantt mounts no arrow layer ... demo-only today'. The migration minimum is making
    * E4-5 proposes deleting recorded experiment variants ('delete the variants', 'these three variants need redesign or deletion', 'remove the option' for splitEquals) and redesigning smartCache. Deleting experiments whose results are published in docs/EXPERIMENTS.md is a product decision, not migration work; it needs to be optional or decision-gated like E0-1's other items.
    * E0-6 bundles a genuine migration prep (one shared lifecycle helper) with a seven-site latch refactor across five harnesses (GanttExperiments:898, index-test:1947, GanttMinimalTest:340, GanttPerfDemo:110/161/211, GanttProfiler:83/87). The latch fix is real but is a behavioural bug-fix, not a 2.0 requirement; mark it clearly as such so the user is not surprised by an M-effort demo refactor before an
    * E0-4 deletes src/demo/GanttSubtaskDemo.tsx and rewrites eleven examples/*.html entry paths. Both are decision-gated by E0-1, but the issue is not typed or titled as optional even though its own notes say 'Optional; safe to defer'.
    * New public API surface introduced inside migration bug-fixes without a decision line: taskStore.patchTask and collapseAllTasks(ids?) (E1-1), taskStore.setBarYs (E3-1), onResizeEnd(taskId, geom?) across GanttEvents/Bar/TaskLayer/Gantt (E1-4), 20 GanttConfigStore setters retyped from Setter<T> to void (E1-2), ColumnDef.render contract (E4-1). E0-1 should list these as an explicit API delta; the plan
    * E0-2 adds `pnpm typecheck` and `pnpm test` steps to publish.yml and consolidates the two onlyBuiltDependencies lists. Both are good hygiene and cheap, but they are CI/product changes unrelated to Solid 2 and should be called out as such rather than folded into 'make config files visible to the gates'.
  BESTIDEAS (14):
    * Do the read-your-own-write redesigns FIRST, on 1.9.12, as pure refactors (E1) — the synchronous runtime plus the existing dev-server smoke verifies them for free, each is independently revertable and bug-fix-quality on main, and by the time the runtime changes the store layer no longer uses produce or path setters. This is the single strongest structural idea in the plan and directly answers the t
    * Writing the flush()-legality table into the principles and into every issue that uses flush, with the explicit rule 'never prescribe flush() in effect apply (silent no-op) or onSettled (throws)' — the plan correctly refuses the flush() that lib-gantt prescribed at Gantt.tsx:316/321 and that lib-scroll-container prescribed inside onSettled.
    * The signals-vs-stores createRoot asymmetry as a first-class principle ('store setters are legal inside createRoot bodies, plain signal setters throw; tests never wrap writes in createRoot to fix them') — this is the fact the briefed FACTS got wrong and only two of nine audits caught.
    * E0-5's block-body sweep on 1.9 with a greppable acceptance (`=> set[A-Z]`, `=> props\.on[A-Z]`) — it neutralises the return-value-validation hazard class the demo critic named, before any callback lands in an onSettled body or an apply phase, at zero runtime risk.
    * E1-1's NEGATIVE regression assertion (a second memo on tasks['t1'].name whose run count must stay 1) as the mechanism that prevents someone porting the object-replacement shape into the draft — the digest says explicitly that without it there is no mechanism preventing regression, and it makes the hot path measurably faster rather than merely legal.
    * Threading committed geometry as DATA instead of flushing: data['lastGeom']/data['finalProgress'], onResizeEnd(taskId, geom?), resolveResizeConstraints(taskId, ctx, geom), and batchMovePositions RETURNING the map of positions it wrote — one design that closes chains B, C, D and H at the source.
    * E3-1's row-layout apply as a single setBarYs draft write that skips entries whose DRAFT y already matches — it removes the read-modify-write, the dependency on the taskStore.ts:108 guard, and chain F's cross-effect staleness in one move.
    * The useGanttScroll(containerApi) inversion plus passing measured containerWidth/containerHeight as plain numbers on the api object — deletes the six illegal effects, both write-in-compute classes, the read-back at GanttContainer.tsx:172 -> useGanttScroll.ts:52, and the extra flush hop per scroll tick.
    * Exact pins for the whole RC line with a dedicated re-bump issue (E7-5a) and the rationale that a caret bump would invalidate the audit — correct given rc-to-rc write-guard changes.
    * vitest 4 test.projects with disjoint include globs (client jsdom for tests/*, server node for tests/server/**) and a condition-free root config — this is precisely the shape the tooling critic proved is the only one that serves both postures.
    * Sequencing the perf corpus as one deliverable: date-stamp the 1.9.12 tables rather than deleting them, re-baseline against a freshly built dist-demo, and make 'chore(demo): refresh built artifacts' the final standalone commit matching 20bd2e6/2e9d8bd/4a0633e.
    * A decision-record issue (E0-1) that every later issue cites instead of re-deciding, plus the tooling-globs-first ordering so that the one unsplittable commit is actually checked by the gates it claims to pass.
    * E5-1's acceptance 'revert the E1/E3 fix locally and confirm at least one assertion in each block fails' — the only acceptance in the plan that proves the tests guard what they claim, for four chains that otherwise fail silently.
    * Restricting storePath to flat primitive-field demo stores with an explicit 'never where a Set/Map/Date lives in a store' rule attached to the same issue.
  MINOR (13): E1-7 contradicts itself and FACT 6: 'Pool high-water counters become createMemo((prev) => ..., [])-style derivations ... | engines.node '>=20.19' admits Node 21, which @solidjs/vite-plugin excludes (engines '^20.19.0 || >=22.12.0'). Use the pl | E0-2 half-fixes the tsconfig finding: the digest says include omits 'vitest.config.ts AND config/vite/*.js', the issue a | E1-2's acceptance requires a new unit test (`setColumnWidth(w => w+10)` twice -> +20) but E1-2's files list has no test  | E3-2 re-issues an edit E1-6 already prescribes: both say seed `setViewportHeight(scrollAreaRef.clientHeight)` next to se | Gantt.tsx:234 is edited twice: E2-1 converts onMount -> onSettled (block body), E3-1 rewrites the same line to onSettled | E1-1's leaf-mutation rewrite lands on 1.9 while SummaryBar.tsx:63's `createMemo(() => getPosition())` (verified: returns | E1-9 (ConstraintDemo) is scheduled in E1 as a '1.9 pure refactor' but its note says it 'can be deferred behind E2-1'. Po | Non-code issues (E0-1, E4-3, E6-3) produce no commit, which conflicts with the stated 'one issue -> one commit -> one pu | E3-3 adds flush() per gesture end 'even though geometry is already threaded'. That is legal (FACT 5) but costs a full gr | E6-1's acceptance `grep -rn 'storePath' src returns hits only in ShowcaseDemo (or none)` is a weak guard for the real in | The plan asserts a specific storePath signature (`setX(storePath('key', v))`) without citing where it was verified; ever

====================================================================================================
PLAN end-state-idioms: End-state idioms: reshape the data flow on 1.9 first, then one atomic runtime cutover, then 2.0-only refinements  | review score=8 | est effort=Roughly 14-18 engineer-days: E1 1d, E2 5-6d (E2.5 and E2.2 dominate), E3 2-3d including rehearsal, E4 2d, E5 3-4d (E5.1 and E5.2 dominate), E6 1.5d, E7 1.5d. Critical path about 11 days; E2.3/E2.4/E2.6/E2.7 and E5.3/E5.5 parallelize.
BRANCH: E1 (prune) and E2 (reshape) land on main: they are 1.9-compatible, each independently green (typecheck + lint + test + build + build:demo + dev smoke) and independently revertable, and they are worth having even if the RC line slips. E3 onward lands on a long-lived branch `solid-2.0` created from main after E2.8; every issue is one commit pushed to that branch. The branch is merged into main by fast-forward (rebase, no squash) after E5 and E6.1 are green so the one-issue-one-commit history survives. The `release` branch (publish.yml trigger) is NOT touched until the follow-up re-bump issue (E7
VERSIONS: Pin EXACT versions for the whole RC line: solid-js 2.0.0-rc.6, @solidjs/web 2.0.0-rc.6, @solidjs/vite-plugin 3.0.0-next.38 (not next.35: next.38 matches @solidjs/compiler rc.6 and adds the mode !== 'test' guards), @solid-primitives/raf 4.0.0-next.2 (devDependencies), jsdom ^25 (caret is fine, not a Solid package), optional @solidjs/testing-library 1.0.0-beta.3 and @solidjs/diagnostics 2.0.0-rc.6 exact. Exact pins because RC-to-RC diffs change runtime guards (the createRoot write asymmetry and on
PRINCIPLES: Producers return their computed values; consumers never read back state they just wrote (setupDates/changeViewMode return the window, batchMovePositions returns | Every write lives in an event handler, an onSettled body, or the APPLY half of createEffect(compute, apply); compute returns PLAIN values (never store proxies)  | Stores are draft-first with leaf mutation: taskStore mutates task._bar.x, never replaces state[id]; removal is delete draft[id]; existence guards live inside th | Built-in collections never live inside a createStore: expandedTasks joins collapsedTasks/collapsedGroups/selectedIds as a signal-held immutable Set; Dates insid | Derive instead of mirror: scroll/viewport are memos over the container's own signals; grouping and pool sizes are memos (prev-driven), not closure caches; isScr | Do the semantic work while still on solid-js 1.9 so the existing synchronous test suite proves it; the cutover commit is then almost purely mechanical (imports, | Delete dead code instead of migrating it; strip // @ts-nocheck from every demo that is touched so tsc actually sees the 2.0 types. | One issue = one commit = one push. The only exception is the cutover, which is one issue precisely because it cannot be split without breaking the build. | Public API changes are confined to what the runtime forces (context defaults, void-returning config setters, useGanttStores(): GanttStores | null) and are each 

## E1 [P1] Prune: delete dead code and unused dependencies while still on solid-js 1.9
   goal: Shrink the migration surface by deleting everything the audit proved has no consumer, so nothing dead is ported to 2.0.
   gate: Per issue: pnpm typecheck && pnpm lint && pnpm prettier-check && pnpm test && pnpm build && pnpm build:demo; pnpm dev smoke of examples/gantt.html and examples/perf.html (scroll + drag). grep proves each deleted symbol has no remaining reference.
   - E1.1 [chore/P1/S] Delete isScrolling machinery and the dead ContainerAPI debug surface  deps=[]  files=2
       useGanttScroll.ts:45-48 and :75-90 (isScrolling signal, scrollTimeout, the effect that mutates closure vars, writes a signal and starts a timer inside a compute) have no reader anywhere in src/ or tests/ (digest lib-scroll-container). Delete them, along with SCROLL_QUIET_MS (:28) and the comment at :10-12. In GanttContainer.tsx 
   - E1.2 [chore/P2/S] Delete ArrowLayer.tsx (no consumer) and GanttSubtaskDemo.tsx (no entry)  deps=[]  files=3
       grep shows no `from '.*ArrowLayer'` import anywhere in src/ and no `<ArrowLayer` JSX; only ArrowLayerBatched is mounted (GanttPerfIsolate.tsx:2015). ArrowLayer.tsx carries the audit's top arrow hazard (untrack-hidden dependencies, permanently blank layer) and a dual-source switch; deleting it is cheaper than redesigning it. Gant
   - E1.3 [chore/P1/S] Remove unused @solid-primitives packages and babel-preset-solid; move raf to devDependencies  deps=[]  files=3
       Repo-wide grep shows @solid-primitives/memo, resize-observer, scheduled and scroll (package.json:96, 98-100) are never imported; babel-preset-solid (:70) has no babel config using it. @solid-primitives/raf (:97) is imported only by src/demo/GanttPerfDemo.tsx:3 and src/entries/index-test.tsx:15, neither reachable from src/index.t
   - E1.4 [decision/P2/S] Delete the taskStore subtask-collapse API (decision-gated)  deps=[]  files=4
       taskStore.collapsedTasks/toggleTaskCollapse/isTaskCollapsed/expandTask/collapseTask/expandAllTasks/collapseAllTasks (taskStore.ts:36-42, :175-244) have no library caller; the documented collapse API is ganttConfig.collapseTask (docs/SUBTASKS.md:155-161), a different feature. Only tests/taskStore.test.ts:269-329 exercise it, and 

## E2 [P0] Reshape data flow into 2.0 idioms while still on solid-js 1.9
   goal: Convert every write-then-read-back pipeline into compute-then-apply, thread drag geometry as data, move stores to leaf-mutation semantics, and derive scroll/virtualization state, all verified by the existing synchronous test suite before the runtime changes.
   gate: Per issue: pnpm typecheck && pnpm lint && pnpm prettier-check && pnpm test && pnpm build && pnpm build:demo; dev smoke of examples/gantt.html (first paint shows bars, drag/resize/progress report final geometry), examples/showcase.html, examples/subtask.html (expand/collapse), examples/perf.html (scr
   - E2.1 [task/P0/M] ganttDateStore: pure window computation; setupDates/changeViewMode/extendTimeline return values and never read their own staged signals  deps=[]  files=2
       Chain A and J1: setupDates (ganttDateStore.ts:140-196) writes setGanttStart/setGanttEnd at :152-154 and :193-194 then generateDates() (:201-216) reads ganttStart()/ganttEnd()/step()/unit() back; changeViewMode (:243-254) stages the mode then generateDates reads step()/unit() memos derived from it; extendTimeline (:221-238) does 
   - E2.2 [task/P0/M] ganttSetup: compute-then-apply pipeline with local resource index map and a single config write  deps=['E2.1']  files=4
       Chain A: ganttSetup.ts:66 writes the date store then :69-73 and :75-84 read five accessors back; :101 writes resources then :102 reads the resourceIndexMap memo back, which on first run is an empty non-null Map so taskProcessor.ts:231-262 marks every task _resourceIndex -1 / _isHidden true and the first paint is blank. Rewrite i
   - E2.3 [task/P0/M] taskStore: leaf mutation, delete-on-remove, guards inside the draft, patch-merge updates, batchMovePositions returns applied positions  deps=[]  files=2
       Rewrite every mutator so the cutover only deletes the word produce. updateBarPosition (:104-121): remove the committed-state guard at :108; inside the produce draft do `const t = state[id]; if (!t?._bar) return; for (const k in position) if (position[k] !== undefined) t._bar[k] = position[k];` (leaf mutation, no object replaceme
   - E2.4 [task/P0/M] ganttConfigStore: expandedTasks becomes a signal-held immutable Set; setters become void-returning draft writes  deps=[]  files=3
       expandedTasks is a Set created inside createStore (ganttConfigStore.ts:54, :146) and mutated in place through produce at :172-203; 2.0 does not proxy built-ins, so expand/collapse would notify nothing (Gantt.tsx:371 rowLayouts and useTaskVirtualization.ts:161 go dead). Move it out of the store into `createSignal<ReadonlySet<stri
   - E2.5 [task/P0/L] Drag contract: geometry travels as data from onDragMove to onDragEnd, resize cascades take explicit geometry, useDrag/useBoxSelect gate on plain locals  deps=['E2.3']  files=7
       Chains B, C, D, H, J3. useDrag.handleMouseUp (useDrag.ts:154-187) runs the final onDragMove (:160-163, writes) then onDragEnd (:177, reads back) in one stack. Fix at the boundary, not with flush(): (1) useDrag keeps `let current: DragState = 'idle'` as the synchronous truth written next to every setDragState (:183, :218), every 
   - E2.6 [task/P0/S] useGanttScroll derives from the container: memos over containerApi(), no mirror effects, dimensions seeded at mount  deps=['E1.1']  files=3
       Showstopper (b) and chain I: handleContainerReady (useGanttScroll.ts:50-98) creates effects from a callback that will fire inside onSettled (children-forbidden) and reads api.getContainerWidth() (:52) one flush stale. Replace the hook with `useGanttScroll(containerApi: Accessor<ContainerAPILike | null>)` whose body is four memos
   - E2.7 [task/P1/M] Virtualization and ColumnPanel derive: tracked grouping memo (shared helper), prev-driven pool sizes, row-level memo in ColumnPanel  deps=['E2.4']  files=4
       Chain G: the grouping cache keyed on task COUNT (useTaskVirtualization.ts:70-96, duplicated at TaskLayerMinimal.tsx:45-73) goes permanently stale once 2.0 coalesces a same-tick add+remove; the pool high-water counters (:201-232) are mutable state inside memo computes. Rewrite: extract `groupTasksByResource(tasks: TaskMap): Map<s
   - E2.8 [task/P0/S] Gantt.tsx consumes the pipeline: runSetup(input), duplicate onMount deleted, single-write y-sync, plain prevViewMode  deps=['E2.1', 'E2.2', 'E2.3', 'E2.6']  files=1
       Chain A/F/J1 orchestration on 1.9 (syntax split happens in E3.1). Delete the duplicate initializer `onMount(() => runSetup(effectiveTasks()))` at Gantt.tsx:288 (the effect at :290 already runs on mount; under 2.0 the pair reads uncommitted state). runSetup (:266-275) takes the plain input object from E2.2: the effect at :290-304

## E3 [P0] Cutover: solid-js 2.0.0-rc.6 runtime, tooling, and all mechanical syntax in one commit
   goal: Swap the runtime and every removed/renamed API in a single buildable commit, with the test suite running in the correct client/server Vitest posture.
   gate: pnpm install && pnpm typecheck && pnpm lint && pnpm prettier-check && pnpm test (client and server projects both green) && pnpm build; pnpm dev smoke of every example whose demo is NOT // @ts-nocheck: gantt, subtask, resource-groups, box-select, multi-select, export, filter-search, critical-path, cu
   - E3.0 [chore/P0/S] Cutover rehearsal in a throwaway worktree (no commit)  deps=['E2.8']  files=4
       In a git worktree on a scratch branch, install the target set (solid-js 2.0.0-rc.6, @solidjs/web 2.0.0-rc.6, @solidjs/vite-plugin 3.0.0-next.38, @solid-primitives/raf 4.0.0-next.2, jsdom), flip jsxImportSource, and run pnpm typecheck to capture the full tsc error inventory into the E3.1 issue notes. Verify concretely: the plugin
   - E3.1 [task/P0/XL] THE cutover commit: dependencies, vite/vitest/tsconfig/CI config, library + entries + tests mechanical migration  deps=['E3.0', 'E1.1', 'E1.2', 'E1.3', 'E1.4', 'E2.1', 'E2.2', 'E2.3', 'E2.4', 'E2.5', 'E2.6', 'E2.7', 'E2.8']  files=17
       ONE commit; must land together. Tooling: package.json deps solid-js + @solidjs/web 2.0.0-rc.6 exact, devDeps @solidjs/vite-plugin 3.0.0-next.38, @solid-primitives/raf 4.0.0-next.2, jsdom; remove vite-plugin-solid; config/vite/solid.js:3 and demo.js:3 import the new plugin; solid.js:18 external ['solid-js','@solidjs/web'], drop f

## E4 [P1] Library end-state on 2.0: positional pools, measured memo laziness, diagnostics-clean mount, arrow layer without manual dependency protocols
   goal: Adopt the 2.0-only primitives and policies that could not be expressed on 1.9, with measurements recorded so nothing is a guess.
   gate: pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm build:demo; dev pages examples/perf.html (10K scroll, drag mid-scroll), examples/perf-isolate.html?bar=dragconst and the arrows variant, examples/gantt.html with zero console diagnostics; numbers recorded in the issue.
   - E4.1 [feature/P1/M] Replace the three sparse-array pools with <Repeat count={poolSize()}> and verify drag survives pool reuse  deps=['E3.1']  files=5
       The pools feed <For keyed={false}> sparse arrays (`new Array(poolSize)` with holes); hole semantics under the new reconciler are unproven and the audit flagged it as load-bearing (a remount tears down Bar's document listeners mid-drag). Repeat is the store-backed positional primitive: `<Repeat count={regularPoolSize()}>{(i) => <
   - E4.2 [task/P2/S] Measured lazy-memo policy for the ~25 memos that used to be lazy  deps=['E4.1']  files=6
       2.0 memos compute eagerly and never autodispose unless { lazy: true }, which also opts into teardown-and-recompute when unobserved. Do not sprinkle lazy: measure examples/perf.html 10K mount with chrome-devtools-cli perf.mjs before and after applying { lazy: true } to the genuinely rarely-read memos only: useBarConfig.ts showExp
   - E4.3 [bug/P1/M] Diagnostics-clean library: zero STRICT_READ / STRICT_READ_UNTRACKED / write-guard output on every library page  deps=['E3.1']  files=6
       Run every non-demo-only page in dev and drive the diagnostics to zero: top-level prop reads in component bodies (Gantt.tsx has 7; Bar.tsx:195-205 handler object; TaskLayer.tsx:197-198 useBoxSelect deps) become untrack for one-shot reads or getters for reactive ones; no store proxy is read inside any effect apply (compute returns
   - E4.4 [task/P2/M] ArrowLayerBatched: instance-level caches, no positionVersion counter, no untrack over position reads  deps=['E5.1']  files=2
       ArrowLayerBatched.tsx:70-72 module-level caches are shared across instances and HMR reloads; :123 `void props.positionVersion` plus untrack at :139-140 is a hand-rolled dependency protocol from 1.x per-write propagation storms, which the 2.0 microtask flush already coalesces. Move the caches into the component body (or return th

## E5 [P1] Demos on 2.0: restore every examples/*.html page with 2.0 idioms and without @ts-nocheck
   goal: Every example page loads, runs its interactions, and shows zero Solid diagnostics; build:demo is green again.
   gate: pnpm typecheck && pnpm lint && pnpm build:demo; each page in the issue opened via pnpm dev (pnpm dev:all for db) with the listed interactions performed and zero console diagnostics.
   - E5.1 [task/P0/L] Perf harness demos: shared onSettled viewport/RAF helper, closure latches, mechanical migration; build:demo green  deps=['E3.1']  files=10
       Create src/demo/perfHarness.ts with `useViewportSize(getEl)` (onSettled: measure, ResizeObserver, window resize, single returned cleanup) and `useRafLoop(tick)` / `createLatch()` (a plain closure boolean plus a signal mirror) and replace the five copy-pasted onMount bodies (GanttMinimalTest.tsx:417/439, GanttExperiments.tsx:964/
   - E5.2 [task/P1/L] ShowcaseDemo: store born populated, config-to-task mirrors as split effects, presets as single draft writes, geometry from callbacks  deps=['E3.1', 'E2.3', 'E2.5']  files=1
       ShowcaseDemo.tsx writes a config store then synchronously re-reads it to mirror into taskStore (updateTaskA/B at :665-693 called from seven handlers :1084-1217 and :1775-1776), and applyPreset (:807-875) stages four updateBarPosition writes then overwrites them with whole-node replacements from stale spreads. Rewrite: seed with 
   - E5.3 [task/P1/M] ConstraintDemo pure planner + split-effect seeding; BarDemo and ArrowDemo mechanical migration  deps=['E3.1', 'E2.5']  files=3
       ConstraintDemo.tsx: resolveMovement (:90-275) interleaves store reads with updateBarPosition writes across recursion (:178/:203/:239/:264, read-back at :251). Rewrite as a pure planner over a local Map<id, {x, y}> seeded lazily from taskStore.getBarPosition, returning the update set; handleMouseMove applies it in one setTasks dr
   - E5.4 [task/P1/M] DbDemo: settle-then-diff persistence, recorded deltas, derived TaskForm  deps=['E3.1', 'E2.5']  files=4
       Chain H: onDateChange (DbDemo.tsx:146) runs inside the mouseup stack and reads taskStore.tasks[id]._bar (:195-196) one frame stale, persists short deltas, then re-bases from the committed value after the await (:239-242) so the error compounds. Idiomatic 2.0 fix with no flush() and no library API change: the handler does `onSett
   - E5.5 [task/P2/S] Feature and main demos: small 2.0 hygiene and full smoke  deps=['E3.1']  files=9
       These demos import only createSignal and inherit their breakage through Gantt's callback contract, which E3.1 fixed (selection callback runs in apply; onReady runs unowned). Remaining edits: CriticalPathDemo.tsx:112 read-modify-write toggle becomes `setCriticalPath(v => !v)` and :135 inline accessor in the options literal become

## E6 [P1] Verification posture: 2.0 regression tests, CI gates, perf re-baseline
   goal: Lock in the migration with tests that fail on the exact failure modes the audit found, make CI run the full gate, and replace the invalidated 1.9 performance corpus with measured 2.0 numbers.
   gate: pnpm test (both projects), pnpm typecheck, pnpm lint on widened globs, CI workflow green on the branch, benchmark scripts complete against a freshly built dist-demo.
   - E6.1 [task/P1/M] Regression tests for the deferred-write failure modes (client project)  deps=['E3.1', 'E4.3', 'E5.1']  files=4
       Write the tests the audit said should exist before the migration: (1) tests/useDrag.test.ts in jsdom: mount a component using useBarDrag with a real taskStore, dispatch mousedown, mousemove, mouseup with `flush()` (or await Promise.resolve()) between dispatches; assert onDateChange receives the final x/width the last move wrote 
   - E6.2 [chore/P2/S] CI and lint coverage for tooling files  deps=['E3.1']  files=4
       tsconfig.json:22 include and the lint/prettier globs (package.json:30/33) omit config/vite/*.js, vitest.config.ts and eslint.config.mjs, and publish.yml runs only prettier-check + build. Widen the lint and prettier globs to include config/**/*.js, vitest.config.ts, eslint.config.mjs; add a ci.yml on push/pull_request running pnp
   - E6.3 [task/P2/M] Perf re-baseline on 2.0 against refreshed dist-demo  deps=['E7.1', 'E4.1', 'E4.2']  files=7
       All recorded tables were measured on solid-js 1.9.12 with <Index>-based pools and synchronous writes; deferred writes move DOM patches into a trailing microtask and eager memos change mount cost, so the numbers are not comparable. After E7.1 refreshes dist-demo, run `npx serve dist-demo -l 5174` and the three bench scripts, plus

## E7 [P2] Docs, memories, built artifacts, and follow-ups
   goal: Make every written contract match the 2.0 code, refresh the committed demo build once, and file the optional modernizations and the re-bump as explicit issues.
   gate: pnpm prettier-check; every code sample in the touched docs compiles when pasted into a scratch .tsx under the repo tsconfig (spot-check three); bd prime shows the rewritten memories; dist-demo commit contains only build output.
   - E7.1 [chore/P1/S] chore(demo): refresh built artifacts (dist-demo) on solid-js 2.0  deps=['E5.1', 'E5.2', 'E5.3', 'E5.4', 'E5.5', 'E4.1', 'E4.4']  files=1
       After E5 is complete and build:demo is green, run pnpm build:demo and commit the nine tracked files (3 examples/*.html + hashed chunks) as the single 'chore(demo): refresh built artifacts' commit, nothing else staged. This is the artifact the benchmark scripts serve (npx serve dist-demo -l 5174).
   - E7.2 [task/P2/L] Docs sweep: README, CLAUDE.md, AGENTS.md, ARCHITECTURE, PERFORMANCE, MINIMAL_TEST, DATABASE, DEMOS, EXPERIMENTS, ANALYSIS, RFC-undo-redo  deps=['E6.3', 'E5.4']  files=11
       README: add an install section (pnpm add ganttss solid-js @solidjs/web, jsxImportSource note), rewrite :106 (useGanttStores returns null outside a provider) and :108 (config setters return void; reads are committed until flush), document that every store setter stages and that imperative read-after-write needs flush() at the cal
   - E7.3 [chore/P2/S] Rewrite the bd memories for the 2.0 model  deps=['E7.2']  files=1
       Update in place with `bd remember --key solidjs-createstore-produce-state-id-newobj-never-wrap` to say: draft-first leaf mutation (task._bar.x = v) is the rule and object replacement is forbidden (it invalidates every leaf); a memo returning a store sub-proxy still never invalidates in 2.0, so bindings read their own leaf (Bar.t
   - E7.4 [task/P3/S] Follow-up: re-bump to the newest Solid 2.0 RC/stable and relax pins  deps=['E7.2']  files=2
       When a newer RC or the stable 2.0.0 ships: bump solid-js, @solidjs/web, @solidjs/vite-plugin, @solid-primitives/raf (and diagnostics/testing-library if adopted) together, re-run the full end-of-migration gate (typecheck, lint, both test projects, build, build:demo, smoke matrix, diagnostics check), read the RC changelog for guar
   - E7.5 [decision/P3/S] Optional: peerDependencies for solid-js/@solidjs/web and an npm `next` dist-tag publish policy  deps=['E3.1']  files=3
       The lib build externalizes both runtimes, so consumers must share one instance with the host app: move solid-js and @solidjs/web to peerDependencies (^2.0.0-rc.6, then ^2.0.0 at stable) mirrored in devDependencies, and document resolve.dedupe in the README. Decide whether RC builds are published at all; if yes, publish.yml uses 
   - E7.6 [feature/P4/M] Backlog follow-ups: rowLayouts as the y source of truth; DbDemo on actions/createOptimisticStore; Arrow.tsx memo re-benchmark  deps=['E7.2']  files=5
       Three modernizations deliberately excluded from the migration: (1) delete the y-sync effect entirely by having useBoxSelect.findBoxHits and svgExport consult the rowLayouts map for y instead of _bar.y (derive, not write-back); (2) rewrite DbDemo's CRUD and drag persistence with action()/createOptimisticStore/refresh so the demo 

TOTAL child issues: 32
CRITICAL PATH: E1.3 -> E2.1 -> E2.2 -> E2.3 -> E2.5 -> E2.8 -> E3.0 -> E3.1 -> E5.1 -> E4.1 -> E4.3 -> E5.2 -> E5.4 -> E6.1 -> E7.1 -> E6.3 -> E7.2
SEQUENCING: E1 first because deletions remove sites from every later issue and are trivially green. E2 does all semantic work on 1.9 where the existing synchronous suite verifies it: E2.1 (date window) feeds E2.2 (setup pipeline) feeds E2.8 (Gantt consumption); E2.3 (taskStore leaf semantics) feeds E2.5 (drag contract) because onDragEnd reports what the store mutators applied; E2.4, E2.6 and E2.7 are independent and parallelizable. E3.0 rehearses so the single XL cutover commit (E3.1) has a known error inventory; E3.1 must be one commit because the runtime swap and the removed-API rewrites cannot be separated without breaking typecheck. Immediately after, E5.1 restores build:demo (the only gate left red by the cutover), then E4.1/E4.3 finish the library on 2.0-only primitives before the remaining demos (E5.2-E5.5) are validated against the final library shape. E6.1 tests land once the library is dia
VERIFICATION: Per-issue gate (E1/E2 on 1.9 and every issue after E5.1): pnpm typecheck && pnpm lint && pnpm prettier-check && pnpm test && pnpm build && pnpm build:demo, plus the pages named in the issue via pnpm dev. Cutover gate (E3.1): same minus build:demo, both Vitest projects (`pnpm test` runs client=jsdom for tests/*.test.ts and server=node for tests/server/**), dist has no @solidjs/web code inlined, and the nine non-@ts-nocheck example pages render with zero diagnostics. End-of-migration gate (after E7.2): (1) pnpm typecheck, pnpm lint (widened globs), pnpm prettier-check; (2) pnpm test with both projects green, including tests/gantt.mount.test.tsx toHaveNoDiagnostics, tests/useDrag.test.ts, tests/arrowLayerBatched.test.tsx, the strengthened taskStore/ganttSetup/ganttConfigStore/ganttDateStore suites; (3) pnpm build and pnpm build:demo; (4) demo smoke matrix in dev with the console open (zero 
DECISIONS:
   ? Exact pins or caret for the RC line? => Exact pins (2.0.0-rc.6, 3.0.0-next.38, 4.0.0-next.2); caret only at stable via E7.4. (RC-to-RC changes altered dev guards (createRoot write asymmetry, onSettled return validation, vite-plugin test posture); a caret bump would silently change test)
   ? Feature branch or main? => E1/E2 on main (1.9-compatible, independently valuable); E3 onward on branch solid-2.0, fast-forward merged after E5 + E6.1. (The cutover leaves build:demo red for a few commits and the RC should not reach the release branch; the prep work is safe on main and keeps main useful if the R)
   ? Drop UMD or ship a self-contained bundle? => Drop umd: formats ['es'], main/module/exports on the es build, remove the require condition. (Neither solid-js nor @solidjs/web publishes a global build, so the UMD contract was already decorative; a self-contained bundle would duplicate the reactive run)
   ? peerDependencies for solid-js and @solidjs/web? => Yes, as optional issue E7.5 (peer + dev), not inside the cutover. (Two externalized runtimes must be the host app's instance; this is a packaging change the user should approve separately.)
   ? Delete or keep: taskStore collapse API, isScrolling, ContainerAPI debug surface, ArrowLayer.tsx, GanttSubtaskDemo.tsx? => Delete all five (E1.1, E1.2, E1.4); the taskStore collapse API is decision-gated because it is reachable via the exported TaskStore type. (None has a library caller or documentation (docs/SUBTASKS.md documents ganttConfig.collapseTask, a different API); porting them is where the hardest sync hazard)
   ? DbDemo modernization scope? => Migration scope = settle-then-diff via onSettled and recorded deltas (E5.4); actions/createOptimisticStore is backlog E7.6. (onSettled from the mouseup handler reads committed geometry including cascades with no flush() and no library API change; the async-model rewrite is a demo feat)
   ? Lazy memo policy? => Measured, minimal: lazy only for rarely-read memos (three useBarConfig memos, modal/popup formatters, getAllDateInfos); everything else eager (E4.2). (lazy also opts into autodispose-and-recompute, which for virtualized bars trades mount cost for churn; the per-bar hot memos are always read.)
   ? expandedTasks representation: signal-held Set or Record<string, true> in the store? => Signal-held immutable Set (E2.4). (Matches the three existing collections, keeps the public accessor/option types and rowLayoutCalculator untouched; the per-key tracking a Record would give is un)
   ? Pool primitive: <For keyed={false}> or <Repeat>? => For keyed={false} in the cutover (identical callback shape, zero risk), Repeat in E4.1 if benchmarks are at parity. (Repeat is the native positional pool and avoids sparse-array semantics, but its reconciler behaviour must be measured before it carries the drag-listener invari)
   ? useGanttStores return type: undefined (via ?? undefined) or null? => null; update README:106. (createContext<T | null>(null) is the documented optional-provider form; a `?? undefined` shim only preserves a type nobody depends on (Gantt.tsx:170 uses ??).)
   ? Adopt @solidjs/testing-library and @solidjs/diagnostics? => Yes, both (dev-only, exact pins); E4.3/E6.1 use toHaveNoDiagnostics as the chain A guard. (A diagnostics-clean mount test is the only automated proof for the silent-failure classes; cost is two devDependencies.)
   ? Publish RC builds to npm? => Not to latest; either hold or publish with --tag next (E7.5). (Consumers on 1.x would receive a breaking runtime from a caret update.)
   ? gitignore dist-demo instead of refreshing it? => Keep tracked and refresh once (E7.1) for this migration; revisit with E7.5. (The bench scripts serve it; changing the artifact policy is orthogonal scope.)
RISKS:
   ! The cutover commit is XL and touches ~60 files; a mistake is hard to bisect. => E2 removes every semantic decision beforehand; E3.0 rehearsal produces the exact tsc inventory; the commit is gated by both test projects and the diagnostics-free smoke of nine pages; revert is a sing
   ! build:demo and the @ts-nocheck demo pages are red between E3.1 and E5.1. => E5.1 is sequenced immediately after the cutover and the red window is stated in E3.1's notes; the branch (not main) carries it.
   ! Repeat's row-preservation semantics or For keyed={false} hole handling break the drag-listener invariant. => E4.1 benchmarks and tests the mid-scroll drag explicitly; fallback is the For keyed={false} build from E3.1 plus a hole test.
   ! Tracked grouping memo over 10K tasks (O(N) subscriptions) regresses mount time. => E2.7 measures on perf.html 10K with a 10% budget and names the fallback (structureVersion signal with untracked leaf reads).
   ! @solidjs/compiler native binary needs a pnpm build-script approval or a wasm fallback on this platform; Node floor 20.19/22.12. => E3.0 verifies install on the developer machine; E3.1 sets CI to Node 22 and the allow-list in pnpm-workspace.yaml.
   ! onSettled from an unowned mouseup handler (DbDemo) does not fire after the gesture's flush as assumed. => E3.0 verifies it against the installed runtime; fallback in demo code is `await Promise.resolve()` at the top of the handler (legal there), never in library code.
   ! RC churn changes dev guards before the migration lands. => Exact pins; E7.4 is the only place versions move, with a full gate and changelog read.
   ! Dev-mode STRICT_READ warning volume at 10K rows makes perf numbers diagnostics-bound. => E4.3 lands before E6.3; benchmarks run against the production build in dist-demo.
   ! Public type changes (void config setters, useGanttStores null, ConfigSetter) surprise a downstream consumer. => Each is named in openDecisions and README/ARCHITECTURE updates in E7.2; no in-repo caller depends on the old shapes.
   ! Demos hidden behind // @ts-nocheck keep 2.0 breakage invisible to tsc. => Every demo issue strips the pragma and its acceptance requires the page to run with zero diagnostics.
FOLLOWUPS: E7.4: re-bump to the newest RC/stable, relax exact pins to caret at stable, re-run the full gate. | E7.5 (optional): peerDependencies for solid-js/@solidjs/web and an npm `next` dist-tag policy. | E7.6 (backlog): rowLayouts as the y source of truth (delete the y-sync effect; useBoxSelect and svgExport consult the layout map). | E7.6 (backlog): DbDemo on action()/createOptimisticStore/refresh. | E7.6 (backlog): Arrow.tsx path memo re-benchmark under the 2.0 scheduler. | Consider gitignoring dist-demo and requiring a local build for the bench scripts. | Document TaskStore method timing (which readers are committed-only) in the public JSDoc if external consumers appear. | If E2.7's tracked grouping memo is replaced by a structureVersion signal, record the measured reason in PERFORMANCE.md.

### REVIEW (score 8): Strong plan and the best available skeleton for the migration: it correctly identifies that the hard work is data-flow shape (not syntax), does that work on 1.9 where the existing suite still proves it, deletes dead code first, and it has clearly absorbed the critics' runtime corrections (onSettled untracked, queueMicrotask for onReady, block bodies for return-value validation, signal-vs-store createRoot asymmetry, no flush in apply/onSettled). The defects are concentrated in the cutover issue and the tail. E3.1 cannot pass its own gates as written: its test list omits the two suites E2.1/E2.4 create, its vitest client include glob (tests/*.test.ts) will silently skip the .tsx suites E4.3/E6.1 add, its acceptance grep covers src/entries while index-test.tsx is assigned to E5.1 (verified: 4 Index sites, 2 Provider tags, a solid-js/store import), and it claims 10 client suites where the repo has 14. Two issues use an invalid bd type (decision). Coverage gaps are real but bounded: SummaryBar.tsx:63's memo-over-proxy silently stops invalidating the moment E2.3 lands leaf mutation; ganttConfigStore's unproxied Dates (verified at GanttConfigState:35/36/48) are handed raw to consumer upda
  BLOCKING (5):
    * E3.1's test file list is incomplete and the cutover cannot be green as specified. It migrates only tests/taskStore.test.ts, tests/selectionStore.test.ts, tests/ganttSetup.test.ts — but E2.1 creates tests/ganttDateStore.test.ts and E2.4 creates tests/ganttConfigStore.test.ts, both of which assert on post-write state and therefore need flush() (and E2.4's 'memo over expandedTasks() re-runs on toggle
    * E3.1's prescribed vitest client project `include: ['tests/*.test.ts']` will silently never run the .tsx suites the plan itself creates: tests/gantt.mount.test.tsx (E4.3, E6.1) and tests/arrowLayerBatched.test.tsx (E6.1). The glob must be tests/*.test.{ts,tsx}. A test that never runs is worse than no test — and E4.3/E6.1 both claim it as the chain-A guard.
    * E3.1's acceptance grep spans src/entries and demands zero hits for `<Index`, `.Provider`, 'solid-js/store' — but src/entries/index-test.tsx (verified: line 14 'solid-js/store', 2298/2307/2316/2325 `<Index`, 2343/2357 `.Provider`) is explicitly assigned to E5.1. E3.1 cannot pass its own acceptance, and index-test.tsx is claimed by two issues (E3.1 files 'src/entries/*.tsx' vs E5.1 files 'src/entrie
    * E1.4 and E7.5 use `--type=decision`, which is not a valid beads type (repo convention: epic|task|bug|feature|chore). `bd create` will reject both issues as written.
    * E6.2 (adds ci.yml running build:demo) dependsOn only E3.1, and its acceptance is 'ci.yml green on the solid-2.0 branch'. Between E3.1 and E5.1 the plan states build:demo is RED by design, so if E6.2 is scheduled per its declared dependency the gate is unachievable. dependsOn must include E5.1.
  MAJOR (7):
    * Missing hazard: src/components/SummaryBar.tsx:63 `const position = createMemo(() => getPosition())` returns the `_bar` store proxy (verified in repo). The digest (lib-bars STRUCT) states it only invalidates today because updateBarPosition/batchMovePositions replace state[id] wholesale; E2.3's leaf-mutation rewrite removes exactly that churn and the memo stops re-running. Rendering survives via lea
    * Missing hazard: ganttConfigStore holds unproxied Dates (verified GanttConfigState:35 ganttStart: Date, :36 ganttEnd: Date, :48 ignoredDates: Date[]) — the critic's MISSED item at ganttConfigStore.ts:127. E2.4's ConfigSetter rewrite hands `draft[key]` straight to a consumer updater, i.e. a raw unproxied Date. E2.4 only documents ProcessedTask Dates (types.ts:198) and the expandedTasks option (types
    * Missing hazard: src/stores/resourceStore.ts:129-132 collapseAll reads the getGroups() memo then writes setCollapsedGroups (verified). Tracer chain G FIX(2) asks for an explicit id-list parameter, same as taskStore.collapseAllTasks. No issue in the plan touches it; taskStore.collapseAllTasks is only fixed in the *keep* branch of E1.4, so the recommended *delete* branch leaves resourceStore's twin u
    * Missing hazard: src/hooks/useGanttModals.ts:47/52/57/62 memos return live store proxies into TaskPopup/TaskModal (lib-hooks STRUCT: 'Snapshotting here is the cheaper fix and is confined to four lines'). The plan only mentions `{ equals }` on the barPosition memos (E3.1 notes) and a vague 'checked against those components' apply phases' in E4.3 — no snapshot() and no named acceptance.
    * E3.1 acceptance states 'client project (10 suites)'. Verified: tests/ contains 14 client suites today, becoming 16 after E2.1/E2.4 add ganttDateStore/ganttConfigStore suites. The count is wrong, so the gate as written either passes vacuously or blocks on a false expectation.
    * Missing hazard: src/demo/GanttExperiments.tsx:596-599 (cachedVisibleTasks) and :652-698 (smartCache) keep incremental state in closure vars mutated from inside memo computes — the digest says they 'cannot be combined with { lazy: true } autodisposal' and must be redesigned 'before the numbers they produce mean anything'. E5.1 covers this file's latches, Index sites and the options-shaped memos at 
    * Acceptance criteria that reference baselines nobody produces. E2.7 requires 'perf.html 10K mount time within 10% of the E1 baseline' and E4.2 requires before/after mount numbers, but no E1 issue has a measurement deliverable and E6.3 (the re-baseline) lands last. Either add a 'capture 1.9 baselines' chore to E1 or restate these as 'record absolute numbers, no threshold'.
  FACTERRORS (1):
    * None of the prescribed rewrites contradicts the runtime facts outright — the plan correctly absorbed the critic's corrections (onSettled is untracked so no defensive untrack; queueMicrotask for onReady; block bodies for apply/onSettled return-value validation; store setters exempt in createRoot but signal setters not; no flush() in apply or onSettled). Two soft issues: (a) E5.4 replaces DbDemo.tsx
  ORDERINGERRORS (6):
    * E6.2 (ci.yml running the full gate incl. build:demo) dependsOn E3.1 only, but build:demo is red by design until E5.1 — dependency must point at E5.1.
    * E6.2's lint/prettier glob widening lands *after* the cutover. Verified: tsconfig.json include is ['src/**/*','tests/**/*.ts','benchmarks/constraint/**/*.ts'] and the lint/prettier globs are the same four — so config/vite/*.js and vitest.config.ts are invisible to every gate at exactly the commit (E3.1) that rewrites them. The glob widening is 1.9-compatible and belongs in E1.
    * E7.1 (dist-demo refresh) dependsOn E4.4, a priority-2 demo-only ArrowLayerBatched refinement. That makes the artifact refresh → E6.3 benchmarks → E7.2 docs → E7.3 memories chain all block on optional arrow work. Drop E4.4 from E7.1's dependsOn.
    * E7.3 (rewrite the bd memories: flush legality table, createRoot signal/store asymmetry, onSettled constraints, built-in collections) dependsOn E7.2, i.e. it lands last. Those memories are the guidance the engineer needs *while doing* E2/E3; write them before E3.1 and amend at the end.
    * E2.5's acceptance ('drag one column and release inside one rAF; onDateChange shows the landed column') cannot fail on solid-js 1.9, where the write is synchronous — the commit that makes the change cannot demonstrate it. State it as 'no behaviour change on 1.9; proof lands in E6.1's tests/useDrag.test.ts'.
    * E3.1's file list ('src/entries/*.tsx') and E5.1's ('src/entries/index-test.tsx') both own the same Index/Provider/store-import sites; whichever runs second finds nothing to do or re-edits the other's work.
  MISSINGSITES (13):
    * src/components/SummaryBar.tsx:63 — memo over the _bar store proxy stops invalidating once E2.3 lands leaf mutation (lib-bars STRUCT)
    * src/stores/ganttConfigStore.ts:127 (GanttConfigState:35/36/48) — unproxied Date/Date[] inside createStore, handed raw to updaters by the E2.4 ConfigSetter rewrite (critic MISSED)
    * src/stores/resourceStore.ts:129-132 — collapseAll reads the getGroups() memo then writes; needs an explicit id-list parameter (tracer chain G FIX 2)
    * src/stores/taskStore.ts:232-243 — collapseAllTasks read-then-write is only repaired in E1.4's 'keep' branch, not in the recommended 'delete' branch
    * src/hooks/useGanttModals.ts:47/52/57/62 — memos leak live store proxies into TaskPopup/TaskModal; digest prescribes snapshot() at the memo boundary
    * src/components/Gantt.tsx:193-220 — buildExportInput reads committed taskStore/relationships/container geometry; export right after a mutation or a drop serializes the previous frame (chain J2)
    * src/components/TaskLayer.tsx:129-143 — onTaskClick fires in the same stack as the selection write, so consumers observe the pre-click selection (chain E FIX 1)
    * src/demo/GanttExperiments.tsx:596-599 and :652-698 — incremental caches held in closure vars inside memo computes; incompatible with { lazy: true } and with re-baselining
    * src/utils/rowLayoutCalculator.ts:73 and src/components/TaskLayer.tsx:39 — RowLayout.taskPositions / Map<string,RowLayout> passed through props; the 'built-ins are replace-only' invariant is unwritten (lib-bars STRUCT)
    * src/types.ts:268 — ConstraintContext's getBarPosition/getTask committed-read contract needs documenting as snapshot-or-draft; E2.5 threads geometry but never states the contract
    * src/index.ts:26 — useGanttEvents' public contract changes when the context gains a real default; only useGanttStores got an openDecision (critic MISSED)
    * src/components/Bar.tsx:110-118 — the getTask() re-lookup exists solely to defeat <Index> snapshot pooling; the digest asks whether it is still needed under <For keyed={false}>/<Repeat>. E4.1 keeps it without re-validating (four store lookups per bar per frame)
    * package.json — @solid-primitives/utils 7.0.0-next.4, named as a mandatory peer bump alongside raf 4.0.0-next.2
  SCOPECREEP (5):
    * E1.3 consolidates the two pnpm build-script allow-lists (package.json pnpm.onlyBuiltDependencies vs pnpm-workspace.yaml) — unrelated to the migration and not marked optional. Keep it if @solidjs/compiler needs the allow-list, but say that is the reason.
    * Dropping the UMD format, retargeting package.json main/module/exports at the es build, and removing the require condition all happen *inside* the XL cutover commit (E3.1). It is covered by an openDecision, but the user's constraint asks for packaging changes to be separate/explicit — and the plan treats the sibling packaging change (peerDependencies) as its own optional issue E7.5. Verified: packa
    * E5.1 folds benchmarks/profiler/index.js's pre-existing ESM/require() defect and memoTracker.js into the demo-migration issue. Small and defensible, but it is a bug fix unrelated to Solid 2.0 riding in an already-L issue.
    * E1.2 deletes two source files (ArrowLayer.tsx, GanttSubtaskDemo.tsx). Verified they have no importer and are not re-exported from src/index.ts, so the API is safe — but package.json `files` publishes `src`, so this is still a published-content change worth one sentence in the issue.
    * E5.2's 'seed with createTaskStore(initialTasks)' adds a new optional parameter to a *public* store factory (exported from src/index.ts:11) to serve one demo. Justified by the digest, but it is a public API addition and is not in openDecisions.
  BESTIDEAS (13):
    * Do all the semantic reshaping on solid-js 1.9 first (E2), where the existing synchronous suite still verifies it, so the runtime swap is almost purely mechanical and any behavioural regression bisects to a small green commit. This is the single best structural decision in the plan.
    * Adopt the digest's one architectural rule instead of ~15 site fixes: producers return their computed values (setupDates/changeViewMode return a DateWindow, batchMovePositions returns the applied map), consumers never read back what they just wrote, and store existence guards move inside the draft. Directly implements the tracer's CROSS finding.
    * E1 first: delete isScrolling, the dead ContainerAPI debug surface, ArrowLayer.tsx and GanttSubtaskDemo.tsx before migrating. This removes two of the six scroll hazards, one primitive from a children-forbidden scope, and the top arrow hazard at zero cost.
    * E2.6's useGanttScroll(containerApi: Accessor<...>) redesign with four memos — the single move that deletes the children-forbidden showstopper, every mirror write, and the extra flush hop, collapsing the hook to ~40 lines.
    * taskStore leaf mutation (t._bar.x = v) with the *negative* regression assertion (a sibling memo on tasks['t1'].name whose run count must stay 1). Without that assertion nothing prevents someone porting the 1.x object-replacement shape into the draft, which compiles and passes the old test while keeping the cost.
    * Deferring consumer callbacks out of onSettled with queueMicrotask (onReady) so the children-forbidden contract never propagates into user code — exactly the critic's shared rule.
    * The E3.0 rehearsal in a throwaway worktree producing the exact tsc error inventory and answers to five concrete runtime questions before the XL cutover. Cheap, and the deliberate no-commit exception is well justified.
    * One shared src/demo/perfHarness.ts (useViewportSize/useRafLoop/createLatch) replacing five copy-pasted onMount+ResizeObserver+RAF bodies and seven read-then-write latches, per the digest's two CROSS findings.
    * Split-effect derivation (compute returns a plain object, apply has a block body) as the fix for the one architectural defect shared by ShowcaseDemo/ConstraintDemo/DbDemo, rather than three independent flush() patches.
    * Stripping // @ts-nocheck from every demo that is touched, with 'page runs with zero diagnostics' as the acceptance — the only way the type-level half of the migration becomes visible in those files.
    * Exact version pins for the whole RC line with a single re-bump issue (E7.4) as the only place versions move, justified by RC-to-RC guard changes.
    * The openDecisions block itself: 13 named decisions with recommendation + rationale, each tied to an issue, is the right shape for keeping the user out of scope-creep surprises.
    * dist-demo refreshed exactly once as its own 'chore(demo): refresh built artifacts' commit, with the benchmark re-baseline sequenced against it and old tables date-stamped rather than deleted.
  MINOR (10): E3.1's dist purity check — 'grep for createRenderEffect absent from dist' — is a proxy symbol, not a check. Verify the e | E1.1 acceptance 'examples/perf.html scrolls at the same fps as before' has no recorded baseline and no threshold; not ch | E4.3 adds @solidjs/diagnostics and @solidjs/testing-library devDependencies inside a `bug`-type issue, contradicting the | onContainerReady is left called synchronously from the onSettled body (E3.1) while onReady is deferred with queueMicrota | @solid-primitives/utils 7.0.0-next.4 is named in the digest (lib-stores-task STRUCT, entries batch) as a mandatory peer  | E7.6 is a single issue whose entire deliverable is 'filed as three separate bd issues'. Just create the three backlog is | The plan has no explicit 'verified safe — do not migrate' inventory, and the digest is emphatic about several: functiona | E2.4 retypes 20 setters to void-returning ConfigSetter but makes setExpandedTasks 'the real signal setter' — an inconsis | E4.1's `<Repeat count>` JSX assumes the callback receives a plain index `(i)`. E3.0 verifies Repeat *exists* but not its | E3.1's reconcile rewrite offers 'or a returned-value replacement per the create-store docs' as an alternative without de

====================================================================================================
PLAN verification-first: Verification-first migration: pin behavior on 1.9, flip atomically on a branch, turn every red test green one issue at a time  | review score=8 | est effort=Roughly 45 issues: E0 ~1 day, E1 ~3 days, E2 (flip) ~3 days, E3 ~8 days, E4 ~5 days, E5 ~3 days, E6 ~2 days; about 5 engineer-weeks with the critical path (E0 -> E1 -> flip -> E3.1..E3.8 -> E3.12 -> E4.8 -> E5.4) around 3.5 weeks if demo and docs work runs in parallel after E3.12.
BRANCH: E0 (gates + Vitest projects) and E1 (characterization tests) land on main first: they are green on solid-js 1.9.12 and give main the regression net before any runtime changes. Create branch `solid-2` from that point. E2 (flip) through E5 land on `solid-2` as one commit per issue; the flip commit is allowed to have the enumerated failing characterization tests (the issue lists them by name) because main is untouched. Merge `solid-2` into main with a merge commit (not squash, to keep one-issue-one-commit history) only when the end-of-migration verification plan is fully green; the dist-demo refr
VERSIONS: Exact pins (no caret) for the whole RC line while on rc.6: solid-js 2.0.0-rc.6, @solidjs/web 2.0.0-rc.6, @solidjs/vite-plugin 3.0.0-next.38 (matches @solidjs/compiler rc.6; next.35 lacks the mode!=='test' guards), @solid-primitives/raf 4.0.0-next.2 (devDependencies), @solidjs/diagnostics 2.0.0-rc.6, @solidjs/testing-library 1.0.0-beta.3 (only if adopted), jsdom ^25. Rationale: RCs break each other across patch numbers and the audit's runtime facts were verified against rc.6 specifically. Re-bump
PRINCIPLES: Every behavioral change ships with a test that is red on the parent commit and green on the fix commit; the flip commit's test report IS the regression inventor | Characterization tests are written on solid-js 1.9 (main) using a tests/helpers/settle() that is a no-op on 1.9 and becomes flush() at the flip, so timing edits | Producers return their computed values; consumers never read back state they just wrote; store guards move inside the draft. flush() only in event handlers/time | Test posture is fixed before any reactive test is touched: Vitest projects (client=jsdom for tests/*.ts[x], server=node for tests/server/*), no signal writes in | One issue -> one commit -> one push, except the flip (E2.1-E2.4) which is one commit because the build cannot pass between them. Feature branch solid-2; main st | Migrate, do not port: the gantt-6hx object-replacement hack becomes leaf mutation guarded by a negative assertion; untrack-hidden dependencies and count-keyed c | Every callback handed to onSettled/createEffect apply or invoked from them gets a block body (rc.6 throws on non-function return values). Consumer callbacks (on | Scope creep is opt-in: UMD, peerDependencies, dead-code deletion, DbDemo modernization and lazy-memo policy are explicit decisions or optional issues, never fol

## E0 [P0] Gates and test posture (pre-flip, on solid-js 1.9, main)
   goal: Make every file the migration touches visible to typecheck/lint/CI and give the reactive suites a real DOM posture, before any Solid code changes.
   gate: pnpm typecheck && pnpm lint && pnpm prettier-check && pnpm test (both projects listed in output: client, server) && pnpm build && pnpm build:demo, all green on solid-js 1.9.12 on main.
   - E0.1 [chore/P0/S] CI: Node 22 + pnpm 10 in publish.yml; add typecheck, lint, test steps  deps=[]  files=1
       publish.yml pins node-version '18' and pnpm/action-setup version 9 while package.json declares packageManager pnpm@10.23.0 and @solidjs/vite-plugin declares engines.node '^20.19.0 || >=22.12.0' (tooling-docs STRUCT: NODE VERSION FLOOR MOVES). Set node-version '22', drop the explicit pnpm version (let action-setup read packageMan
   - E0.2 [chore/P0/S] Widen static gates: config/vite/*.js -> .ts, tsconfig include, lint/prettier globs  deps=[]  files=6
       tsconfig.json:22 include is [src/**/*, tests/**/*.ts, benchmarks/constraint/**/*.ts]; package.json lint/prettier/prettier-check globs are the same four roots. Every file the flip edits (config/vite/*.js, vitest.config.ts, eslint.config.mjs, server/**) is invisible to every gate (tooling critic CROSS). Rename config/vite/solid.js
   - E0.3 [chore/P0/M] Vitest projects: client (jsdom) for tests/*.ts[x], server (node) for tests/server/*; jsdom + setup file  deps=['E0.2']  files=3
       vitest.config.ts sets test.environment 'node' plus hand-written resolve.conditions ['development','browser'] (tooling-docs HAZ vitest.config.ts:15; tooling critic MISSED vitest.config.ts:14). Under @solidjs/vite-plugin the 'node' environment selects the SERVER build (inert writes) but tests/server/{routes,crud,adapter,schema}.te

## E1 [P0] Characterization tests: pin 1.9 behavior for every hazard before the flip
   goal: Land, on main and green under 1.9, one test per showstopper/high hazard so the flip commit produces a precise list of what broke and each fix issue has a named red test to turn green.
   gate: pnpm test green on main (1.9); each new test file lists in a header comment which digest chain it pins; `git grep -n 'flush()' tests` returns nothing (only settle()).
   - E1.1 [task/P0/S] Test scaffolding: settle(), mountGantt(), withDiagnostics() placeholder  deps=['E0.3']  files=3
       settle.ts exports `settle(): void` as a no-op today; the flip (E2.4) rewrites it to `export { flush as settle } from 'solid-js'`. mountGantt.tsx: `mountGantt(props: Partial<GanttProps>, opts?: {provider?: boolean, clientWidth?: number})` renders <Gantt> (optionally inside <GanttProvider>) via render() from 'solid-js/web' into a 
   - E1.2 [task/P0/M] Gantt first-paint mount test (chains A, I; showstoppers a/b/c; selection-callback write)  deps=['E1.1']  files=1
       Pins the library's most common path. Cases: (1) bare <Gantt tasks={3 tasks on 2 resources with explicit start/end in 2025}> renders 3 `.bar` elements after settle(), each with `_bar.x` in stores.taskStore equal to dateStore.dateToX(task.start) (not computed from `new Date()`), `_resourceIndex >= 0`, `_isHidden === false` (chain 
   - E1.3 [task/P0/M] ganttSetup + dateStore pipeline characterization (chains A, J1)  deps=['E1.1']  files=2
       Extend tests/ganttSetup.test.ts (unowned makeStores() posture must stay unowned: signal writes inside createRoot throw in rc.6): after initializeTasks on fresh stores + settle(), assert dateStore.dates().length > 0, ganttStart() <= earliest task start, ganttConfig.ganttStart() === dateStore.ganttStart(), and for each task `_reso
   - E1.4 [task/P0/S] taskStore characterization: removeTask deletes key, guard-inside-draft, patch-merge, gantt-6hx negative half  deps=['E1.1']  files=1
       Add to tests/taskStore.test.ts: (1) after removeTask('a') + settle(): `'a' in store.tasks === false`, Object.keys(store.tasks) excludes 'a', taskCount() and getAllTasks() agree (taskStore.ts:134 `setTasks(id, undefined)` keeps the key in rc.6). (2) Same-turn create+position: `updateTasks([a]); updateBarPosition('a', {x: 50}); se
   - E1.5 [task/P0/S] ganttConfigStore characterization: expandedTasks reactivity, setter composition, updateOptions  deps=['E1.1']  files=1
       New file. Store writes ARE legal inside createRoot bodies, but keep writes outside the root anyway for uniformity. (1) createRoot owns `const has = createMemo(() => cfg.expandedTasks().has('t1'))`; outside: toggleTaskExpansion('t1'); settle(); expect(has()).toBe(true); collapseTask('t1'); settle(); false (ganttConfigStore.ts:146
   - E1.6 [task/P1/M] Virtualization cache invalidation characterization (useTaskVirtualization + TaskLayerMinimal)  deps=['E1.1', 'E1.2']  files=2
       useTaskVirtualization.ts:70-96 and TaskLayerMinimal.tsx:45-73 cache the resource grouping keyed on task COUNT; rc.6 coalesces a same-tick add+remove into one flush so the cache is permanently stale (lib-virtualization HAZ). Test at the hook level: inside createRoot, build taskStore/resourceStore/ganttConfig, run initializeTasks 
   - E1.7 [task/P0/L] Drag-end geometry characterization (chains B, C, D; DbDemo H root cause)  deps=['E1.1', 'E1.2']  files=2
       Two levels. (A) tests/drag.test.tsx via mountGantt with onDateChange/onProgressChange/onResizeEnd spies: dispatch mousedown on a `.bar`'s parent group (Bar.tsx:416 role=button), settle(), document mousemove (+40 clientX), settle(), mouseup, settle(). useDrag.ts:159-177 runs the final onDragMove (writes) then onDragEnd (reads bac
   - E1.8 [task/P2/S] ArrowLayer / ArrowLayerBatched dependency characterization (demo-only path)  deps=['E1.1']  files=1
       The shipped Gantt mounts no arrow layer (tracer chain B: only demo/GanttPerfIsolate.tsx:2015 does), but ArrowLayer.tsx:103 hides all task dependencies behind untrack and ArrowLayerBatched.tsx:123 depends solely on a manual positionVersion counter, so under rc.6 the first eager memo run reads pre-flush state and never recovers (l

## E2 [P0] The flip: dependencies, build config, mechanical API sweep, test migration (ONE commit on branch solid-2)
   goal: Move the whole repo to solid-js 2.0.0-rc.6 / @solidjs/web / @solidjs/vite-plugin with purely mechanical edits so typecheck, lint, build and build:demo pass, the test runner runs, and the only failing tests are the enumerated behavioral characterization tests.
   gate: pnpm typecheck && pnpm lint && pnpm prettier-check && pnpm build && pnpm build:demo green; pnpm test runs both projects with EXACTLY the failures enumerated in E2.1 (paste the vitest summary into the issue); `git grep -nE "solid-js/(web|store)|\bIndex\b|\bonMount\b|\bbatch\(|createComputed|\bproduce
   - E2.1 [task/P0/M] Flip part 1: package.json deps, config/vite externals/globals, tsconfig jsxImportSource, vitest client project plugin swap, diagnostics devDep  deps=['E1.2', 'E1.3', 'E1.4', 'E1.5', 'E1.6', 'E1.7', 'E1.8']  files=8
       package.json: dependencies solid-js 2.0.0-rc.6 + @solidjs/web 2.0.0-rc.6 (exact); devDependencies @solidjs/vite-plugin 3.0.0-next.38, @solidjs/diagnostics 2.0.0-rc.6, jsdom; remove vite-plugin-solid, babel-preset-solid (no babel config exists), @solid-primitives/{memo,resize-observer,scheduled,scroll} (repo-wide grep proves unim
   - E2.2 [task/P0/L] Flip part 2: mechanical sweep of src/ library + src/entries  deps=['E2.1']  files=8
       Purely mechanical, no behavior redesign (that is E3). (1) 19 entries: 'solid-js/web' -> '@solidjs/web'. (2) 'solid-js/store' -> 'solid-js' (taskStore.ts:2, ganttConfigStore.ts:2, others per audit). (3) 33 `JSX` imports -> `import type { JSX } from '@solidjs/web'`; Dynamic from '@solidjs/web'. (4) <Index> -> <For keyed={false}> a
   - E2.3 [task/P0/L] Flip part 3: mechanical sweep of src/demo + benchmarks/profiler  deps=['E2.1']  files=5
       Same rule set as E2.2 applied to demos so build:demo passes and every example page loads: ShowcaseDemo 38 path setters + 7 object-merge setters -> draft callbacks, onMount -> onSettled with block bodies, batch removal, drop dead `Show` (line 6) and dead createGanttConfigStore (546); ConstraintDemo/BarDemo/ArrowDemo imports (drop
   - E2.4 [task/P0/M] Flip part 4: test migration — settle() = flush(), no signal writes in createRoot bodies, diagnostics capture helper  deps=['E2.1']  files=5
       tests/helpers/settle.ts -> `export { flush as settle } from 'solid-js'`. tests/helpers/diagnostics.ts -> `withDiagnostics(fn)` uses `DEV.diagnostics.capture()` from 'solid-js', runs fn, calls settle(), returns `capture.stop().filter(e => e.severity !== 'info')`; export `expectNoDiagnostics(events, allow: DiagnosticCode[] = ['NO_

## E3 [P0] Library runtime correctness: showstoppers and read-after-write, each red -> green
   goal: Restore first paint, mount without throws, correct drag/resize/progress commit geometry, live expand/collapse, correct virtualization, and pass the diagnostics gate, one named test per issue.
   gate: pnpm typecheck && pnpm lint && pnpm test (both projects fully green) && pnpm build; pnpm dev smoke: examples/gantt.html, subtask.html, resource-groups.html render bars on first paint with zero console diagnostics; a drag on gantt.html persists the dropped position (log onDateChange in console).
   - E3.1 [bug/P0/S] Contexts: null defaults so bare <Gantt> and bare <Bar> mount; keep public contracts  deps=['E2.1']  files=4
       GanttStores.tsx:30 `createContext<GanttStores>()` and GanttEvents.tsx:23 `createContext<GanttEventHandlers>()` have no default; rc.6 useContext THROWS ContextNotFoundError outside a provider. Gantt.tsx:170 `useGanttStores() ?? {...}` is the library's bare-<Gantt> path (showstopper c). Rewrite both to `createContext<T | null>(nul
   - E3.2 [bug/P0/M] useGanttScroll inversion: take the containerApi accessor; GanttContainer onSettled with one returned cleanup  deps=['E3.1']  files=4
       Showstopper (b): GanttContainer.tsx:187 calls props.onContainerReady from onMount -> onSettled (children-forbidden); useGanttScroll.handleContainerReady (useGanttScroll.ts:50-98) creates six createEffects there -> PRIMITIVE_IN_FORBIDDEN_SCOPE on first mount. Rewrite: `useGanttScroll(containerApi: Accessor<ContainerAPILike | null
   - E3.3 [bug/P0/M] ganttDateStore compute-then-apply: parameterized generateDates, setupDates/changeViewMode return their window  deps=['E2.1']  files=2
       Four high hazards in one file (digest-t2 lib-stores-config-date): setupDates writes ganttStart/ganttEnd at :193-194 then generateDates reads them back at :202-203 (first run generates from the `new Date()` defaults -> EMPTY dates array); changeViewMode stages viewMode at :247/:251 then generateDates reads the step()/unit() memos
   - E3.4 [bug/P0/M] ganttSetup pipeline: consume returned window, compute the resource index map locally, single config write  deps=['E3.3']  files=3
       Chain A: ganttSetup.ts:66 setupDates then :69-73/:75-84 re-read five dateStore accessors (stale), and :101 updateResources then :102 resourceIndexMap() (stale, EMPTY non-null Map -> taskProcessor.ts:231-262 assigns _resourceIndex -1 / _isHidden true / y -1000 for EVERY task -> useTaskVirtualization.ts:87 filters everything -> bl
   - E3.5 [bug/P0/L] Gantt.tsx effect splits: setup/options/view-mode/selection/row-sync; delete duplicate onMount; onReady via onSettled + queueMicrotask  deps=['E3.1', 'E3.2', 'E3.4']  files=2
       All writes reached from effect COMPUTES throw REACTIVE_WRITE_IN_OWNED_SCOPE (untrack does not clear the owner): Gantt.tsx:298/302/324 runSetup, :309 updateOptions, :320-321 setPrevViewMode/changeViewMode, :395 updateBarPosition, :184-188 props.onSelectionChange. (1) Delete the onMount at :288 (duplicate of the :290 effect; under
   - E3.6 [bug/P0/M] taskStore: leaf mutation inside the draft, guard in draft, patch-merge updateTask, strengthened gantt-6hx guard  deps=['E2.1']  files=3
       lib-stores-task STRUCT: the current object-replacement shape (`state[id] = {...task, _bar: {...}}`, commit 6e5538c / gantt-6hx cause #2) invalidates every leaf of every dragged task each mousemove; measured on rc.6, draft-mutating `_bar.x` re-runs only the _bar.x subscriber. Rewrite updateBarPosition as `setTasks(draft => { cons
   - E3.7 [bug/P0/M] ganttConfigStore: expandedTasks as a signal-held immutable Set, draft-based setters, delete committed-read guards, Date invariant  deps=['E2.1']  files=3
       ganttConfigStore.ts:146 holds `expandedTasks: Set<string>` INSIDE createStore and :175-201 mutate it in place; rc.6 does not proxy Sets so expand/collapse notifies nothing (Gantt.tsx:371 rowLayouts and useTaskVirtualization.ts:66/161 go dead). Move it out of the store into `createSignal<ReadonlySet<string>>(new Set(options.expan
   - E3.8 [bug/P0/L] Gesture-commit boundary: thread committed geometry through onDragEnd/onResizeEnd/onDateChange; useDrag mirrors; flush() stopgap  deps=['E3.6']  files=10
       Chains B, C, D, H: useDrag.ts:161 runs the final onDragMove (writes) then :177 onDragEnd (reads back) in one stack, so drop position (useBarDrag.ts:271), progress (:281), keyboard-resize cascade (Bar.tsx:300-302 -> taskLayerConstraints.ts:87) and summary-bar cascade (SummaryBar.tsx:142-150) are one frame stale, and DbDemo persis
   - E3.9 [bug/P1/M] Virtualization: tracked grouping memo shared by useTaskVirtualization and TaskLayerMinimal; pool counters via prev; ColumnPanel strict-read fix; For-holes proof  deps=['E3.6', 'E3.7']  files=6
       useTaskVirtualization.ts:78 and TaskLayerMinimal.tsx:53 key a closure cache on task COUNT (`taskKeys.length === cachedTaskCount`); rc.6 coalesces same-tick add+remove so the cache never invalidates. Extract `groupTasksByResource(tasks, keys)` into src/utils and in both consumers use `createMemo(() => { const t = props.taskStore?
   - E3.10 [bug/P2/M] ArrowLayer / ArrowLayerBatched: real dependencies, per-instance caches, drop positionVersion protocol  deps=['E3.6']  files=4
       Demo-only today (no library mount), but exported components with silent-blank failure. ArrowLayer.tsx:103: hoist the key-set read out of untrack (`const ids = Object.keys(tasks)`, tracked via ownKeys) or delete the untrack entirely and let flush coalescing do the batching (lib-arrows STRUCT recommends delete + re-measure). Arrow
   - E3.11 [task/P2/S] Snapshot at memo boundaries and small proxy hygiene: useGanttModals, useBarConfig, Bar top-level reads  deps=['E3.5']  files=5
       useGanttModals.ts:47/52/57/62 memos return live store proxies into TaskPopup/TaskModal (STRICT_READ_UNTRACKED risk in apply phases) and the barPosition memos allocate a fresh object per call (notify every frame): wrap with snapshot() at the memo boundary and add an `equals` on x/y/width. useBarConfig: leave eager for now; E5.2 d
   - E3.12 [task/P1/S] Diagnostics gate: zero error/warn diagnostics across mount, setup, drag, expand/collapse, custom columns  deps=['E3.5', 'E3.7', 'E3.8', 'E3.9', 'E3.11']  files=3
       Turn withDiagnostics from an inert wrapper into a gate. tests/diagnostics.gate.test.tsx runs the following scenarios and asserts `expectNoDiagnostics(events)` (allowing only NO_OWNER_EFFECT for unowned test stores): mountGantt with 50 tasks/5 resources, a full drag gesture, expandTask/collapseTask on a summary task, options.view

## E4 [P2] Demos: fix shared architectural defects, then run the smoke matrix
   goal: Every examples/*.html loads, renders, and interacts under rc.6 with zero console diagnostics; the three repeated demo defects are fixed once each.
   gate: pnpm build:demo green; E4.8 smoke matrix executed with results recorded in the issue; zero Solid diagnostics in the console on every page.
   - E4.1 [task/P2/M] Shared demo helpers: createDemoViewport() (onSettled + ResizeObserver + RAF + one cleanup) and plain-closure latches  deps=['E3.12']  files=7
       GanttMinimalTest.tsx:417/439, GanttExperiments.tsx:964/978, GanttPerfIsolate.tsx:1758/1769, GanttPerfDemo.tsx:269/276/298, GanttProfiler.tsx:165/177 are the same body; three leak an uncancelled RAF (GanttMinimalTest:468, GanttPerfIsolate:1806, GanttExperiments stress loop). Write one helper that takes a container accessor and re
   - E4.2 [bug/P2/M] ShowcaseDemo: derive taskStore mirrors with split effects; single-draft applyPreset; geometry-carrying handleResizeEnd  deps=['E3.8']  files=1
       Hazards #1-#7: seven input handlers write taskConfig/taskBConfig then call updateTaskA/B (665-693) which re-read the store (stale by one edit; the locked toggles invert drag behavior). Replace both imperative mirrors with `createEffect(() => ({name: taskConfig.name, ...plain fields}), (cfg) => { taskStore.updateTask('task-a', cf
   - E4.3 [bug/P2/M] ConstraintDemo pure planner + split-effect seeding; BarDemo/ArrowDemo hygiene  deps=['E3.8']  files=3
       ConstraintDemo:777-779 writes selectedScenario then updateTasks() reads the allTasks memo (stale -> invisible bars); replace the onMount seed (662) + updateTasks helper (667) with `createEffect(() => allTasks(), (tasks) => { taskStore.updateTasks(tasks); })` and make onChange only set the signal. resolveMovement (90-275) interle
   - E4.4 [bug/P2/M] DbDemo: consume geometry from onDateChange payload; record patch coordinates; TaskForm updaters  deps=['E3.8']  files=4
       Chain H (highest consequence: stale value persisted to SQLite and the error compounds per drag). DbDemo.tsx:195-196 scrapes window.__ganttTaskStore.tasks[id]._bar inside the mouseup stack; :239-242 re-bases lastX/lastWidth from committed state after the await. Use the third `{x,width}` argument Gantt.handleDateChange now forward
   - E4.5 [task/P2/M] Perf harness demos: GanttPerfIsolate cascade geometry, GanttExperiments equals/caches, index-test probes, raf devDep  deps=['E4.1', 'E3.10']  files=4
       GanttPerfIsolate.tsx:1645->1648 writes the dragged bar then calculateCascadeUpdates reads it back through mockTaskStore.getBarPosition: overlay the new geometry in the context (`getBarPosition: id => id === taskId ? {...pos, x: finalX, width: finalWidth} : mock.getBarPosition(id)`). Normalize the six legacy variants that treat p
   - E4.6 [task/P2/S] Feature demos: CriticalPath/FilterSearch getters, selection/export callbacks, GanttSubtaskDemo, CustomColumns  deps=['E3.12']  files=7
       CriticalPathDemo:135 embeds `criticalPath()` in the options object literal (whole prop becomes a rebuilt getter) -> per-key getter like FilterSearchDemo:189-194; :112 toggle -> `setCriticalPath(v => !v)`. FilterSearchDemo:105 predicate -> createMemo so options.filter identity is stable. BoxSelect/MultiSelect/Export callbacks alr
   - E4.7 [chore/P3/S] benchmarks/profiler: ESM fix, memoTracker loadingValue, flush-aware instrumentation note  deps=['E4.1']  files=3
       benchmarks/profiler/index.js:71-82/241 mixes ESM with require() (throws ReferenceError in the browser today and will be misread as a Solid 2 regression) — use the static imports (done mechanically in E2.3; verify window.__ganttPerf works). memoTracker.js:136 createTrackedMemo forwards an initial value: use `{ loadingValue }` (cr
   - E4.8 [chore/P1/M] Execute the demo smoke matrix and record results  deps=['E4.2', 'E4.3', 'E4.4', 'E4.5', 'E4.6', 'E4.7']  files=1
       Run `pnpm dev` (and `pnpm dev:server` for db) and open every example with DevTools console visible; also run chrome-devtools-cli to capture console for each URL. Matrix (page -> checks): index.html (links resolve); gantt.html (3+ bars on first paint at correct dates, drag persists in onDateChange log, view-mode switch re-lays ou

## E5 [P2] Performance re-baseline and built artifacts
   goal: Replace the invalidated 1.9 performance corpus with rc.6 numbers measured on a rebuilt dist-demo, and decide the lazy-memo policy from data.
   gate: benchmarks/traces/runs/ contains fresh bench-*.json for the three scripts; docs tables date-stamped; `git status` clean after the dist-demo refresh commit; pnpm build:demo reproduces the committed artifacts byte-for-byte modulo hashes.
   - E5.1 [decision/P2/S] Harness timing decision: keep work inside the measured frame (flush at end of RAF tick) and settle the splitEquals question  deps=['E4.5']  files=4
       Deferred writes push the scroll -> signal -> memo -> DOM pipeline out of the RAF/scroll callback into the microtask flush, so createRAF frame attribution changes. Decide once: (recommended) call `flush()` at the end of each harness RAF/interval tick so the DOM update is inside the measured frame and comparable in shape to 1.9, a
   - E5.2 [decision/P2/M] Lazy-memo policy measured: useBarConfig (7/bar), Bar (4/bar), chrome memos  deps=['E5.1']  files=5
       rc.6 memos are eager; { lazy: true } restores lazy first-evaluation AND opts into autodisposal (torn down when unobserved, recomputed on next read) — a churn cost for virtualized rows (critic CROSS). Measure mount time and scroll frame time on examples/perf.html with 1000 rows for three configs: all eager (default), lazy on the 
   - E5.3 [chore/P2/M] Run the bench scripts against a fresh build:demo; date-stamp 1.9 tables; append rc.6 tables  deps=['E5.2', 'E4.8']  files=5
       `pnpm build:demo && npx serve dist-demo -l 5174 &` then benchmarks/scripts/run-comprehensive.sh, run-virt.sh, run-virt-comprehensive.sh (they drive perf.mjs at http://localhost:5174/examples/experiments?variant=..&virt=..&test=..). Mark every existing results table with 'measured on solid-js 1.9.12' and append rc.6 tables with t
   - E5.4 [chore/P1/S] chore(demo): refresh built artifacts (dist-demo)  deps=['E5.3']  files=2
       dist-demo/ is tracked (9 files: 3 examples + 6 hashed chunks) and embeds the 1.9 runtime. After E5.3's final build, commit the rebuilt output as the last commit on the branch with the conventional message 'chore(demo): refresh built artifacts'. Remove the stale hashed chunks. If decision D12 chooses to gitignore dist-demo instea

## E6 [P2] Docs, agent guidance, memory, and follow-ups
   goal: Every document that teaches a 1.x pattern now teaches the 2.0 rule, agents get the deferred-write/write-guard/onSettled/flush rules up front, and the RC pin has a scheduled re-bump.
   gate: `git grep -nE 'solid-js/(web|store)|<Index|onMount|produce\(|Context\.Provider' README.md CLAUDE.md AGENTS.md docs` returns nothing except dated history sections; pnpm prettier-check green; a reviewer reads CLAUDE.md's new Solid 2 section and can answer the six rules without the digest.
   - E6.1 [task/P1/M] CLAUDE.md + AGENTS.md: Solid 2.0 rules, test posture, diagnostics gate, drift fixes  deps=['E3.12']  files=2
       Add one shared 'SolidJS 2.0 rules' section (same text in both files): deferred writes (reads return committed until flush), write guard (untrack does not help; store setters exempt in createRoot bodies, signal setters not), onSettled constraints (untracked body, children-forbidden, single returned cleanup, no flush), the flush()
   - E6.2 [task/P2/S] README: install section (solid-js + @solidjs/web), useGanttStores/setter contracts, deferred-read note, packaging  deps=['E2.1']  files=2
       README has no install section (critic MISSED README.md:5): add `pnpm add ganttss solid-js@2.0.0-rc.6 @solidjs/web@2.0.0-rc.6` and a resolve.dedupe note (or peerDependencies per D4). Update :106 (useGanttStores returns undefined outside a provider — still true, now via a null-default context), :108 (config setters are void update
   - E6.3 [task/P2/M] docs/*.md sweep: ARCHITECTURE, PERFORMANCE, MINIMAL_TEST, DATABASE, DEMOS, EXPERIMENTS, ANALYSIS, RFC-undo-redo, AUDIT  deps=['E4.4', 'E5.3']  files=9
       ARCHITECTURE.md:398-431 rewrite the ganttConfigStore section (store, draft setters, expandedTasks signal, updateOptions single draft, getConfig is a committed snapshot; rename the local `snapshot` example since snapshot is now a solid-js export), :365 updateTasks via reconcile(v,'id'), :379 Set inventory table, :67 provider; add
   - E6.4 [task/P2/S] Rewrite the memory 'solidjs-createstore-produce-state-id-newobj-never-wrap'  deps=['E3.6', 'E6.1']  files=1
       The memory encodes the 1.x rule 'replace state[id] with a new object inside produce, never wrap' (gantt-6hx). In 2.0 the rule inverts: leaf-mutate inside the draft (`draft[id]._bar.x = v`), never replace the task object (it invalidates every leaf of the task per mousemove), keep Bar reading leaves (a memo returning the _bar prox
   - E6.5 [chore/P3/S] Follow-up: re-bump to the newest RC/stable, re-run verification, relax pins  deps=['E5.4', 'E6.3']  files=2
       When a newer solid-js/@solidjs/web/@solidjs/vite-plugin/@solidjs/diagnostics/@solid-primitives/raf set is published, bump all together, run the full end-of-migration verification plan (typecheck, lint, both Vitest projects, build, build:demo, smoke matrix, bench scripts, dist-demo refresh), and only then relax solid-js/@solidjs/
   - E6.6 [decision/P4/M] Optional modernizations (each its own decision): peerDependencies, self-contained bundle, dead-code deletion, @ts-nocheck removal, DbDemo actions/optimistic, .jsx example URLs  deps=['E6.5']  files=5
       Not required by the migration; list here so they are visible and never folded into a required issue: (a) peerDependencies for solid-js/@solidjs/web (D4); (b) a self-contained IIFE bundle if UMD consumers exist (D3); (c) delete taskStore collapse API, GanttSubtaskDemo, legacy GanttPerfIsolate variants if not done under D5; (d) re

TOTAL child issues: 45
CRITICAL PATH: E0.2 -> E0.3 -> E1.1 -> E1.2 -> E1.3 -> E1.4 -> E1.7 -> E2.1 (+E2.2/E2.3/E2.4 same commit) -> E3.1 -> E3.2 -> E3.3 -> E3.4 -> E3.5 -> E3.6 -> E3.8 -> E3.12 -> E4.2/E4.3/E4.4 -> E4.8 -> E5.3 -> E5.4 -> E6.1 -> E6.5
SEQUENCING: Gates first (E0) because the flip edits files no gate sees and the reactive suites currently resolve a server build. Characterization tests next (E1) on 1.9 so the flip's test output is a precise, named list of regressions rather than a mix of timing and behavior; settle() keeps those tests untouched by the flip. The flip (E2) is one atomic commit because deps, configs, imports and removed-API rewrites cannot pass the build separately; it is mechanical only, so its diff is greppable and its failures are exactly the pinned behaviors. E3 orders the showstoppers by dependency: contexts (tiny, unblocks mounting) -> scroll inversion (unblocks the mount without throwing) -> dateStore -> ganttSetup -> Gantt effects (first paint green) -> taskStore leaf mutation -> gesture boundary (needs the store API) -> virtualization (needs stores) -> arrows/hygiene -> diagnostics gate. Demos (E4) come after
VERIFICATION: Per issue: the named red test(s) go green, plus `pnpm typecheck && pnpm lint && pnpm prettier-check && pnpm test && pnpm build` (add `pnpm build:demo` from E2 onward). End of migration, on branch solid-2 before merge: (1) pnpm typecheck, lint, prettier-check green with the widened globs; (2) pnpm test prints both projects (client jsdom: 15 suites incl. gantt.mount, drag, ganttDateStore, ganttConfigStore, useTaskVirtualization, useGanttScroll, arrowLayer, forPooling, diagnostics.gate, contexts; server node: 4 suites) all green; `git grep -n 'flush()' tests` only in helpers; (3) pnpm build: dist/ganttss.es.js externalizes solid-js and @solidjs/web (grep), no UMD unless D3 keeps it; (4) pnpm build:demo green; (5) demo smoke matrix (E4.8) fully passed with zero console diagnostics on all 21 pages: index, gantt, subtask, resource-groups, showcase, constraint, bar, arrow, box-select, multi-sel
DECISIONS:
   ? D1: Exact pins or caret for the RC line? => Exact pins (2.0.0-rc.6 / 3.0.0-next.38 / 4.0.0-next.2 / diagnostics rc.6) until E6.5; then caret. (The audit's runtime facts (write guard asymmetry, onSettled untracked, return-value validation) were verified against rc.6 specifically; RC-to-RC drift would si)
   ? D2: Feature branch or main? => E0+E1 on main (green on 1.9), then branch solid-2 for E2-E6, merge commit when fully green. (The flip commit necessarily has failing characterization tests and a non-functional chart; main must stay releasable and the tests should exist before the branc)
   ? D3: Drop UMD or ship a self-contained bundle? => Drop 'umd' from formats, keep 'es' only; point package.json main/exports.require at the ES build (or remove require); note in README. (Neither solid-js nor @solidjs/web publishes a global/IIFE build, so the UMD globals contract has been decorative and doubles now; a self-contained bundle is sco)
   ? D4: peerDependencies for solid-js/@solidjs/web? => Not in this migration; keep exact pins in dependencies, add a resolve.dedupe note to README, and revisit in E6.5/E6.6. (Two externalized runtimes that must be the host's instance argue for peers, but changing dependency classification during an RC migration multiplies install-tim)
   ? D5: Delete dead code (taskStore collapse API, useGanttScroll isScrolling + dead ContainerAPI surface, GanttSubtaskDemo,  => Delete isScrolling and the dead ContainerAPI surface in E3.2 (they are the hardest scroll sites to migrate and have zero readers); delete the taskStore collapse API in E3.6 (tests-only consumer; remov (Roughly a third of the store test migration and two of six scroll hazards are spent on code with no production caller; deleting is cheaper than porting and shri)
   ? D6: expandedTasks representation: signal-held immutable Set or Record<string,true> in the store? => Signal-held ReadonlySet with whole-Set replacement (public accessor type unchanged, matches the three existing safe patterns). (Consistency with taskStore.collapsedTasks/resourceStore.collapsedGroups/selectionStore.selectedIds and zero public type change; Record gives per-key tracking bu)
   ? D7: Adopt @solidjs/testing-library for the mount tests? => No; use render() from @solidjs/web + a mountGantt helper, and DEV.diagnostics.capture() for the gate; add @solidjs/diagnostics only for its Vitest matchers if they load under vitest 4.1. (The tests assert store state and SVG elements, not accessible roles; one fewer beta dependency on the RC line and the helper works on 1.9 for the pre-flip chara)
   ? D8: Lazy-memo policy? => Default eager everywhere; { lazy: true } only on getAllDateInfos and the modal/popup formatters now; decide per-Bar memos from E5.2 measurements. (lazy also opts into autodisposal, which churns for virtualized rows; four audits recommended it without stating the second effect, so it must be measured, not g)
   ? D9: Row-sync effect (Gantt.tsx:382-400): keep as a split effect writing _bar.y, or delete and feed rowLayouts to useBoxS => Keep as a split effect with one draft write in E3.5 (smallest change, keeps _bar.y for exported components); reconsider deletion in a follow-up. (Bar already prefers props.taskPosition; the only readers are useBoxSelect hit-testing and demo arrow layers, but removing _bar.y changes exported data shape and)
   ? D10: Remove // @ts-nocheck from demos during the port? => Remove it only in files being structurally rewritten (ShowcaseDemo, ConstraintDemo, DbDemo); the rest stay nocheck and are proven by the smoke matrix; full removal is E6.6. (2000-line untyped demos would dominate the diff with unrelated type fixes; the browser smoke matrix with the diagnostics console is the verification for demos.)
   ? D11: DbDemo modernization to 2.0 actions/optimistic stores? => Out of scope; E4.4 only fixes the drag-PATCH correctness via the geometry payload. (Explicit user rule: no surprise scope creep; the correctness fix is small once E3.8 threads geometry.)
   ? D12: Keep dist-demo tracked or gitignore it? => Keep tracked and refresh in E5.4 (existing convention 'chore(demo): refresh built artifacts'). (benchmarks/scripts/*.sh and CLAUDE.md assume a servable dist-demo; changing the convention is unrelated to the migration.)
RISKS:
   ! jsdom cannot support the Bar drag test (SVG createSVGPoint/getScreenCTM, layout-free clientWidth) so E1.7 cannot be written before the flip. => tests/setup.client.ts stubs the three SVG methods (useDrag.ts:117-123 only needs matrixTransform identity) and defineProperty on clientWidth; fallback is a useDrag-level test with a fake currentTarget
   ! The flip commit is large and its failing-test list is misread as 'migration broken' by a reviewer. => E2.1 pastes the exact vitest summary and maps every failure to the E3 issue that turns it green; the branch policy keeps main green.
   ! Someone ports updateBarPosition's object-replacement shape verbatim into the draft; it compiles and passes the old guard while keeping the per-mousemove full-le => E1.4 lands the negative half of the gantt-6hx guard skipped; E3.6 un-skips it and the reviewer must demonstrate red with the replacement shape.
   ! flush() sprinkled in effect apply (silent no-op) or onSettled (throws) reintroduces stale-read bugs that look fixed. => Rule in CLAUDE.md/AGENTS.md with the three-case table; the only sanctioned flush() sites are useDrag.ts mouseup, DbDemo onDateChange, and test helpers; E3.12 diagnostics gate catches the throw case.
   ! <For keyed={false}> does not preserve Index's pooling over sparse arrays, tearing down Bar listeners mid-drag. => tests/forPooling.test.tsx (E3.9) proves holes and index reuse before shipping; fallback is <Repeat count from>.
   ! @solidjs/diagnostics/vitest matchers or @solidjs/compiler's native binary fail under vitest 4.1 / pnpm 10 build-script blocking. => Gate falls back to DEV.diagnostics.capture() (documented API); E2.1 verifies pnpm install output and adds the compiler to onlyBuiltDependencies if blocked.
   ! Eager memos regress 1000-row mount time and the perf corpus is compared to 1.9 numbers as if equivalent. => E5.2 measures three lazy configurations before choosing; E5.3 date-stamps old tables and documents the Index->For and flush-timing changes next to every affected finding.
   ! Public API contract changes (void setters, onResizeEnd geom, onDateChange third arg, useGanttStores null-context) surprise external consumers. => All additive or return-type-only; README/ARCHITECTURE document them (E6.2/E6.3); no in-repo caller uses a setter return value (audit grep).
FOLLOWUPS: E6.5: re-bump to newest RC/stable and relax pins; consider replacing tests/helpers/settle with direct flush(). | Consider peerDependencies + resolve.dedupe guidance once the RC line stabilizes (D4). | Evaluate deleting the Gantt row-sync effect and feeding rowLayouts to useBoxSelect (D9). | Benchmark <Repeat count from> against <For keyed={false}> for DateHeadersOptimized and TaskLayer pools; adopt if faster. | Full // @ts-nocheck removal across src/demo and src/entries so tsc proves the demos. | Normalize the 11 examples/*.html .jsx entry URLs to .tsx. | DbDemo modernization to 2.0 actions/optimistic stores (separate epic if wanted). | Revisit ArrowLayer as a createProjection/keyed store spatial index once E3.10 numbers are in. | Add a Vitest browser-mode (Playwright) project for the drag tests if jsdom stubs prove brittle. | Wire the diagnostics gate into CI as a separate job once publish.yml runs on PRs.

### REVIEW (score 8): The strongest of the plans on runtime fidelity: it encodes the corrected facts (createRoot signal-vs-store asymmetry, flush's three scopes, onSettled untracked + children-forbidden, return-value validation, delete draft[id], unproxied Set, <For>'s strict-read label, null-default contexts) and its site coverage of the digests is close to complete, with real gates (grep sweeps, diagnostics capture, a negative gantt-6hx assertion, a For-pooling proof) rather than prose. Three things stop it being executable as written: E2.3's Index inventory misses DateHeadersOptimized/GanttMinimalTest/GanttPerfIsolate so the atomic flip commit cannot pass its own `pnpm build:demo` gate; E3.5 is ordered before the taskStore API (E3.6) it depends on; and E1's 'green on main' gate is contradicted by E1.5/E1.8 admitting red tests on 1.9. Secondary: the flip is one ~120-file commit that smuggles public-API changes under 'purely mechanical'; the same-turn view-mode path is not actually closed by E3.3-E3.5; E4.4 prescribes a store-proxy read in an effect apply that its own E4.8 gate forbids; and nothing in the plan produces the beads epic/issue tree the user asked for. Fix those and this is the plan to ship
  BLOCKING (3):
    * E2.3 misses live <Index> sites that the flip must convert, so its own gate (`pnpm build:demo` green) cannot pass: src/demo/DateHeadersOptimized.tsx:2/166/203 (import + 2 JSX sites, bundled by experiments.html and perf.html), src/demo/GanttMinimalTest.tsx:2/672, src/demo/GanttPerfIsolate.tsx:7 (dead `Index` import). Verified by grep: the 12 JSX Index sites are TaskLayer 217/249, TaskLayerMinimal 13
    * Dependency inversion that leaves a commit boundary broken: E3.5 (Gantt effect splits) dependsOn E3.1/E3.2/E3.4 but not E3.6, yet its item (6) requires `ONE setTasks(draft => …) leaf write of _bar.y` and its acceptance requires first paint green. At the E3.5 commit taskStore still has the committed-state guard at src/stores/taskStore.ts:108 (chain F: a task created in the same flush is silently ski
    * E1's verification gate ('pnpm test green on main (1.9)') is contradicted by its own children: E1.5 acceptance says '(if (1)/(2) are already red on 1.9 that is a pre-existing bug: record it and keep the test)' and E1.8 says 'ArrowLayerBatched-without-positionVersion may be red on 1.9 (documented)'. Landing knowingly-red tests on main breaks the branch strategy's premise ('main stays green throughou
  MAJOR (6):
    * E2.2 is labelled 'purely mechanical, no behavior redesign (that is E3)' but contains public-API and semantic changes: makeSetter retyped to void across 20 interface fields (ganttConfigStore.ts:81-100), removeTask's delete semantics, reconcile's draft-returning form, expandedTasks copy-and-reassign, and untrack wrapping of 7 Gantt props reads. Combined with E2.3 (ShowcaseDemo 38 setters, GanttPerfI
    * The same-turn view-mode path is not actually closed by E3.3+E3.4+E3.5. E3.3 parameterises generateDates only; setupDates still reads viewMode-derived memos (ganttDateStore.ts:152 `dateUtils.start_of(start, unit())`, and unit/step/columnWidth again for the returned window). E3.5(4) says 'pass `win` into runSetup/initializeTasks so no flush() is needed', but E3.4 never gives initializeTasks a precom
    * E4.4 prescribes replacing `queueMicrotask(snapshotChartX)` (DbDemo.tsx:100) with a split createEffect that snapshots `window.__ganttTaskStore.tasks[id]._bar` — a store-proxy read in the apply phase, i.e. STRICT_READ_UNTRACKED (runtime fact 4), which E4.8 requires to be zero and which the plan's own principle forbids. The issue even records that the critic refuted this hazard (setBundle's schedule(
    * No issue creates the beads deliverable. The user asked for the migration *as bd epics/issues*, and the repo convention names `bd create --type/--priority/--parent` and `bd dep add`. The plan carries a key-based dependsOn graph with no bd id mapping, no epic-creation step, no statement of which key becomes an epic vs a task/bug/chore in bd, and no `bd dep add` wiring — so the first executor has to 
    * The 18 `.js`-extension imports of `.ts` files in src/demo (e.g. BarDemo.tsx:3-4 `'../stores/taskStore.js'`, GanttExperiments.tsx:14, GanttProfiler.tsx:13, GanttPerfDemo.tsx:15) are flagged in the digest as 'confirm @solidjs/vite-plugin preserves the .js -> .ts extension rewrite before assuming these files even load'. No plan issue verifies this; if the native-compiler plugin does not rewrite, ever
    * E0.2 is sized 'S' but adds `server/**/*.ts` to tsconfig include, putting drizzle/Hono code (never typechecked — current include is `[src/**/*, tests/**/*.ts, benchmarks/constraint/**/*.ts]`) under `strict` + `noUncheckedIndexedAccess` + `noPropertyAccessFromIndexSignature` for the first time. Acceptance mentions only lint/prettier fallout. Unbounded typecheck fallout inside a gate-widening chore t
  FACTERRORS (6):
    * E2.1 prescribes `import { solid } from '@solidjs/vite-plugin'`. The official v2 docs use a default import — docs/v2/testing.mdx:47 and docs/v2/plugin-index.mdx:38 both `import solid from "@solidjs/vite-plugin"` (plugin-options.mdx:17 uses `import solidPlugin from …`). Inside a single atomic flip commit this is a build break.
    * E4.4: 'replace queueMicrotask(snapshotChartX) with a split createEffect on the task store that snapshots when the layout commits' — snapshotChartX reads `window.__ganttTaskStore.tasks[id]._bar`, i.e. a store proxy, in the apply phase → STRICT_READ_UNTRACKED (fact 4), contradicting the plan's own 'compute returns plain values' rule and E4.8's zero-diagnostics gate.
    * E2.4/E3.12 treat `createMemo(() => setX(1))` as a diagnostics probe; the write guard THROWS in that scope (fact 2), it is not a capturable warn-severity event, so the acceptance 'withDiagnostics returns a non-empty array' cannot hold.
    * E3.3+E3.5 assume changeViewMode's returned window is sufficient, but setupDates itself still reads the viewMode-derived memos (ganttDateStore.ts:152 `start_of(start, unit())`) in the same turn; the prescribed rewrite does not remove the last same-turn memo read on the view-mode path (chain J1).
    * E2.2(10) renames Gantt.tsx:234 `onMount(() => props.onReady?.(api))` to onSettled while explicitly deferring the body to E3.5. That leaves a concise arrow returning the consumer callback's value inside an onSettled body — the rc.6 return-value validation throws for hosts like ExportDemo.tsx:197 — even though E2.3 applies the block-body rule everywhere else in the same commit.
    * E5.4/E5.3 assume benchmarks measure the new runtime once dist-demo is rebuilt, but the perf issues (E5.2, E5.3) run before E5.4 commits the artifacts while benchmarks/scripts/*.sh serve `dist-demo`; E5.3's description does rebuild locally, yet the gate 'fresh bench-*.json … against npx serve dist-demo' and the commit ordering should state that the rebuild precedes the run, not the commit.
  ORDERINGERRORS (6):
    * E3.5 dependsOn [E3.1,E3.2,E3.4] but needs E3.6's taskStore draft/leaf API and the removal of the committed guard at taskStore.ts:108 for its row-sync apply and its first-paint acceptance — dependency points the wrong way
    * E2.1 (P0 flip) dependsOn E1.6 (P1) and E1.8 (P2): a priority-2 arrow-layer characterization test blocks the whole migration. The flip's critical path runs through seven test-writing issues, one of them L-effort and jsdom-brittle (E1.7)
    * E4.7 (benchmarks/profiler ESM + memoTracker loadingValue) dependsOn E4.1 (shared demo viewport helper) — unrelated; it only needs E2.3
    * E4.1 dependsOn E3.12 (the diagnostics gate), which itself dependsOn E3.5/E3.7/E3.8/E3.9/E3.11 — the whole demo epic is serialised behind the last library issue although E4.1/E4.6/E4.7 need only the flip
    * E3.1's acceptance is verified by a mount that still throws (PRIMITIVE_IN_FORBIDDEN_SCOPE) until E3.2 — the stated red→green transition cannot be observed at that commit
    * E5.2 changes library memos (useBarConfig, Bar, Grid, DateHeaders) after E4.8 recorded the 21-page smoke matrix; nothing re-runs the matrix afterwards, so the recorded result no longer describes the shipped code
  MISSINGSITES (11):
    * src/demo/DateHeadersOptimized.tsx:2 / :166 / :203 — Index import + two <Index> slot pools; no issue converts them (E4.5 only memoises upperSlots, E5.3 only benchmarks <Repeat>)
    * src/demo/GanttMinimalTest.tsx:2 / :672 — Index import + <Index> site; E2.3 lists only 'imports + onSettled' for this file
    * src/demo/GanttPerfIsolate.tsx:7 — removed `Index` import in the solid-js import block (E2.3 lists produce/batch/onSettled/provider only)
    * src/demo/GanttMinimalTest.tsx:121 — Date instances stored inside createStore (unproxied in rc.6); digest recommends normalising to epoch numbers while the file is open
    * src/stores/resourceStore.ts:129-132 — collapseAll reads the getGroups() memo then writes setCollapsedGroups; same-turn updateResources+collapseAll collapses stale ids (chain G fix item 2). No issue covers it (E3.6 covers only taskStore.collapseAllTasks, and only via decision D5)
    * src/stores/taskStore.ts:232-243 — collapseAllTasks reads Object.values(tasks) before writing; covered only as 'decide per D5', with no fix prescribed if D5 says keep
    * src/components/Gantt.tsx:193-220 — buildExportInput reads taskStore.tasks/relationships()/api.getScrollLeft… imperatively (chain J2); a host that mutates or drops a drag and exports in the same turn serialises the previous geometry. ExportDemo is only smoke-tested
    * src/index.ts:26 — the useGanttEvents public contract (critic MISSED); E3.1 fixes the context but only says 'index.ts export types unchanged'
    * src/stores/ganttConfigStore.ts:127 (GanttConfigState ganttStart/ganttEnd: Date, ignoredDates: Date[]) — the makeSetter draft rewrite hands raw, unproxied Dates to consumer updater callbacks (critic MISSED); E3.7 documents replace-only in types.ts but not this hand-off
    * src/demo/BarDemo.tsx:3-4 and 16 more `.js`-extension imports of .ts modules across src/demo — flagged in the digest as an unverified @solidjs/vite-plugin resolution risk; no issue verifies it
    * src/components/ColumnPanel.tsx — the ColumnDef.render contract change (now called in a tracking scope, must be pure, no primitives) is public API but appears only as a code comment in E3.9; E6.2/E6.3 do not list it
  SCOPECREEP (5):
    * E0.2 renames config/vite/solid.js and demo.js to .ts (with __dirname → fileURLToPath) and rewrites four package.json scripts. Gate coverage can be had by widening globs/allowJs; the rename is a modernization sitting on the critical path of every later issue and is not marked optional.
    * E4.3 rewrites twelve ArrowDemo `setConfig({...config(), k})` calls to functional setters — the digest records these as safe per-event, so this is cleanup folded into a required bug issue.
    * E4.4's queueMicrotask → createEffect rewrite is applied to a hazard the critic refuted, and is not marked optional.
    * E4.5 deletes the three 'force memo evaluation' probes and normalises/deletes six legacy GanttPerfIsolate variants inside a required task; D5 covers the variants but not the probes.
    * E5.2 mandates a three-configuration chrome-devtools measurement plus applying 'the winner' to library code (useBarConfig/Bar/Grid/DateHeaders) — a perf tuning exercise that changes shipped code, presented as required rather than as a decision the user can decline.
  BESTIDEAS (10):
    * tests/helpers/settle() as a no-op on 1.9 that becomes `export { flush as settle } from 'solid-js'` at the flip — timing edits never mix with behavioral edits, and `git grep -n 'flush()' tests` returning only the helpers is a real, checkable gate.
    * Characterization tests land on main (green on 1.9) before branching, and the flip commit's vitest summary is pasted into E2.1 as the named regression inventory that maps 1:1 to E3 issues.
    * The negative half of the gantt-6hx guard (a memo on tasks['t1'].name whose run count must stay at 1 across updateBarPosition), landed skipped in E1.4 and un-skipped in E3.6 — the only mechanism that stops someone porting the object-replacement shape into the draft.
    * The diagnostics gate (DEV.diagnostics.capture via withDiagnostics/expectNoDiagnostics, E3.12) applied to mount/drag/expand/view-mode/custom-columns scenarios, with a documented fallback when @solidjs/diagnostics' vitest matchers do not load — verified against docs/08-dev-diagnostics.md:329-338.
    * tests/forPooling.test.tsx proving <For keyed={false}> reads holes as undefined and reuses row i before shipping, with <Repeat count from> as the recorded fallback — the pooling contract is load-bearing for Bar's document listeners.
    * The one unifying rule set: producers return computed values, consumers never read back same-turn writes, guards move inside the draft, consumer callbacks are never invoked synchronously from an onSettled body (queueMicrotask), and every callback handed to onSettled/apply gets a block body — with greppable acceptance (`=> set[A-Z]`, `=> props.on[A-Z]`).
    * E0 first: CI Node 22 + typecheck/lint/test in publish.yml, tsconfig/lint/prettier glob widening, the misplaced top-level `output: { interop: 'auto' }` in config/vite/solid.js:28, and vitest 4 test.projects (client jsdom / server node) landed on 1.9 so the flip diff is only about Solid.
    * The twelve explicit openDecisions (UMD, peerDependencies, dead code, expandedTasks representation, testing-library, lazy policy, @ts-nocheck, DbDemo modernization, dist-demo) that keep every non-required change visible and declinable.
    * Exact RC pins across all five packages with a scheduled re-bump issue (E6.5) that re-runs the whole verification plan before relaxing to caret.
    * Treating the 1.9 perf corpus + committed dist-demo as one deliverable: date-stamp the old tables, append rc.6 tables, note the Index→For and flush-timing caveats next to each affected finding, and make 'chore(demo): refresh built artifacts' the last commit on the branch.
  MINOR (12): '19 entries: solid-js/web -> @solidjs/web' is wrong: 20 files in src/entries import solid-js/web, plus src/demo/GanttExp | E2.3 says GanttExperiments has '3 Index sites'; there is one JSX site (1187) plus the import at :7. | E2.4's file list omits tests/helpers/mountGantt.tsx and the six new E1 test files, yet E1.1 writes mountGantt against `r | E2.4 and E3.12 acceptance rely on a probe `createMemo(() => setX(1))` producing a captured diagnostic. A write inside a  | E0.1 acceptance suggests `act` or 'a dry-run branch push' to prove publish.yml green — the workflow triggers on push to  | E5's gate 'pnpm build:demo reproduces the committed artifacts byte-for-byte modulo hashes' is not checkable as written ( | E6.1's 'a reviewer reads CLAUDE.md and can answer the six rules without the digest' and E3.6's 'reviewer temporarily por | E1.7 does not account for useDrag's rAF loop (useDrag.ts:78-92): intermediate onDragMove runs from requestAnimationFrame | E1.2 case 4 asserts a scroll target via 'the container's scrollLeft or api.getScrollLeft()' but tests/setup.client.ts on | E3.1's acceptance ('gantt.mount case 1 no longer throws ContextNotFoundError') is not observable at its own commit: the  | tsconfig.build.json (declaration emit, include src/**/*) is never mentioned; it inherits jsxImportSource so it is fine,  | Chain G's 'document that isTaskCollapsed/isGroupCollapsed answer about committed state and must not be used as a toggle-